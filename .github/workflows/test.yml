name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    permissions:
      id-token: write
      contents: read
    uses: autom8y/autom8y-workflows/.github/workflows/satellite-ci-reusable.yml@807b3d0b8c739cbac039391534c8f22987dd88f5
    with:
      mypy_targets: 'src/autom8_asana'
      coverage_package: 'autom8_asana'
      coverage_threshold: 80
      test_parallel: true
      test_markers_exclude: 'not integration and not benchmark'
      run_integration: ${{ github.event_name == 'push' }}
      test_env: 'ASANA_WORKSPACE_GID=${{ vars.ASANA_WORKSPACE_GID }}'
      spectral_enabled: true
      openapi_spec_path: 'docs/api-reference/openapi.json'
      spec_check_enabled: true
      spec_check_extras: '--extra dev'
      spec_check_env: |-
        AUTOM8Y_ENV=local
        AUTH_DEV_MODE=true
      semantic_score_enabled: true
    secrets: inherit
