name: TruffleHog Secrets Scan (Scheduled)

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trufflehog:
    uses: autom8y/autom8y-workflows/.github/workflows/security-trufflehog.yml@f5601acbe3905270dfcb9069854c78c0f940ad05
