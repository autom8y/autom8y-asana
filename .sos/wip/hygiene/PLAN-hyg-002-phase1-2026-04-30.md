---
type: design
artifact_type: refactor-plan
rite: hygiene
session_id: session-20260430-131833-8c8691c1
target: HYG-002 Phase 1
charter: PYTHIA-INAUGURAL-CONSULT-2026-04-30-hygiene-sprint
authored_by: architect-enforcer
substrate: SCOPE-hyg-002-phase1-2026-04-30.md
evidence_grade: STRONG
evidence_basis: code-smeller scope (Read) + direct file inspection (tests/conftest.py L1-L204; tests/integration/test_lifecycle_smoke.py L1-L1707; src/autom8_asana/{transport/asana_http.py,protocols/auth.py,client.py,lifecycle/{engine.py,config.py},resolution/context.py})
---

# PLAN — HYG-002 Phase 1: spec= Adoption (root fixtures + lifecycle_smoke)

## §1 Plan Purpose

Mitigate SCAR-026 (mocks-without-spec) at root fixtures (`tests/conftest.py`) and the high-density consumer (`tests/integration/test_lifecycle_smoke.py`) by binding `MagicMock(...)` instantiations to canonical source-tree classes via `spec=` where canonical resolution is VERIFIED; defer Pydantic-forward-ref-blocked sites to Phase 2; preserve test behavior so the janitor executes mechanically and audit-lead verifies via `pytest -m scar` + green run.

## §2 Pre-Flight Drift-Audit

Anchors re-probed at plan-authoring (2026-04-30) against scope artifact:

- `AsanaHttpClient` at `src/autom8_asana/transport/asana_http.py:61`; methods `get`(L253), `post`(L275), `put`(L294), `delete`(L311), `request`(L513), `get_paginated`(L322), `post_multipart`(L380), `get_stream_url`(L487) — 8/8 verbatim match against conftest L80–L87. VERIFIED.
- `AuthProvider` at `src/autom8_asana/protocols/auth.py:6`; `get_secret` at L18. VERIFIED.
- `AsanaClient` at `src/autom8_asana/client.py:50`. VERIFIED.
- 6 service Protocols at `src/autom8_asana/lifecycle/engine.py:77/89/100/110/123/135`. VERIFIED.
- `ResolutionContext` at `src/autom8_asana/resolution/context.py:35`; `business_async`(L176), `unit_async`(L206), `offer_async`(L252), `resolve_holder_async`(L294). VERIFIED.
- `InitActionConfig`/`StageConfig` at `src/autom8_asana/lifecycle/config.py:35,101`. VERIFIED.

**Drift detected**: NONE for the 12 canonical types. **Test-file import discipline**: `test_lifecycle_smoke.py` uses lazy in-method imports (verified L129/L922–931); the plan introduces ONE consolidated top-level import block for `spec=` types.

## §3 Ordered spec= Application List

### §3.1 conftest.py — Edit C-1 (root fixture rewrite, L68–L112)

- **L68 import**: `from unittest.mock import AsyncMock` → `from unittest.mock import AsyncMock, MagicMock`
- **Insert after L73**: `from autom8_asana.protocols.auth import AuthProvider` and `from autom8_asana.transport.asana_http import AsanaHttpClient`
- **Replace L76–L94** (class shells `MockHTTPClient` + `MockAuthProvider`) with two top-level factory functions:
  - `_make_mock_http_client() -> MagicMock`: returns `MagicMock(spec=AsanaHttpClient)` (auto-resolves async method attrs to AsyncMock via spec introspection on `async def` — verified all 8 methods are `async def`).
  - `_make_mock_auth_provider() -> MagicMock`: returns `MagicMock(spec=AuthProvider)`.
- **Update fixtures**: `mock_http()` returns `_make_mock_http_client()`; `auth_provider()` returns `_make_mock_auth_provider()`. Return-type annotations → `MagicMock`. `config`/`logger`/`_bootstrap_session`/`reset_all_singletons` UNCHANGED.

**Sites bound**: 8 (AsanaHttpClient methods) + 2 (AuthProvider class shells) = **10**.

### §3.2 test_lifecycle_smoke.py — Edit T-0 (top-level import hoist, after L22)

```python
from autom8_asana.client import AsanaClient
from autom8_asana.lifecycle.engine import (
    CompletionServiceProtocol, CreationServiceProtocol,
    InitActionRegistryProtocol, ReopenServiceProtocol,
    SectionServiceProtocol, WiringServiceProtocol,
)
from autom8_asana.resolution.context import ResolutionContext
```

### §3.3 test_lifecycle_smoke.py — Edit T-1 (Cluster B, AsanaClient)

- **L89**: `client = MagicMock()` → `client = MagicMock(spec=AsanaClient)`
- L90/L101/L109 sub-namespace shells UNCHANGED (concrete attrs, not Protocols; `__setattr__` on spec'd mock is permitted). Per-method `AsyncMock()` at L91–L99/L102/L106/L110 UNCHANGED (Pattern C deferred).

**Sites bound**: **1**.

### §3.4 test_lifecycle_smoke.py — Edits T-2..T-5 (Cluster C, 6 service Protocols × 4 blocks)

Mechanical token-replace `MagicMock()` → `MagicMock(spec=<Protocol>)` at each site:

| Edit | Block | Lines (creation/section/completion/init/wiring/reopen) |
|---|---|---|
| T-2 | `_make_engine` body | L939 / L945 / L951 / L957 / L963 / L967 |
| T-3 | `test_concurrent_engine_calls_isolated` inline | L1495 / L1499 / L1501 / L1505 / L1507 / L1518 |
| T-4 | `test_build_result_maps_error_correctly` inline | L1433 / L1434 / L1435 / L1436 / L1437 / L1438 |
| T-5 | `test_engine_build_result_execution_time_positive` inline | L1600 / L1601 / L1602 / L1603 / L1604 / L1605 |

Map: creation→`CreationServiceProtocol`, section→`SectionServiceProtocol`, completion→`CompletionServiceProtocol`, init→`InitActionRegistryProtocol`, wiring→`WiringServiceProtocol`, reopen→`ReopenServiceProtocol`.

**Sites bound**: 6 × 4 = **24**.

### §3.5 test_lifecycle_smoke.py — Edit T-6 (Cluster D, ResolutionContext)

Mechanical replace `ctx = MagicMock()` → `ctx = MagicMock(spec=ResolutionContext)` at each site: **L493, L527, L551, L585, L618, L828, L849, L874, L899, L1356, L1396, L1575**.

Subsequent attaches `ctx.{offer,unit,business,resolve_holder}_async = AsyncMock(...)` UNCHANGED — all four method names declared on `ResolutionContext` (verified §2).

**Sites bound**: **12**.

### §3.6 Total Phase 1 Application Count

| Edit | File | Sites | Canonical type(s) |
|---|---|---|---|
| C-1 | conftest.py | 10 | AsanaHttpClient (8) + AuthProvider (2) |
| T-0 | lifecycle_smoke | 0 | imports |
| T-1 | lifecycle_smoke | 1 | AsanaClient |
| T-2..T-5 | lifecycle_smoke | 24 | 6 service Protocols × 4 blocks |
| T-6 | lifecycle_smoke | 12 | ResolutionContext |
| **TOTAL** | **2 files** | **47** | **9 canonical types** |

**Reconciliation against substrate** (~102 VERIFIED+AMBIGUOUS): plan binds **47**; defers ~39 per-method AsyncMock attaches (Pattern C — brittle without per-Protocol method-signature audit) and ~12 Cluster A/E/F/G/H Pydantic-blocked targets per §5. Phase 1 envelope is HIGH-LEVERAGE-LOW-RISK — every binding on a verified canonical class with no Pydantic-rebuild dependency.

## §4 Per-File Mutation Pattern

### §4.1 conftest.py (fixture-scope)
Class shells `MockHTTPClient`/`MockAuthProvider` DELETED; replaced with two factory functions. Fixtures call factories. Behavior preserved: `MagicMock(spec=AsanaHttpClient).get` auto-resolves to `AsyncMock` (Python ≥3.8 spec introspection on coroutine functions). Net delta ≈ -9 lines.

### §4.2 test_lifecycle_smoke.py (test-method-scope)
T-0 = single import block insert. T-1..T-6 = mechanical token replace. ZERO assertion / signature / test-name changes. Behavior preserved: subsequent `.method = AsyncMock(...)` attaches work because attached names are declared on the spec'd class. Net delta ≈ +9 lines.

### §4.3 Critical Ordering
- Apply C-1 (conftest) BEFORE T-0..T-6 (lifecycle_smoke) — conftest is session-fixture; collection must clean before integration suite collects.
- Within conftest: imports → class deletions → factory inserts → fixture-body updates.
- Within lifecycle_smoke: T-0 FIRST, then T-1..T-6 in any order (independent token replaces).

## §5 CANNOT-SPEC-PHASE1 Sites (deferred, ~33 sites)

| Cluster | Sites (~) | Reason | Phase 2 routing |
|---|---|---|---|
| A — `Process` (Pydantic BusinessEntity) | 10 (L66/L68/L72/L79 + reuses) | NameGid forward-ref resolved only via `_bootstrap_session` model_rebuild (conftest L169–L190); spec=Process at module-import time risks pre-rebuild collection failure | Phase 2: Pydantic-rebuild characterization-test |
| G — Business/Unit/Offer/contact | 13 (L460–L900 cluster) | Same Pydantic forward-ref concern | Phase 2 |
| H — Generic paginator stubs | 6 (L105/L480/L523/L581/L1384) | NO canonical Protocol declared in `src/autom8_asana/protocols/` | **Drift signal — route to /eunomia at sprint close per charter §11.1**; declare `PaginatorProtocol` as separate ticket |
| H — Section/Task model mocks | 4 | Pydantic forward-ref | Phase 2 |
| Cluster C per-method AsyncMock attaches | 15 of 39 | Per-method `spec=Class.method` requires per-Protocol signature audit | Phase 2 (iterative) |
| conftest L79 nested process_type | 1 | spec=Enum over-specifies tests that only use `.value` | Phase 2 |
| Cluster E (`InitActionConfig`) / F (`StageConfig`) | 6 (L825/L846/L871/L896/L1351/L1391) | Pydantic BaseModel; spec= can break test-side overrides; needs per-site verification | Phase 2 |

**Janitor MUST NOT touch any of the above in this commit.** Mis-classification suspicion → file sub-issue per §9; do NOT modify in-place.

## §6 Verification Protocol (run sequentially; ALL must pass before commit)

1. **Collection (lifecycle_smoke)**: `pytest tests/integration/test_lifecycle_smoke.py --collect-only -q | tail -3` → no collection errors; test count unchanged from baseline.
2. **Run (lifecycle_smoke)**: `pytest tests/integration/test_lifecycle_smoke.py -x --tb=short` → exit 0.
3. **Conftest collection self-check**: `pytest tests/conftest.py --collect-only -q` → no collection errors (collection-only validates import discipline; fixtures don't run standalone).
4. **SCAR collection count** (charter §11.4): `pytest -m scar --collect-only -q | tail -3` → ≥47.
5. **SCAR full run**: `pytest -m scar -x --tb=short` → exit 0.
6. **Optional pre-push**: `pytest tests/ -x --tb=short -q --ignore=tests/integration/live` → exit 0.

If any of 1–5 fails: REVERT, reclassify failing site as AMBIGUOUS-promoted-to-CANNOT-SPEC-PHASE1, append rationale to §5, re-dispatch.

## §7 Atomic Commit Shape

**RECOMMENDED: single commit** — `refactor(tests): adopt MagicMock(spec=) for root fixtures and lifecycle_smoke (HYG-002 Phase 1)`.

Rationale: 47 mechanical edits across 2 files; revertibility preserved within bounded scope; downstream consumers (~30+ tests via `_make_mock_client`, ~12 ResolutionContext sites) require T-0 import block AND T-1 helper edit to land together to avoid intermediate-state ImportError. Splitting introduces a noisy bisect window without behavior-correctness benefit.

**Alternative: 2 commits** (only if architect-downstream judgment prefers HYG-001 staging):
- A: conftest.py (C-1) — verify §6 step 3 standalone.
- B: lifecycle_smoke (T-0..T-6) — verify §6 steps 1/2/4/5 after A applied.

HYG-001 staging pattern does NOT apply (both files address the same smell-class SCAR-026); prefer single commit unless explicit instruction.

## §8 Risks (janitor pre-flight)

1. **Interface drift surfaces during execution** (HYG-002 AC 5 binding). Method on canonical class renamed/removed in src/. Janitor MUST NOT modify production code or extend spec= scope. Action: file sub-issue at `.sos/wip/hygiene/SUB-ISSUE-hyg-002-{topic}-2026-04-30.md`; mark site as CANNOT-SPEC-PHASE1; route to /eunomia at sprint close. **Do NOT absorb interface-drift fixes into this commit.**
2. **SCAR collection-count regression** (charter §11.4 BLOCKING). §6 step 4 must show ≥47. Regression likely indicates top-level import ImportError suppressing collection. Action: revert; investigate; do not commit.
3. **spec= cascade test-count amplification** (charter §11.8). `spec=` exposes previously-silent `mock.nonexistent_method` calls. Bounded to lifecycle_smoke.py for Phase 1. If >3 failures surface, REVERT and re-scope; if ≤3, fix in-line within lifecycle_smoke.py only (no production-code edits) and document in commit body.
4. **Async-method spec auto-resolution**. `MagicMock(spec=AsanaHttpClient).get` auto-resolves to `AsyncMock` because `AsanaHttpClient.get` is `async def` (verified). Existing `mock.method = AsyncMock(...)` overrides continue to work. No sync-override pattern exists in scope (verified plan-authoring).
5. **Pre-existing platform-runtime working-tree mods**. Charter context notes mods present. Janitor MUST stage ONLY the 2 in-scope files; do NOT `git add -A`.

## §9 Out-of-Scope Refusal (BINDING for janitor)

Janitor MUST NOT, in this commit:
- Modify any file in `src/autom8_asana/` (production code).
- Modify any test file other than `tests/conftest.py` and `tests/integration/test_lifecycle_smoke.py`.
- Touch any CANNOT-SPEC-PHASE1 site enumerated in §5.
- Add `spec=` bindings beyond the 47 enumerated in §3.6.
- Surface interface drift by changing production code — file as sub-issue per §8.1 instead.
- Modify configuration / CI / pre-commit / pyproject.
- Stage working-tree mods unrelated to the 2 in-scope test files.
- Skip any §6 verification step.

If janitor encounters a refusal trigger: STOP, file sub-issue artifact, await architect-enforcer or audit-lead adjudication. Do not improvise scope expansion.

---

**End of plan.** 47 spec= applications, 2 files, 1 recommended commit, 6-step verification, 5-risk surface, 8-rule scope-fence. Janitor executes mechanically; audit-lead verifies via §6 + commit-shape.
