---
type: design
artifact_type: refactor-plan
rite: hygiene
session_id: session-20260430-144514-3693fe01
target: HYG-002 Phase 2
charter: PYTHIA-INAUGURAL-CONSULT-2026-04-30-phase2
authored_by: architect-enforcer
substrate: SCOPE-hyg-002-phase2-2026-04-30.md
evidence_grade: STRONG
evidence_basis: code-smeller scope (Read 223L) + direct file inspection (Read tests/unit/lambda_handlers/test_workflow_handler.py L1-L1403) + grep re-probe at plan-authoring (2026-04-30, branch hygiene/sprint-phase2-2026-04-30 HEAD 550ec57a) confirming 87 bare-form sites distributed C1=22+2SPOF, C2=22+2SPOF, C3=29, C4=1, C5=4, C6=1
---

# PLAN — HYG-002 Phase 2: spec= Adoption (test_workflow_handler.py)

## §1 Plan Purpose

Mitigate SCAR-026 at `tests/unit/lambda_handlers/test_workflow_handler.py`
(1,403 lines, 87 bare-form sites) by binding `MagicMock()`/`AsyncMock()` to
canonical source-tree classes via `spec=` for 4 VERIFIED clusters; defer 29
Lambda-context + 1 Callable-default sites to /eunomia. Single atomic commit on
`hygiene/sprint-phase2-2026-04-30`.

## §2 Pre-Flight Drift-Audit

Anchors re-probed at plan-authoring (2026-04-30, HEAD `550ec57a`) against SCOPE
§3: `AsanaClient` (`src/autom8_asana/client.py:50`), `DataServiceClient`
(`src/autom8_asana/clients/data/client.py:72` — `__aenter__`/`__aexit__`
declared), `WorkflowAction` (`src/autom8_asana/automation/workflows/base.py:106`
— declares `validate_async`/`enumerate_async`/`execute_async`/`workflow_id`),
`EventPublisher` (`autom8y_events.publisher` — `publish`) — all VERIFIED.
`aws_lambda_powertools.utilities.typing.LambdaContext` — `ModuleNotFoundError`
at probe; **CANNOT-SPEC-PHASE2** (29 sites, C3).

**File re-probe** (`grep -nE "MagicMock\(\)|AsyncMock\(\)"` at HEAD `550ec57a`):
**87 sites** (61 `MagicMock()` + 26 `AsyncMock()`). Matches SCOPE §1. Drift: NONE.

**Process-global-state audit** (SCOPE §5): `WorkflowRegistry` singleton
(L488–L502; `reset_workflow_registry()` setup/teardown) and
`sys.modules["autom8y_events"]` mutation (L703–L732). Neither interacts with
spec= application order — symbols bound at module-import time before mutations. SAFE.

## §3 Ordered spec= Application List

### §3.1 Edit P-0 — Top-level import block (after L19)

```python
from autom8_asana.client import AsanaClient
from autom8_asana.clients.data.client import DataServiceClient
from autom8_asana.automation.workflows.base import WorkflowAction
from autom8y_events import EventPublisher
```

Sites bound: 0 (enables P-1..P-4).

### §3.2 Edit P-1 — C4 WorkflowAction helper (1 site)

**L64** (helper `_mock_workflow`, L59–L70): `wf = MagicMock()` →
`wf = MagicMock(spec=WorkflowAction)`. Subsequent attaches L65/L67/L69 +
property writes L527/L556 UNCHANGED (names declared on `WorkflowAction`;
`spec_set=False` default permits writes). Propagates to ~26 `_mock_workflow()`
consumers. Sites bound: **1**.

### §3.3 Edit P-2 — C2 DataServiceClient (24 sites)

`mock_ds = AsyncMock()` → `mock_ds = AsyncMock(spec=DataServiceClient)` at:
**L98, L130, L163, L194, L251, L282, L330, L367, L402, L432, L462, L521, L550,
L591, L636, L671, L708, L747, L800, L834, L869, L915, L953, L986, L1101, L1371**
(22 base + 2 SPOF helper). Composite `__aenter__`/`__aexit__` chain attaches
UNCHANGED — spec= permits write-on-declared-attribute. Sites bound: **24**.

### §3.4 Edit P-3 — C1 AsanaClient (24 sites)

LHS (22): `mock_asana = MagicMock()` → `mock_asana = MagicMock(spec=AsanaClient)`
at: **L95, L127, L160, L191, L248, L279, L327, L364, L399, L429, L459, L518,
L547, L588, L633, L668, L705, L744, L797, L831, L866, L912, L950, L983**.
RHS (2 SPOF, L1100/L1370): `mock_asana_class.return_value = MagicMock()` →
`mock_asana_class.return_value = MagicMock(spec=AsanaClient)`.
Sites bound: **24**.

### §3.5 Edit P-4 — C5 EventPublisher (4 sites)

`mock_publisher = MagicMock()` → `mock_publisher = MagicMock(spec=EventPublisher)`
at: **L601, L646, L681, L757**. Existing `mock_publisher.publish.{assert_called_once,call_args,side_effect}`
refs continue to work (`publish` declared on `EventPublisher`). P-0 import
binds symbol BEFORE L703 `sys.modules` mutation; symbol remains valid (SCOPE
§5: race NONE). Sites bound: **4**.

### §3.6 Total Phase 2 Application Count

| Edit | Sites | Canonical type |
|---|---|---|
| P-0 | 0 (4 imports) | enables P-1..P-4 |
| P-1 | 1 | WorkflowAction |
| P-2 | 24 | DataServiceClient |
| P-3 | 24 | AsanaClient |
| P-4 | 4 | EventPublisher |
| **TOTAL** | **53 spec= + 4 imports = 57 edits** | **4 types** |

**Reconciliation vs SCOPE §1 "49 VERIFIED"**: SCOPE 49 = 22 C1 + 22 C2 + 1 C4 +
4 C5 (excludes 4 SPOF sites L1100/L1101/L1370/L1371 enumerated under SCOPE §2
but not rolled into the 49-count). Plan INCLUDES the 4 SPOF sites (same
canonical type; mechanically identical). **Total = 49 + 4 SPOF = 53 spec= edits**;
8 composite chain attaches auto-propagate via spec=DataServiceClient (zero
additional edits).

## §4 Per-Cluster Mutation Pattern

- **C1 (24)**: 22 LHS + 2 RHS form; inside `with patch(...)` / `@patch(...)`
  scopes — spec= reads original class; patch replaces class object; mock
  substitutes for return_value. No interaction.
- **C2 (24)**: `AsyncMock(spec=DataServiceClient)`; composite chain UNCHANGED.
- **C4 (1)**: helper-level binding propagates to ~26 consumers.
- **C5 (4)**: top-level import bound BEFORE L703 `sys.modules` mutation.

**Critical ordering**: P-0 FIRST (imports must precede P-1..P-4). P-1..P-4 are
independent token-replacements; any order safe. ALL edits in SINGLE atomic
commit (§7).

## §5 CANNOT-SPEC-PHASE2 Sites (deferred, 30 sites)

| Cluster | Sites | Count | Rationale | Routing |
|---|---|---|---|---|
| C3 — Lambda context | L108, L143, L173, L211, L231, L261, L292, L311, L340, L378, L412, L442, L472, L532, L562, L566, L605, L651, L685, L722, L761, L810, L847, L882, L928, L966, L1000, L1106, L1376 | 29 | `aws_lambda_powertools.utilities.typing.LambdaContext` NOT importable (`ModuleNotFoundError`; SCOPE §2.3); handler never accesses `context.*` (zero refs in `workflow_handler.py`); single uniform drift signal — NOT 29 distinct gaps. | **Drift signal — surface to /eunomia at sprint close per charter §11.1**; recommended `_lambda_context()` factory in `tests/conftest.py` backed by `types.SimpleNamespace`. |
| C6 — Callable default | L49 | 1 | `Callable[..., WorkflowAction]` per `workflow_handler.py:76`; `MagicMock(spec=callable)` shape-thin no-op (SCOPE §2.6 AMBIGUOUS-DEFER). | **DEFER-NO-ACTION**. |

Total CANNOT-SPEC-PHASE2: 30/87 = 34%. Charter §4.3 NO-LEVER threshold (>50%)
NOT triggered. Janitor MUST NOT touch any C3 or C6 site.

## §6 Verification Protocol

Sequential; ALL must pass before commit. Per charter §11.2 + SCOPE §5 +
`.know/test-coverage.md:111`: this file shares process-global state; requires
`--dist=loadfile` mode under xdist. File-scoped uses single-process `-p no:xdist`.

1. **Pre-commit file-scoped run**: `pytest tests/unit/lambda_handlers/test_workflow_handler.py -p no:xdist --tb=short` → exit 0; baseline test-count unchanged.
2. **SCAR collection-count check** (charter §11.2 BLOCKING; OPERATIONAL ≥47): `pytest -m scar --collect-only -q | tail -3` → ≥47.
3. **SCAR full run**: `pytest -m scar --tb=short` → exit 0.
4. **Full unit suite (loadfile REQUIRED)**: `pytest -n 4 --dist=loadfile tests/unit/ --tb=short -q` → exit 0.

If any step fails: REVERT, reclassify failing site as
AMBIGUOUS-promoted-to-CANNOT-SPEC-PHASE2, append rationale to §5, file
sub-issue per §9, re-dispatch.

## §7 Atomic Commit Shape

**RECOMMENDED: single commit** —
`refactor(tests): adopt MagicMock(spec=) for test_workflow_handler.py (HYG-002 Phase 2)`.

Rationale (SCOPE §7 + Phase 1 PLAN §7 precedent): 53 spec= + 4 imports = 57
edits in a single test file; revertibility preserved within bounded scope;
splitting introduces noisy bisect window with intermediate ImportError risk and
zero correctness benefit. **Complexity**: LOW (SCOPE §7).

## §8 Risks (janitor pre-flight)

1. **Interface drift** (HYG-002 AC 5; charter §9.3 BINDING). spec= cascade
   exposes silent-passes as `AttributeError` if canonical method declarations
   drifted. Janitor MUST NOT modify production code. File sub-issue at
   `.sos/wip/hygiene/SUB-ISSUE-hyg-002-phase2-{topic}-2026-04-30.md`; mark site
   CANNOT-SPEC-PHASE2; route /eunomia. **Do NOT absorb drift fixes here.**
2. **SCAR collection-count regression** (charter §11.2 BLOCKING). §6 step 2
   must show ≥47. Regression likely indicates top-level import ImportError
   suppressing collection. REVERT immediately.
3. **Process-global state preservation** (SCOPE §5; LOW). spec= order does NOT
   interact with `WorkflowRegistry` singleton (L488–L502) or
   `sys.modules["autom8y_events"]` mutation (L703–L732) — verified §2. Use
   `-p no:xdist` for file-scoped pre-commit; `--dist=loadfile` (NOT `--dist=load`)
   for full-suite.
4. **spec= cascade test-count amplification** (charter §11.1 MEDIUM). spec=
   exposes previously-silent `mock.nonexistent_method` calls. Bounded to this
   file. If >3 failures: REVERT and re-scope. If ≤3: fix in-line in-scope only
   (no production-code edits); document in commit body.
5. **Pre-existing platform working-tree mods** in `.sos/`/`.knossos/`. Janitor
   MUST stage ONLY the in-scope test file; do NOT `git add -A`.

## §9 Out-of-Scope Refusal (BINDING for janitor)

Janitor MUST NOT, in this commit:
- Modify any file in `src/autom8_asana/` (production code).
- Modify any test file other than `tests/unit/lambda_handlers/test_workflow_handler.py`.
- Modify `tests/conftest.py` / fixture files (introducing the deferred
  `_lambda_context()` factory is OUT-OF-SCOPE — surface drift signal to
  /eunomia per §5).
- Touch any C3 (Lambda context, 29 sites) or C6 (Callable default L49) site.
- Add `spec=` bindings beyond the 53 enumerated in §3.6.
- Change production code to absorb interface drift — file sub-issue per §8.1.
- Modify configuration / CI / pre-commit / pyproject.
- Stage working-tree mods unrelated to the in-scope test file (no `git add -A`).
- Skip any §6 verification step.

If janitor encounters a refusal trigger: STOP, file sub-issue, await
architect-enforcer or audit-lead adjudication. Do not improvise scope expansion.

---

**End of plan.** 53 spec= + 4 imports = 57 edits / 1 file / 1 atomic commit /
4-step verification / 5-risk surface / 9-rule scope-fence. Audit-lead verifies
via §6 + `AUDIT-VERDICT-hyg-002-phase2-2026-04-30.md`.
