# spectral-fleet.yaml
# Fleet OpenAPI governance rules for autom8y services.
#
# Minimal ruleset: extends standard OAS validation only.
# Custom fleet governance rules (R-01..R-04) temporarily removed because
# JSONPath filter expressions (@property.match) are unsupported by the
# Spectral version on CI runners. Re-add once CI Spectral is upgraded.

extends: ["spectral:oas"]
