# spectral-fleet.yaml
#
# Fleet OpenAPI governance rules live in autom8y-api-schemas/spectral-fleet.yaml.
# This file is NOT consumed by CI. The satellite-ci-reusable.yml Spectral job
# fetches the canonical ruleset from autom8y-api-schemas via sparse checkout.
#
# This pointer file is retained for developer reference only.
# Run locally: spectral lint docs/api-reference/openapi.json \
#   --ruleset ../autom8y-api-schemas/spectral-fleet.yaml

extends: ["spectral:oas"]
