---
type: decision
status: accepted
artifact_id: GATE-coc-pt03-2026-08-13
wave: chain-of-custody-closure
checkpoint: PT-03 (HARD) — Phase-1 fan-in / NCSR roll-call
rendered_by: potnia (10x-dev, Read-only; main thread persisted)
date: 2026-08-13
verdict: PASS-WITH-CARRIES
scope_fence: rules NO operator fork (F-1, F-4, F-7, F-8, RE-2 severity, GATE-FORK all operator-reserved, untouched)
landing: AUTHORED-UNMERGED (Q-4 HALT)
---

# GATE — chain-of-custody-closure PT-03 (Phase-1 fan-in)

## §1 NCSR ROLL-CALL — five negatives, five rite-disjoint second-reads, ZERO FALLS

| # | Negative | Author (rite) | Reader (rite) | Verdict |
|---|---|---|---|---|
| **NR-1** | a count-preserving swap passes as OBSERVABLE at origin/main; the guard is genuinely absent | principal-engineer (10x-dev) | structure-evaluator (**arch**) | **STANDS** — 2 named narrowings (N1 clause-3 frozen wire-token deferral ACCEPTABLE; N2 clause-4a residual NARROWS: unattested for the live hashless emitter) |
| **NR-2** | no `asana:*` write scope at minting layer / no delegation claim / **CR-1 is the ONLY control** | security-reviewer (**security**) | dependency-analyst (**arch**) | **STANDS** — N-1 NARROWS (namespace partial-and-unmapped, not greenfield); "only control" narrows to app/gateway layers of the two repos read own-hands |
| **NR-3** | agent seat can't get a fleet JWT / ALB is internal-only | penetration-tester (**security**) | platform-engineer (**sre**) | **STANDS** (both genuinely FALSE own-hands) — consequent **severity framing NARROWS**: F-001 Critical→High, business-scoped absent an exempt-SA grant |
| **NR-4** | warmer max-ever 7,460 < offer start 10,617, so offer is NEVER reached | platform-engineer (**sre**) | remediation-planner (**arch**) | **STANDS** — all four sub-legs; author's own null (order-seed) DISCHARGED beyond claim (deterministic numeric-priority sort, 16 distinct values) |
| **NR-5** | the `\|\| true` bypass lives in a repo I cannot read | pipeline-cartographer (**eunomia**) | threat-modeler (**security**) | **STANDS** (pin re-fetched byte-identical); UV-P-CoC-4 corollary SURVIVES; **fix-locus (c) NARROWS** (AR-1) |

**Rite-disjointness: 5/5.** `qa-adversary` critiqued nothing; no barred seat appears. arch supplied 3/5 readers via 3 distinct seats (note reader-diversity for Phase 2). No critic graded its author's design STRONG — the wave's single STRONG (NR-1) is correctly leg-scoped to own-hands re-derivation.

## §2 Rung confirmation — every rung honest, none over-claimed

- **CC-1** = `rung-BUILT-DARK` (mechanism closure, two-sided teeth own-hands; NOT a live-emitter closure). N1 frozen wire-token over-claim = conservative fail-side label (both HUMAN and UNKNOWN yield `not_observable`, never passes a bad payload), documented + pinned. N2 clause-4a residual UNATTESTED for the live emitter, pinned, rides on the un-reprobed census (CF-2).
- **CC-2** = `rung-DESIGN`; `rung-ENFORCED` refused for a STRUCTURAL reason (two-sided harness needs two minted tokens (CR-5) + a live write route (CR-1)). Each layer named-owner'd; no CODEOWNERS in autom8y-asana (verified NULL).
- **CC-3** = UV-P-dispositioned (UV-P-C-1/C-2 both CLOSED-WITH-RECEIPT, adverse); infra legs STRONG (rite-disjointly reproduced), judgement MODERATE.
- **CC-4** = `rung-DESIGN` slate, binds NOTHING to F-1 (no ranked preference, no steering); O-E routed-out genuinely first-class; tiers priced separately, arithmetic reproduced from fresh live logs.
- **CC-6** = UV-P-dispositioned + fix-locus finding; (c) NARROWED per AR-1; both seats concur NO F-8 halt (reported, not ruled).

**Hygiene flag (§2.1):** `PROBE-re2-blast-radius` carries `status: complete` with NO `rung:` field — the exact rung-less-"done" token-shape the wave refuses. Cured in-body (state/cap/disposition make the honest rung unambiguous). FLAG for CC-8 attest hygiene; must not recur.

## §3 The two MATERIAL corrections (downstream inputs, not footnotes)

- **MC-1 → CC-7 / E1 / F-7.** RECON's "fixing (a) or (c) alone is sufficient" is TRUE for (a), **FALSE for (c)** — and (c) is RECON's recommended locus. Locus (c) is a **two-action** remediation (local enforcing job **+** branch-protection contexts registration); shipped without the second action it reproduces the silent-gate class the wave exists to close. CC-7 must ingest `CRITIQUE-cc6-gitleaks-recon:99-149` alongside `RECON:192`.
- **MC-2 → RE-2 severity.** F-001 is **High, business-scoped** absent an exempt-SA grant, with a named re-escalation trigger (CF-1). The CWE-862 defect itself STANDS unqualified own-hands; only the severity + end-to-end framing narrow.

## §4 Carry-forward ledger — LOAD-BEARING (move a fork or a rung)

- **CF-1** — the **exempt-SA determinant** (are `ace`/`iris` `bypass_scope_enforcement=True`?): the RE-2 High↔Critical fork. Cross-repo, CR-5-bound, unprobed. The single operator-actionable question that resolves severity.
- **CF-2** — CC-1's hashless-live-emitter census NOT re-probed; grounds N2's scoping + the properties-not-required judgment. Rides as UV-P, not fact. Becomes live the moment EX-5 ships `report_generated` while REC-002 stays undone.
- **CF-3** — CC-4's null: offer-GIDs-shared-with-a-warmed-entity, unresolved by both parties.
- **CF-4** — CC-6's CR-5 regex residual: `asana-native-pat` validated against synthetic shapes only; if the real token deviates the rule silently doesn't fire. Cannot close without reading the credential.
- **CF-5** — CC-6's baseline-scope extension: the baseline must cover EVERY historical finding that trips (fingerprint-keyed), not only cred-t21's 3 commits. F-7 is "rotate + baseline-then-enforce"; the **baseline** unblocks CI, not the rotation.
- **CF-6** — CC-2 UV-P-3 half-discharged: `agent_access` token silently reclassified as `ServiceClaims` (structural half CONFIRMED own-hands); remaining half = audience + mintability. Widens blast radius beyond 18 SAs.
- **CF-7** — CC-3 R-2 NEW scope: `/api/v1/tasks/*` + `/api/v1/projects/*` are JWT-EXCLUDED, publicly enumerated (incl. `DELETE`), posture UNKNOWN — mutation surface WIDER than the five write classes.
- **CF-8** — AL-5 window-open clock ~`2026-08-15T12:45Z` (~2 days from this gate); on its own clock — the merge fence lifting does not relax it. Any warming option under F-1 must land before it or pay O-7a segmentation.

BOUNDED carries CF-9..CF-22 (named, non-load-bearing, not dropped): admin.py:456 maybe-never-green (CF-9); wheel byte-identity (CF-10); allowlist fail-closed acceptance criterion (CF-11); Asana ACL granularity (CF-12); OAuth-client second SA population (CF-13); ASANA_PAT value-identity (CF-14); Lambda exec-role IAM (CF-15); no-compromise-sought (UNKNOWN not clean bill) (CF-16); **CF-17 NEW live finding**: prod Lambda emits CloudWatch metrics dimensioned `environment=staging` while `AUTOM8Y_ENV=production` (real observability-labeling bug, out of wave scope, hygiene/sre referral); warm_priority tie-fragility (CF-18); option-(a) blast radius across pinning repos (CF-19); code-scanning-alert-for-cred-t21 not queried CR-5 (CF-20); §3(b) listing not re-derived (CF-21); REC-002 cross-repo coordination point (CF-22).

## §5 VERDICT — PASS-WITH-CARRIES

No sprint re-opens. Binding carries into Phase 2 / CC-8:
- CC-8 attests at the HELD rungs and no higher (BUILT-DARK / DESIGN / UV-P-dispositioned); all Phase-1 artifacts AUTHORED-UNMERGED.
- eunomia's limb-(a) attestation at CC-8 is BLOCKED until both WS-A halves land (sequencing input).
- CC-7 must consume the CORRECTED CC-6 slate (MC-1).
- FR-3 anchor repairs (`story_warmer.py:159`→`:157`; `stories.py:34`/`:30`) before CC-4's slate reaches the F-1 fork.

No operator fork ruled here. PT-02 (F-1), PT-04 (F-7/F-8), the RE-2 severity determinant (CF-1), and the fix-locus choice pass to the operator priced, corrected, and unruled. Phase 2 (CC-7, gated on CC-6 via E1 + F-7/F-8 disposition) and CC-8 (attest at held rung) are reachable.
