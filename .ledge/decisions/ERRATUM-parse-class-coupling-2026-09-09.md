# ERRATUM — the parse-class 30-vs-10 gap was coupled to the AFTER projection. It does not belong to it.

**Class:** ERRATUM. **It amends NOTHING.** `RATIFICATION-client-outcome-decision-space-2026-09-08.md`
§4.2 is a **ratified operator record** and its text stands **exactly as written**. This erratum
records what a reader of it would wrongly infer, and nothing more.
**Raised by:** the `calendar-integration-locus` lane. **Verified own-hands here** from
`autom8y` PR #2071 RECEIPT LEG 1 via API — the fenced `autom8y` worktree was not touched.

## The ratified text, verbatim and UNCHANGED
> *"2. **That S-14's cure does what it projects.** The AFTER figure (41.6% → 21.2%) is a
> **PROJECTION, never a measurement** — nothing is deployed. One denominator is **unreconciled:
> parse-class 30 vs 10.**"*

**Both sentences are TRUE.** The defect is the **juxtaposition**: they read as two reasons the
assumption is unconfirmed, which implies the projection depends on the disputed number. **It does
not.**

## `[OWN-HANDS]` — the receipt's own arithmetic
| receipt line | verbatim |
|---|---|
| `:98` | denominator `pipeline_completed` = **255** |
| `:102` | `failed` = **106** = **41.6 %** → 502 |
| `:107` | the 106 decompose **exactly**: `52 + 30 + 19 + 5 = 106` — *"the accounting closes with no residue"* |
| `:112` | parse `Missing body field` = **30** — *"different stage — **out of this file's scope**"* |
| `:116` | *"the projected delta is **exactly `-52`, entirely from `:725`**"* |

`106 − 52 = 54`; `54 / 255 = 21.2 %`; `106 / 255 = 41.6 %`. **Checked against 255 in both
directions. The parse class contributes ZERO.**

## And they are not two measurements disagreeing
`:118`, verbatim: *"dispatch said 53 / 10 / 24; I measure 52 / 30 / 24 … I report my own measured
number and have not reconciled it."*
**30 is S-14's own-hands measurement with its window and class stated. 10 is the DISPATCH's figure,
whose basis is unstated.** The dispatch is not simply wrong — its other two corroborated (24 exact;
53 vs 52 by a minutes-wide window offset) — so **something differs about the class definition.**
**That remains genuinely open and belongs to whoever authored the dispatch figure. `NO WATCHER`.**

## What changed, and what did NOT
- **`shape.md` hard_gates — RE-AIMED.** The gate *"the parse-class 30-vs-10 denominator is
  RECONCILED before any AFTER figure is quoted"* fenced the AFTER figure behind a number that
  **cannot move it.** ★ **The correct gate was already on the line directly above it** — *"the AFTER
  figure is MEASURED post-apply, or it stays a PROJECTION and is labelled one."* The gate was not
  wrong to exist; **it was aimed at the wrong precondition while the right one sat adjacent.**
- **`frames/name-the-client.md` `:521` and `:770` — DECOUPLED.**
- **The RATIFICATION — UNTOUCHED.** Amending an operator's assumption register is the operator's
  act. The peer lane declined for that reason and so does this seat.

## ★ The weakness that is UNDIMINISHED
**The AFTER figure is a PROJECTION and nothing is deployed.** That was always the load-bearing
caveat and this erratum does not soften it by one degree. **Under R-73/R-74 the deploy that would
produce a real AFTER window is FORBIDDEN**, so the figure cannot become a measurement until the
freeze releases. **Decoupling the parse class makes the remaining gate sharper, not weaker.**

> **The two findings, separated for good:**
> **(1)** the projection is unmeasured — **load-bearing, unchanged, blocks any AFTER citation**;
> **(2)** the parse-class gap — **tracked separately, binds ONLY citations of the parse class.**
