# RATIFICATION — decision-space sitting XI · 2026-09-14 · the SHAPE sitting (S0b)

**Seat:** calendar-integration-locus · **Operator:** Tom Tenuta · **Method:** `/interview` under the standing constraints; the S0a packet (`.sos/wip/SHAPE-PACKET-read-the-name-wave1-2026-09-14.md`) as the question set; two pre-sitting measurements taken own-hands (the 09-04..09-11 replay under each candidate predicate over three windows, and the founding office's event breakdown). **Fork numbering = the spine's** (F4 = consumer word, F5 = population); F10–F12 are the packet's new forks. **Continues R-159 → R-160..R-172.** **Rite:** sre, stations borrowed per the charge.

## §0 THE THREE SENTENCES

1. **The ruled floor would never have paged the office it was chartered on.** Measured own-hands over the founding window: office `ccb52f4c` has 580 office-bearing lines, 329 distinct messages, 336 terminal declines, and **4 completed bookings** (4 / 4 / 1 across the three windows) — never zero — while a "bookings = 0" floor fires 15 other offices in eight days, among them the unnamed residual bucket, a genuinely disabled office and a malformed-guid office. The consult's "406 mails / 2 bookings" is not reproducible from the log plane under any predicate measured. **R-160: the floor has two page classes**, a zero floor and a collapsed-rate floor.
2. **The operator delegated every implementation fork to the seat with the architect** ("you rule this kind of implementation decision for me"). The architect's ADR rules them (`.ledge/decisions/ADR-read-the-name-s1-implementation-2026-09-14.md`, D1–D8), with rejected alternatives named; the sitting records the operator's four value rulings in plain terms.
3. **S-1 is built, proven two-sided, deployed, soaked — and NOT ARMED this wave.** The operator gave no consumer commitment (R-168: "nobody yet"). By the charge's own §6, S1.7 is REFUSED-CORRECTLY on the S1.2 word, the page path stays on a scratch topic, and R1 is not realized this wave; the wave's honest ceiling is "instrument proven, reader withheld, blocker named with its instant."

## §1 RULINGS

| # | fork | ruled by | options offered | ruling | REC held? |
|---|---|---|---|---|---|
| **R-160** | F10 floor shape | operator | rate floor re-ruling R-150 · keep the zero floor · **both floors, two page classes** · wrong question | **Both floors in one evaluator, two page classes:** ZERO floor (arrivals ≥ A, bookings = 0) and RATE floor (arrivals ≥ A′, bookings ≥ 1, bookings/arrivals < r). Each its own probe at S1.4 and its own runbook class. | **displaced** (REC was rate-only) |
| **R-161** | F10 arrival unit + thresholds | **delegated → seat + architect (ADR D1)** | one terminal-outcome line · distinct message id · any office-bearing line · wrong question | **U-3:** arrivals = distinct `message_id` over id-bearing lines **plus** terminal-outcome lines lacking an id (terminal set: `terminal_decline · ad_lead_gate_refused · booking_gate_declined · booking_completed · contente_booking_booked · booking_intake_fault`); bookings = `booking_completed ∪ contente_booking_booked`. Business meaning: **one inbound email attributed to that office.** Zero floor A = 5; rate floor A′ = 20, r = 0.025 (founding office measures 0.69 / 1.16 / 0.89 %; nearest quiet office 3.70 %; r = 0.02 rejected for 4 % headroom on the de-dup factor). S1.1 publishes 3 windows × 2 units × 12 settings; exit gates K1–K5; **on K1/K2 failure the seat recedes to the operator, it does not re-tune.** | delegated |
| **R-162** | F3 + F11 deadman + degraded-run posture | **delegated (ADR D2)** | prober + success-gap + fail-closed · prober only fail-open · success-gap only + refusal · wrong question | **C4 + L4:** module v1.4.1 prober (`declared_cadence_seconds=3600`, `buffer_multiplier=2`, `check_schedule_expression="rate(5 minutes)"`) **plus** the module's opt-in success-gap alarm keyed on `LastSuccessTimestamp` (ns `Autom8y/EbiOfficeFloor`, `success_max_gap_seconds=7200`), emitted **only** when the in-run control passes (`status==Complete ∧ records_scanned ≥ 500 ∧ offices_with_bookings ≥ 5`); otherwise page class **`FLOOR-REFUSED`** and the timestamp is withheld. Pythia's stated mechanism was inverted (a MISSING gauge does NOT page; the watcher-of-the-watcher is the separate `prober_self_deadman`) — the S1.7 proof is written against the real mechanism. IAM short name `autom8-ebi-booking-floor` (64-char trap). Success-gap alarm created at S1.7 (the module shares one `alarm_actions` list). | delegated |
| **R-163** | F2 cadence | **delegated (ADR D3)** | hourly, page at 11:00Z · daily + period override · daily default · wrong question | **Hourly evaluate; page gate `hour == 11` UTC.** Blind window ≈ 4.1 h (vs ≈ 50 h and ≈ 96 h — the daily default exceeds the instrument's own 72 h window). The §2(ii) control counts four columns never summed: evaluations/day, controlled evaluations/day, pages/day, page-class distribution. | delegated |
| **R-164** | F1 evaluator home | **delegated (ADR D4)** | dedicated Lambda in EBI · separate service · (A3/A4/A0 rejected) | **A1: a dedicated Lambda in `services/email-booking-intake/`** via `service-lambda-scheduled v1.0.2`. New IAM = **four** grants (`logs:StartQuery`, `logs:GetQueryResults`, `sns:Publish` topic-scoped, `cloudwatch:PutMetricData` namespace-conditioned), correcting pythia's two. The merge is the apply (R-136): pending set enumerated at the merge instant, deploy singleton. | delegated |
| **R-165** | F5 class-annotation mechanism | **delegated (ADR D5)** | dated snapshot · live Asana read · SSM census | **E2: dated snapshot of the 2026-09-11 sizing read**, date printed on every page, `class=unknown` fallback, `STALE` marker past 30 d. **`***` is not an office:** excluded from evaluation, printed as an attribution residual with a > 10 % tripwire. Dark/disabled offices are labelled and sectioned, never filtered. Snapshot refresh = a defer with the page as its self-surfacing watcher. | delegated |
| **R-166** | F7 scan scope | **delegated (ADR D6)** | none · field index · window · prefix · dedicated group | **G1, no narrowing.** Insights bills scanned, not matched (20,276 rec / 5.41 MB per 3-day scan, identical with a filter). Baseline 82.4 MB @ 90 d; > 10 GB/90 d triggers G2. | delegated |
| **R-167** | F12 repeat policy | operator | every day with a day count · once per incident · plain daily · wrong question | **A page every day with a day count** (M3, state-free from a widened lookback; ≈ 35 MB/run, no prior-state store). | held |
| **R-168** | F4 consumer word | operator | I do within one business day · someone else · arm and measure · **nobody yet** | **"Nobody yet: build, prove, do not arm."** S-1 is built, deployed, soaked and probed two-sided with every page path (digest, prober, success-gap) pointed at a **scratch topic**; S1.7 is **REFUSED-CORRECTLY** on the S1.2 word; R1 is **not realized this wave**. The blocker is the commitment, recorded here with its instant (2026-09-14, S0b). Repointing to `platform_alerts` is one terraform change on the word. | no REC (correctly) |
| **R-169** | F6 smoke leads | operator | reserved test office · **real offices, no marker** · no such practice · wrong question | **Test leads go through real offices with no marker today.** The replay runs with no exclusion plus a sensitivity check; live evaluation counts them as arrivals; the convention is named by the operator and codified (F5-1) **before** any arming. UV-P-1 stays open with the operator as owner. | **displaced** (REC was a reserved office) |
| **R-170** | F9 WS-2 sequencing | operator | recon this wave, build next · build now on a second seat · next wave · wrong question | **Read-only recon this wave (T5, architect), build charged next wave with a dated consumer commitment.** The write-authz routing (K-slate) is the recon's first question; the "executor bypasses write-authz" claim is INHERITED and must be re-derived. | held |
| **R-171** | F8 WS-0 disposition (bookkeeping) | seat | verify + route · rebuild in EBI · rebuild in monolith IaC | **H1: verify + route + the taken teeth receipt.** The Calendly halt tripwire (`autom8-calendly-calibration-halt-prod` + divergence twin → `platform_alerts`) has stood since 2026-07-30; the frame's WS-0 sentence and R-148's "the leg has none" are corrected on the record. The handoff says **"verified-as-armed; teeth on synthetic strings only"** while the post-#121 emitted string (V-33) is NOT TAKEN. Orphaned IaC (`ManagedBy=terraform`, declared by no `*.tf`) and the structured-line false-match hazard are defers **with owners** (monolith owner), never NO WATCHER. | seat |
| **R-172** | D8 page shape | **delegated (ADR D8)** | — | **At most one SNS publish per UTC day**, a digest sectioned class-first (`ACTIONABLE · EXPECTED SILENCE · CLASS UNKNOWN · ATTRIBUTION RESIDUAL`, empty sections print `(none)`); per-run log lines `office_floor_evaluated` + `office_floor_office`, **flattened never nested**; **one pinned query constant** shared by the S1.1 replay and the live run, printed verbatim once in the handoff. | delegated |

## §2 SVR AMENDED AT THIS SITTING

- **SVR-B′ (2026-09-14, own-hands at autom8y `a1f3ecf3`, #2200):** the EBI **image pin is retired** from `production.tfvars` (comment history only); `service-deploy-lambda.yml` resolves the alias-served tag from the registry; `service-deploy-dispatch.yml` now excludes `services/**/{.ledge,.know,.sos,tests}/**`. Alias `live` = **v69**, image `0850228` (#2199). **Consequence:** the image-rollback hazard of a terraform-only apply is **dissolved on EBI** (the other seat's "No changes" plan receipt is INHERITED); the asana stack keeps its pin and its hard stop. This wave still uses no manual apply.
- **The founding count is unrecoverable.** "406 mails / 2 bookings" cannot be reproduced under any measured predicate; A=5's calibration basis is gone and the thresholds are re-derived from measured quantities (ADR D1).
- **`@requestId` is empty on EBI's structured lines; no uniform correlation field exists** (`message_id` on the decline path, absent on booking/refusal paths; `lead_id` on 3 of 580 lines for the founding office). Hence U-3.

## §3 EXPLICITLY DEFERRED (owner named; none NO WATCHER by the sitting's rule)

| item | waits on | owner / watcher |
|---|---|---|
| the consumer commitment (S1.2 word) → S1.7 arm | the operator, on the record with an instant | operator |
| the smoke-lead convention (F5-1 codification; F5-4 reserved office as the durable cure) | the operator naming it | operator → S1.7 gate |
| WS-2 build charge with a dated consumer commitment | next wave; T5 recon this wave | architect (T5) → operator (charge) |
| the 3-day de-dup factor for the founding office in window B (must be < 2.15 for K1 at r=0.025; 8-day factor 1.65) | S1.1 | observability-engineer, S1.1 |
| the class-snapshot refresh | 30-day STALE marker on the page | the page itself surfaces it; refresh = a read-only re-run of the sizing read |
| orphaned Calendly tripwire IaC · the structured-line false-match (V-33) | the monolith owner | monolith owner (routed, not hand-edited) |
| the asana stack's image pin + apply path (still the rollback shape) | the R-143 charges | to be seated |
| the 12:20Z/13:00Z resolve-step timeouts on the Calendly-intake path (fact, cause unclaimed) | one look while writing the Calendly closure (W0) | observability-engineer, W0 |

## §4 ASSUMPTIONS THAT REMAIN UNCONFIRMED

1. That `terminal_decline_parked` never co-occurs with `terminal_decline` for one mail — the architect reports it DISCHARGED by direct read; S1.1 re-proves the inflation factor on the replay.
2. That a prober-published gauge has datapoints from apply-time (`prober_seed`) — architect: discharged by read; S1.6's daily sums are the receipt.
3. The Insights $/GB rate — UV-P; the conclusion (negligible) survives a 100× error.
4. Whether a human ever will read `platform_alerts` — withheld by R-168; not this wave's to resolve.
5. The other seat's "No changes" plan under the retired pin (#2200) — INHERITED; this wave does not use that path.

## §5 CALIBRATION

Eight decided items: **two held** (R-167, R-170), **two displaced** (R-160, R-169), **three delegated** to the seat with the architect by the operator's word (R-161..R-166 as a block, R-172), one correctly without a recommendation (R-168). Sitting X was 7/4; sitting IX 8/4. The operator's feedback at this sitting — implementation forks are the seat's to rule, only value questions come to him — is banked and governs the rest of the wave.

## §6 EXECUTION ORDER (this wave, from here)

Phase B: **W0 ‖ S1.1** (both observability-engineer; critics change-warden / qa-adversary) → Phase C: **S1.2 ‖ S1.3** (S1.2 = runbook + the word recorded as WITHHELD; S1.3 builds from ADR D1–D8) → S1.4 → S1.5 (pending set enumerated at the instant; alias `live` recorded `--qualifier`; deploy singleton) → Phase E: **S1.6 ‖ T1 ‖ T2 ‖ T3 ‖ T5** → S1.7 **REFUSED-CORRECTLY** (page paths on the scratch topic; deadman proven both ways there) → S1.8 attests (i)(ii)(iii) on the mechanism and states R1 NOT REALIZED pending the word → T4 `/handoff`.

**Standing fences, unchanged** (charge §7), plus: no manual apply on the asana stack (still pinned); every EBI merge re-verifies the pending set and the alias at the instant; every page path stays on the scratch topic until the operator's word.
