# RATIFICATION — decision-space sitting X · 2026-09-11 (late evening)

**Seat:** calendar-integration-locus · **Operator:** Tom Tenuta · **Method:** `/interview` under the standing constraints (neutral framing · phased and adaptive · assumptions surfaced · litigate not confirm · recommendations labelled REC, never default · wrong-question door on every item) · **Convened on:** the wave 1 closure (`ATTEST-name-the-client-wave1-closure-2026-09-11`, #442), the READ telos draft (`.know/telos/read-the-name.md`, #440), the S-1 charge, and the Calendly-intake measurement taken tonight after the email-intake conductor's note. **Numbering continues from R-147 into the READ initiative: R-148..R-158.** Supersedes handoff #443 §3 rows `R-147-WORDS` and `READ-R2-SUBSTRATE`.

## §0 THE THREE SENTENCES

1. **READ is a successor initiative, ratified as drafted.** Its founding instrument S-1 is the logs-native evaluator (rolling 3 days, 5 arrivals, 0 bookings, own silence deadman) paging `platform_alerts` in business hours; a fresh cold-start seat builds it from the charge, after a warm-up.
2. **The Calendly-intake cutover arc closes with a tripwire, not a build.** Its founding loss measured zero over 30 days on the frame's own query with a control; the kill mechanism stays unverified in code; a metric filter and alarm on the legacy kill signature is the fresh seat's warm-up, and the arc's closure record is written by whoever lands it. Its drafts and its disabled recon schedule are left exactly as they are, NO WATCHER.
3. **#439 and #441 wait.** The attested activation smoke and its Offer-grain referent merge only after clause (d)'s substrate is ruled; the unit-reconciliation spike goes to the identity-activity-substrate session by relay.

## §1 RULINGS

| # | question | options offered | ruling | REC held? |
|---|---|---|---|---|
| **R-148** | Calendly-intake arc disposition (loss 0/30d with control; mechanism unverified; modern path above canary floor with 38 resolve failures; recon rule DISABLED; record stale since 08-13; email seam ruled it corollary) | close with a tripwire · close cleanly, no instrument · re-charter narrowly · wrong question | **Close with a tripwire.** A metric filter + alarm on the legacy kill signature (`V2-MISSING`) in `/ecs/monolith-prod`, routed to `platform_alerts`, two-sided guard test; the telos closes on the measured zero with the mechanism check recorded as UNTAKEN (the legacy repo is outside this seat's directories). | held |
| **R-149** | READ frame | successor initiative as drafted · amendment of name-the-client · not yet, frame first · wrong question | **Successor initiative, as drafted.** `.know/telos/read-the-name.md` is RATIFIED by this sitting; the parent stays closed; decision-space numbering continues from R-147. | held |
| **R-150** | S-1 shape and window | logs-native evaluator W=3d/A=5 · logs-native, other window · per-office CloudWatch alarms · wrong question | **Logs-native evaluator, W = 3 days rolling, A = 5 arrivals, bookings floor 0**, evaluated daily, reading `office_name` from the lines, one alert naming every office over the floor, carrying its own silence deadman; positive control = the 09-04..09-11 replay on logs (the 406-mail office fires day one); negative control = offices with arrivals and bookings stay quiet; the page carries each office's Offer-grain class. | held |
| **R-151** | Next session | fresh cold-start seat builds S-1 · this seat builds · reads first, no builds · wrong question | **A fresh cold-start seat builds**, from the charge and the ratified telos pasted, no inherited framing. This seat stops after tonight's framing (R-157). | held |
| **R-152** | Tripwire hand | this seat tonight on your word · charge it to the fresh session · the email seam's room · wrong question | **Charged to the fresh session as its warm-up, before S-1.** The builder locates the home of `/ecs/monolith-prod`'s existing metric filters, lands the filter + alarm plan-only, checks the stack's apply path against the image-pin rollback hazard before any apply, and writes the Calendly closure record. | **displaced** (REC was this seat tonight) |
| **R-153** | Calendly loose ends (multi) | close the three drafts · find who disabled the recon rule first · re-enable the recon rule now · leave both exactly as they are | **Leave both exactly as they are.** #1561/#1563/#1564 stay open as drafts; `autom8-calendly-intake-recon-schedule` stays DISABLED; both recorded in the closure with NO WATCHER. A closing arc takes no actions on the leg it is leaving. | **displaced** (REC was close drafts + find the disabler) |
| **R-154** | S-1 consumer | platform_alerts business hours · the SEV1 topic · a dedicated channel · wrong question | **`platform_alerts`, business hours**, the topic the EBI semantic alarms already use, dual-routed as `semantic_alarms.tf` is. An unbooked client found Friday evening waits until Monday; accepted. | held |
| **R-155** | #439 / #441 merge words | merge both now · merge after R2's substrate is ruled · merge #439 only · wrong question | **Merge after R2's substrate is ruled.** No module lands without a named consumer; the PRs stay open, attested, updated onto main as needed. | **displaced** (REC was merge now) |
| **R-156** | The unit-reconciliation spike (R2 substrate) | fresh session after S-1 · this seat tonight · the identity-activity-substrate session · wrong question | **The identity-activity-substrate session**, by jurisdiction (as F-2 was). Relayed as a request in their room, not an authorization; read-only: what `reconciliation/processor.py:69-71` keys on, why `reconciliation_runner.py:125` is hard-coded `dry_run=True`, what a real emission would look like, whether enforcement needs an Asana write. Its answer gates R-155. | **displaced** (REC was the fresh session) |
| **R-157** | READ ceremony | this seat frames tonight, fresh seat shapes · fresh seat frames, shapes, builds · no frame yet · wrong question | **This seat writes the frame tonight** (`.sos/wip/frames/read-the-name.md`, from the ratified telos and the charge); **the fresh seat shapes it at its first sitting** and builds from the shape. | held |
| **R-158** | Charge tail (multi) | S-4 triage · the books-while-dark office · C6 arm push · the R-143 PR notice | **S-4 malformed-GUID triage (read-only) · the books-while-dark office `15caa02c` adjudication · arm `push:` on `manifest-validate.yml` (C6).** The R-143 apply-visibility notice stays a separate charge with its own critic. | held (4/4 incl. the no) |

## §2 EXPLICITLY DEFERRED

| item | waits on | watcher |
|---|---|---|
| R-143 charges: PR-side pending-terraform notice · Service Terraform rollback guard | a charge of their own, READ epoch | this seat (charge), then whoever is seated |
| identity one-pager (F-1..F-7) behind (c2) / READ R3 | operator sitting (R-110) | operator |
| GHL contactId un-scaffold (RATIF-VIII-CONTACTID) | operator | **NO WATCHER** |
| the Calendly smoke office and timing (email seam's second window) | operator's act, customer-visible floor | operator, in the email room |
| the asana stack's apply path for its #2178 re-pin (same image-pin shape as EBI) | READ epoch, before any asana terraform apply | **NO WATCHER** |
| the three Calendly draft PRs · the disabled recon schedule (R-153) | — | **NO WATCHER**, by ruling |
| whether the `V2-MISSING` raise is still in the monolith's code | someone with the legacy repo | **NO WATCHER** |

## §3 ASSUMPTIONS THAT REMAIN UNCONFIRMED

1. `platform_alerts` is read by a human in business hours (the S-1 consumer). If not, S-1 has no consumer and the charge says it is not built.
2. Where `/ecs/monolith-prod`'s two existing metric filters are managed, and whether that stack carries an image pin (rollback hazard) — the tripwire builder finds out first.
3. Who disabled `autom8-calendly-intake-recon-schedule` and why; left as is by ruling.
4. The identity-activity-substrate session has room for the spike; if not, R-155 waits longer.
5. The fresh seat's toolchain: a fresh worktree cannot sync a venv (CodeArtifact 401); the charge carries the main-venv + `PYTHONPATH` method with the `__file__` proof.
6. The Calendly measurement's canary/real discrimination was NOT taken (no source field; guid parse did not match); the 38 resolve-stage failures are unattributed to either.
7. Pythia's READ consult and the email room's durability ratification are inherited, not re-derived here.

## §4 CALIBRATION

Eleven items: **seven RECs held, four displaced** (R-152 hand · R-153 loose ends · R-155 words · R-156 spike owner). Sitting IX was 8/4; sitting VIII 0/7. The displacements cluster on *who acts and when*, never on *what is true*; noted, not explained.

## §5 EXECUTION ORDER

**Tonight, this seat:** land this record + the RATIFIED telos (one docs PR, auto-merge) · amend the S-1 charge with R-150/R-152/R-154/R-158 and the fresh-seat toolchain method · write the READ frame (R-157) · relay R-156 to the substrate session · update memory · stop.
**Fresh seat, tomorrow:** read the charge in full, then the ratified telos, then the frame · shape (R-157) · WS-0 tripwire (R-148/R-152) → Calendly closure record · WS-1 S-1 (R-150/R-154) two-sided, deadman included, replay receipt · tail (R-158) · `/handoff`.
**Substrate session:** the R2 spike (R-156); its answer returns here or to the fresh seat's room; then the #439/#441 words (R-155).

**Standing fences, unchanged:** never-grantable floor · no freeze-gate re-arm · no ruleset 17263542 touch without a direct word in the seat's own room · nothing merges on a relay · nothing bundles · nothing merges past red · a peer's relay is never authorization, both directions · no phone digits on any face, no raw client names in the record · `nhc-db` KNOWN AND ACCEPTED · R-80 ROLL-FORWARD-ONLY · no Service Terraform manual apply on a pinned stack (EBI, asana) · Lambda config attestations name the alias-resolved version · an Insights zero needs `recordsScanned` comparable to its control · no AI attribution trailer in commits.

## §6 ADDENDUM — R-156 answered (2026-09-11 ~23:50Z), measured by the identity-activity-substrate session at asana `53f1abb2` / autom8y `225ad2b9`; relayed here, not re-derived

**The candidate substrate does not exist.** autom8y `terraform/services/asana/main.tf:1904-1950` declares `module "unit_reconciliation"` with `handler_command = ["autom8_asana.lambda_handlers.unit_reconciliation.handler"]`, `rate(4 hours)`, `schedule_enabled = false` since the 2026-09-04 recurring-job decommission. **`src/autom8_asana/lambda_handlers/unit_reconciliation.py` has never existed in the repo's history** (0 commits at that path; control: `cache_warmer.py` 50 commits). Its `RECONCILIATION_MODE` / `RECONCILIATION_ENABLED` env keys are read by no code. Un-pausing it yields `Runtime.ImportModuleError` on every invoke. **"Un-pause" and "un-dry-run" are not the levers they appear to be.**

**The dry-run reconciliation the attester found is a different path:** the cache-warmer Lambda's "Phase 5c: Reconciliation shadow mode" (`cache_warmer.py:1200-1215` → `lambda_handlers/reconciliation_runner.py`), gated by `ASANA_RECONCILIATION_SHADOW_ENABLED` (present in **zero** terraform files at autom8y origin/main), hard-coded `dry_run=True` (`runner:125`, "not configurable"), on host Lambda `autom8-asana-cache-warmer`, itself `schedule_enabled = false` since 09-04. Its "emission" is `logger.info` lines only (`report.py:6-7`); `ReconciliationExcludedCount` is a log field, not a metric. Zero events is fully accounted for. The 553 boots are the asana ECS service, which runs none of this.

**Shape mismatch, prior to any lever:** `reconciliation/processor.py` iterates UNIT rows (project 1201081073731555), keys on `(phone, vertical)` (:485), takes offer activity as an INPUT (`OFFER_CLASSIFIER.classify` :602 → `OFFER_ACTIVITY_VALID_UNIT_SECTIONS` :67-77) and reasons about UNIT placement as the OUTPUT; the offer composite index is last-row-wins (:288) so **offer identity is lost at the join**; it is stateless per run — presence in a bucket, never a CROSSING. It cannot observe per-offer transitions into ACTIVATING, which is what #441's referent classifies. Also a **third site** of the Engaged/Scheduled disagreement (:69-71 agrees with `UNIT_CLASSIFIER`, against the vendored monolith table).

**Enforcement would be an ungated Asana write.** `executor.py:44-115`: `dry_run=False` requires four injected dependencies (the runner injects none) and performs `task_service.move_to_section` on Unit tasks via `AsanaClient`. `write_authz` / `is_authorized` (SID-26, observe mode) is enforced only in `api/routes/*` — zero sites under `clients/`, `client.py`, `reconciliation/`. **A Lambda calling the executor live writes around the write-authz gate.** Flagged to the security limb / SID-26 lane; not acted on here.

**Consequence for the READ epoch and R-155:**
- **R2 has no existing substrate.** WS-2 becomes a **build**: a scheduled observer at OFFER grain (project 1143843662099250) that holds prior membership state and emits dated crossings INTO the activating bucket, feeding the smoke (#439) under the ruled referent (#441). Its enforcement path, if ever, must pass write-authz and needs its own ruling. To be charged separately, with its own critic; not the S-1 seat's.
- **Two new carriers for the risk map:** **#15** terraform declaring a Lambda whose handler never existed (a declared instrument with no code, scheduled, paused); **#16** a live write path around the authz gate.
- **#439 / #441 merge words:** R-155's condition ("after R2's substrate is ruled") is now answerable: the ruling is that the substrate must be built. The words stay the operator's.

## §7 ADDENDUM — R-159 (same night, after §6)

| # | question | options offered | ruling | REC held? |
|---|---|---|---|---|
| **R-159** | #439 / #441 after the substrate ruling (§6: no substrate exists; WS-2 is a build) | merge both now as WS-2's inputs · hold until WS-2 is charged and seated · close both and re-land from the WS-2 charge · wrong question | **Merge both now as WS-2's inputs**, stacked order (#439 then #441), recorded as built-unconsumed by design with the consumer named as the WS-2 observer to be built. Execution: #439 auto-merge armed on green by this seat; #441 retargeted to main and merged after #439 lands; shas in the next seat's handoff. | held |

Calibration after R-159: **eight held, four displaced** across twelve items.
