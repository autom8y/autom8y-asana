# DISPOSITION — the dark-office class

- **sprint:** S-08 · WS-DARK · rite `clinic` (attending) · deploy class **C-INERT**
- **initiative:** `name-the-client` · WAVE 1 · session `autom8y-asana-f3`
- **posture:** **DISPOSITION, NOT A FIX.** Read-only. No build, no PR, no branch, no deploy, no
  pipeline cure proposed or designed. DIAGNOSIS GO/PARK's park on pipeline engineering for these
  accounts is **honored and re-affirmed, not revisited** (§9).
- **instant:** 2026-09-09 (probes run 2026-09-08 late; refs and controls recorded per-probe in §2)
- **self-cap:** **MODERATE.** Single seat, self-attested, no rite-disjoint attestation. This artifact
  **re-derives no measurement** — it translates S-01's and PT-02's evidence into a disposition, and
  re-verifies own-hands only the structural facts it leans on (§2).
- **what this advances:** realization predicate **clause (c)** only, and only partially — it settles
  which offices are REAL and which one is REMOVED BY NAME. **It discharges nothing.** A disposition
  produces no booking.

---

## §0 THE DISPOSITION — STATED FIRST

> # FIVE offices. PER-ITEM. TWO evidentiary grades carry the class-reality claim. ONE removed from the class, BY NAME, WITH A RECORDED REASON.

**`REAL CLASS (6)` is an overclaim. It is never ASSERTED in this artifact — it appears only where
it is named as the thing being refused — and it must not be asserted downstream.** Writing a wave-level token over five per-item findings at unequal grades is the
`wave-level CLOSED` pattern (`telos-integrity-ref` §3 Gate-B, F-HYG-CF-A). Per-item or nothing.

| # | GUID | office | class disposition | grade | axis on which the grade was earned |
|---|---|---|---|---|---|
| 1 | `6f22301a-4c51-4282-bf86-a64108e644ad` | **Dr. David Salkin** | **REAL — IN CLASS** | **receipted-live** | outbound lead-notification plane, **lineage-disjoint from Asana**, 75 rows / 73× *"new lead"* in 21 d, controls firing |
| 2 | `1b271a63-33ff-4135-a92d-f1ef0eeea062` | **Sand Lake Dental** | **REAL — IN CLASS** | **receipted-live** | same instrument, 41 rows / 39× *"new lead"*, controls firing |
| 3 | `241355e3-ad2b-4efb-8935-cf44f311a3a1` | **Lazar Spinal Care PC** | **REAL — IN CLASS** | **CRM-evidenced** | Asana-present (4 hits, positive control firing), PLAY task, a month of Intercom forwarding threads, named owner |
| 4 | `cdd8c6cf-b5e5-4689-8383-90b5ba80c709` | **Restore Neuro Rehab** | **REAL — IN CLASS** | **CRM-evidenced** | Asana-present, deck staged `2026-07-08`, contact card, `2026-08-27` DIG result naming an open unanswered objection |
| 5 | `ba3dd6c7-41c7-4745-9ada-556bb661bc1f` | **FNC Minneapolis Concussion Clinic** | **IN CLASS — on MEMBERSHIP** | **membership settled, state UNMEASURED** | Asana-present (1 hit, task `1214407456474353`), allowlist-present. **No state receipt on either axis.** |
| — | `70316996-f9ee-4e54-ac0a-790d2439ae71` | **— no identity of record —** | **★ REMOVED FROM THIS CLASS, BY NAME (§4)** | **not graded — the call is REFUSED** | operator fork **F-1**. **Not decided here. Not decided by any agent.** |

**The two grades, stated precisely so they are not collapsed downstream.** Two offices are
**receipted-live** on an instrument that is lineage-disjoint from Asana. Two are **CRM-evidenced** —
real, worked, named, but corroborated only on the Asana lineage. The fifth (`ba3dd6c7`) is settled on
**MEMBERSHIP** and unmeasured on **STATE**; per PT-02's precision correction, that is an **evidentiary
gap on the state axis, NOT a denominator hole**. It is in the class. Its class-reality is carried by
membership, not by a state receipt. **Do not promote it to `CRM-evidenced` and do not demote it out
of the class.**

### The one-line answer

> *"The dark class is REAL and it is FIVE. Two are proven live by an instrument Asana cannot echo;
> two are proven worked by the CRM; one is proven a member and nothing more. The sixth was never a
> dark client at all — it is an **allowlist orphan**, production-enabled to book while invisible to
> every record we keep, and deciding what it is belongs to the operator, not to me. **Nobody knows
> WHY any of the five are dark, and that does not matter for this disposition** — an office does not
> stop counting because we cannot yet say why it is silent."*

---

## §1 WHAT THIS DISPOSITION IS, AND FOUR THINGS IT IS NOT

**IS:** the translation of S-01's measurement and PT-02's adjudication into a per-office disposition
of record, with the reason for the one exclusion written down BY NAME so it is checkable.

**IS NOT — 1. A RE-DIAGNOSIS.** I did not re-derive S-01's SendGrid probes, its Asana identity
resolution, or DIAGNOSIS §3's 89-day CloudWatch zero. Where I lean on them I cite them (§2.2) and
I re-verified only the *structural* facts underneath (§2.1). Nothing here reinterprets their evidence
to reach a different conclusion. Had the diagnosis been insufficient to disposition, the correct move
was the `diagnosis_insufficient` back-route, not a second opinion. **It was sufficient.**

**IS NOT — 2. A FIX, OR A DESIGN FOR ONE.** No cure is proposed for any of the five. See §9.

**IS NOT — 3. A DENOMINATOR RULING.** These five are dispositioned as **REAL and IN CLASS**. Whether
"in class" resolves to "in the C-3 denominator" turns on the *membership predicate*, which PT-02
established is **two-way ambiguous (H-2) and definitionally undefined (H-3)** and is routed to forks
**F-2** and **F-3**. **I do not rule it and I do not pre-empt it.** What I *do* bind, because it holds
on either branch, is §3.6: **if any of these five ends up OUT, it goes out BY NAME with a recorded
reason.**

**IS NOT — 4. A DECISION ON `70316996`.** §4. The call is refused, deliberately, and the refusal is
itself the disposition.

---

## §2 THE EVIDENCE THIS STANDS ON

### §2.1 Re-verified OWN-HANDS at this sprint's instant — with the asserted dimension named FIRST

> **Fence 2 — DIMENSION NAMED IN WRITING BEFORE THE PROBE.** The dimension asserted is
> **office-GUID membership in the LIVE production booking allowlist** — for each of the six GUIDs,
> is it present in `contente_booking_live_allowlist`, and is the set's cardinality 42? A control that
> only proves the file is readable does not vary that dimension. So: a **corpus control** (file
> non-empty at an explicit ref), a **cardinality control**, a **positive control** (a GUID known
> present), and a **negative control** (a GUID that must come back absent).

**Ref re-resolution — `autom8y origin/main` MOVED A SEVENTH TIME.** S-01 opened at `e292b616` and
closed at `883eb3bf`. At my start it is **`4e0b41f9`**. The asana repo
is unchanged at `389c59bc`. Repo distinctness was proven with
`git remote get-url origin` per repo *before* any read — S-01 recorded a `cd`-persistence error in
exactly this spot (`PROBE §2`) and I did not reproduce it. Every read below used
`git -C <abs-repo> show <explicit-ref>:<path>`. **Nothing was read from a working tree.**

```
autom8y  git[at]github.com:autom8y/autom8y.git         origin/main  4e0b41f9
asana    git[at]github.com:autom8y/autom8y-asana.git   origin/main  389c59bc
```

**PROBE A — allowlist membership.** `4e0b41f9:terraform/services/email-booking-intake/environments/production.tfvars`

```
CORPUS CONTROL   file resolves at explicit ref, rc=0, stderr:[]        347 lines
LINE CONTROL     :160 is 1590 chars, single comma-separated string     (a line-wise count returns 1, not 42 -- not a dead probe)
CARDINALITY      unique full GUIDs extracted from :160                 = 42        <- matches the record
EXTRACTED-SET NON-EMPTY CONTROL                                        = 42 entries

THE SIX -- ALL SIX PRESENT:
  PRESENT  6f22301a-4c51-4282-bf86-a64108e644ad   Salkin
  PRESENT  1b271a63-33ff-4135-a92d-f1ef0eeea062   Sand Lake
  PRESENT  241355e3-ad2b-4efb-8935-cf44f311a3a1   Lazar
  PRESENT  cdd8c6cf-b5e5-4689-8383-90b5ba80c709   Restore Neuro
  PRESENT  ba3dd6c7-41c7-4745-9ada-556bb661bc1f   FNC Minneapolis
  PRESENT  70316996-f9ee-4e54-ac0a-790d2439ae71   THE ORPHAN

POSITIVE CONTROL   PRESENT  b167331c-536f-4996-9b2d-2f696f35f556  (Oak Springs)
NEGATIVE CONTROLS  ABSENT   deadbeef-0000-...    ABSENT  e3267756-0000-...   <- the probe CAN say no
PREFIX-COLLISION CONTROL   each 8-hex prefix -> exactly 1 full GUID; deadbeef -> 0 matches
```

**Two things this earned beyond re-confirmation.** (i) The membership fact **survives a ref move** —
S-01 took it at `883eb3bf`, I take it at `4e0b41f9`, unchanged. It is not a snapshot artifact.
(ii) I resolved the **full GUIDs** for `cdd8c6cf` and `ba3dd6c7` from the allowlist own-hands and they
match the frame's independently-authored list at `.sos/wip/frames/name-the-client.md:529-534` **exactly** —
an independent cross-check of the prefix→full-GUID hop.

**PROBE B — is the named next measurement actually unblocked?** Dimension asserted first: **presence
of `INTERCOM_*` secret NAMES in AWS Secrets Manager**, acct `<aws-account-id>`, `us-east-1`. Names only —
**no secret value was read, requested, or printed.**

```
ACCOUNT CONTROL     aws sts get-caller-identity            -> <aws-account-id>   stderr:[]
LISTING (unbounded, auto-paginated; a --max-results 100 call returns 100 and would be a TRUNCATED listing, not a count)
                                                            -> 191 secret names
POSITIVE CONTROL    grep -i sendgrid   -> SENDGRID_AUTH, SENDGRID_BASE_URL, autom8y/auth/sendgrid-api-key   (3)
ASSERTION           grep -i intercom   -> INTERCOM_APP_ID, INTERCOM_AUTH, INTERCOM_BASE_URL                 (3)
NEGATIVE CONTROL    grep -i zzzznotarealsecret -> 0 matches    <- the probe CAN say no
```

⇒ **The credential whose absence forced the 2026-09-05 VERDICT to refuse B-arm-1 is present.**
Q-C-prime is unblocked. **I did not run it** (§7).

**PROBE C — C-INERT class, verified rather than assumed.** `.ledge/**` is under `paths-ignore` at
`.github/workflows/test.yml:31`, read at asana `389c59bc` (control: the file is 587 lines). Writing
this artifact therefore does not trigger `Test`, and so does not trigger `satellite-dispatch.yml` and
does not roll the asana ECS service. **Note the file's own warning at `:16` — *"Deny-list, not an
allowlist… an unlisted or newly-added path still triggers."*** The guard covers this write; it is not
a general licence.

### §2.2 Inherited, cited, NOT re-derived

| # | fact | source | why I did not re-run it |
|---|---|---|---|
| E-1 | Zero `guid_extracted` for these GUIDs across the **full 90-day retention** on **two disjoint instruments**, 76 offices and 60,830 events of in-query control, no truncation | `DIAGNOSIS-verified-not-enabled-2026-09-08.md` §3 | live CloudWatch Insights measurement; the pathologist/diagnostician lane owns it |
| E-2 | Salkin **75 rows / 73× "new lead"**, Sand Lake **41 / 39**, on a plane lineage-disjoint from Asana, every zero carrying a two-sided operator control | `PROBE-qc-outbound-deck-delivery-2026-09-08.md` §5.3, §3 | same |
| E-3 | **★ The control that fired:** Foundation Spine & Posture, the only deck staged inside the SendGrid window (`2026-08-27`), appears **64 times** in-window and **the deck is absent from all 64** ⇒ the deck travels by **Intercom, not SendGrid** | ibid. §4, §7 | this is the load-bearing control of the whole wave; re-running it would not strengthen it, and S-01 published it verbatim-reproducible at §9 |
| E-4 | SendGrid Email Activity retains **30 days**, measured floor `2026-08-09T22:23:55Z` by bisection; **not** a 403 — the add-on is live, `messages.read` present | ibid. §3, §6.1 | ibid. |
| E-5 | Five of six resolved to named offices in live Asana with a firing negative control (`deadbeef` → 0) and firing positive controls (`241355e3` → 4, `ba3dd6c7` → 1); **`70316996` → 0 on all three query forms** | ibid. §5.1 | live Asana API; measurement lane |
| E-6 | `Forwarding Stage` is wrong in **both** directions — Lazar `Flowing` with zero arrivals (false-GREEN), Salkin `Verified → Stalled` | ibid. §0, §5.2 | ibid. |
| E-7 | Membership-invariance of the deck question; the denominator-inversion argument; the `REAL CLASS (6)` overclaim; the named-exclusion constraint; the F-register | `CUSTODY-name-the-client-wave0-register-2026-09-08.md` A9.1–A9.4 | adjudication, not measurement; binding on this sprint by dispatch |

> **PT-02's own self-limits travel with its findings and are repeated here so they are not laundered:**
> PT-02 **measured nothing** and self-capped **explicitly lower than the seats it read**. Its
> **H-1(ii)** (the symmetric orphan — an Asana-active office *not* on the allowlist) and its
> **H-2 narrow-reading consequence** are marked **its own inference, NOT measurement**, and are not
> re-cited as measurement anywhere in this artifact. H-1(ii) is **structural in kind, evidentiary in
> cardinality — never counted, and NOT asserted non-empty.**

---

## §3 PER-OFFICE DISPOSITION — five, at grade

### §3.1 `6f22301a` — Dr. David Salkin — **REAL, IN CLASS — receipted-live**
**Reason of record.** 75 rows of outbound mail in 30 days, 73 of them *"You have a new lead from
Contente"*, on an instrument **lineage-disjoint from Asana**, with a firing two-sided operator control
(`to_email LIKE "%zzzznotarealdomain%"` → 0). Bookings on the arrival axis: **zero for 90 days.**
**A live customer being served 73 leads into a pipe whose return path was never connected.** This is
the single strongest row in the class and it is not a reporting artifact under any reading.

### §3.2 `1b271a63` — Sand Lake Dental — **REAL, IN CLASS — receipted-live**
**Reason of record.** 41 rows / 39× *"new lead"*, same instrument, same controls. CRM corroboration is
additionally present (deck staged `2026-07-07`, `Forwarding Stage = Sent`, an Intercom permalink on the
task) but **the grade is earned by the lineage-disjoint instrument, not by the CRM.** A `2026-08-27`
DIG result records the account blocked on the client's unanswered `06-22` objection, with the `07-07`
walkthrough sent **on top of** the open question.

### §3.3 `241355e3` — Lazar Spinal Care PC — **REAL, IN CLASS — CRM-evidenced**
**Reason of record.** Asana-present (4 hits, positive control firing). A **month** of Intercom
follow-ups on Gmail forwarding, `2026-04-30 → 2026-05-19`, including the client's own *"Thanks for
adding our forwarding address. We've approved it on our end…"*. Named owner. A PLAY task.
**They were asked, repeatedly, by name.** Then `2026-07-11T00:27:40Z`: `Forwarding Stage → Flowing`.
**Zero arrivals, ever.**

**★ The false-GREEN, recorded as a finding of this disposition and not merely as context.** `Flowing`
on an office with a 90-day arrival zero is a **false-GREEN in a surface a human reads.** PT-02 admitted
`Forwarding Stage` as the **fourth** member of the forbidden-denominator-input family — joining ASR
`activity`, `account_status`, and `active_section_days` — and it is **the first of them to sit where a
PERSON, not a query, consumes it.** Binding consequence: **`Forwarding Stage` MUST NOT be used as a
denominator predicate or as an exclusion criterion, in either direction.** Diagnosing *why* Lazar reads
`Flowing` is **not this artifact's lane and is parked** (§9); S-01 established the contradiction and
explicitly declined to diagnose it.

### §3.4 `cdd8c6cf` — Restore Neuro Rehab — **REAL, IN CLASS — CRM-evidenced**
**Reason of record.** Asana-present. Deck staged `2026-07-08T13:57:29Z`; contact card
`[mailbox redacted][at]bridgewellnesscenteraz.com`; `Forwarding Stage → Sent` `2026-07-09T10:38:09Z`. A
`2026-08-27T17:15:05Z` DIG result: *"account blocked on our unanswered 06-21 question (Jane App →
Google Calendar reverse sync); 'backend team will review' (06-22) never closed. Weekly reports show
10–14 leads/we[ek]."*

**Grade caveat that travels with this row.** Its outbound zero is graded **dark-on-outbound**, NOT
*no-outbound-mail*: the contact card is from `2026-07-08`, the office name does not match the address
domain, and **address confidence is LOW** (S-01 §10.6). The zero is honest about what it is.

### §3.5 `ba3dd6c7` — FNC Minneapolis Concussion Clinic — **IN CLASS on MEMBERSHIP; state UNMEASURED**
**Reason of record.** Asana-present — 1 hit, task `1214407456474353`, positive control firing — and
allowlist-present (verified own-hands, §2.1). **Membership is SETTLED.**

**What is NOT settled, stated plainly.** Its outbound state is **unmeasured, not zero.** S-01's
`%neurology%` / `%concussion%` probes were **guessed domains** and S-01 refused to report their zeros
as taken. There is **no contact address in 100 task stories**, **no `Forwarding Stage` field at all**,
and **no deck story** — it appears never to have entered the deck motion.

> **This row is the class's own untaken-zero test and it was passed.** The temptation is to read
> "0 rows" as "dark like the others." It is not. **An unmeasured office is ABSENT from the
> measurement, not ZERO in it** — and the difference between ABSENT and ZERO is the exact
> distinction this initiative is built on. Recorded as unmeasured. **Not promoted. Not demoted.**

### §3.6 ★ BINDING ON ALL FIVE — AND ON EITHER BRANCH OF EVERY OPEN FORK

> **If any of these five ends up OUT of the denominator, it must be OUT BY NAME, WITH A RECORDED
> REASON — never merely absent.**

This is PT-02's shape-constraint and it follows from the ratified predicate's own two-sidedness. **A
denominator carrying SILENT exclusions makes clause (c)'s *"held across ALL"* unfalsifiable for
exactly the offices most likely to be wrongly excluded.** A named exclusion is checkable; an absence
is not. It is the untaken-zero fence, one altitude up.

**The live hazard this guards against, stated because it is not hypothetical.** PT-02 established
that on the **narrow** reading of "active client" (`active` bucket only), **the bar could be reported
GREEN while naming none of the dark offices — vacuously satisfied by excluding the very class it was
written for.** *[PT-02's inference. The HAZARD is asserted; **the PLACEMENT is not** — which section
each of the five actually occupies live was **not measured by any seat**, including this one.]*

---

## §4 ★ THE NAMED EXCLUSION — `70316996-f9ee-4e54-ac0a-790d2439ae71`

> ## REMOVED FROM THE DARK-OFFICE CLASS. BY NAME. WITH THE REASON RECORDED HERE.
> ## AND: WHAT IT ACTUALLY IS, IS **NOT DECIDED** — THAT IS OPERATOR FORK **F-1**, AND THE CALL IS **REFUSED**.

**THE RECORDED REASON FOR REMOVAL.** It fails the class's defining conjunction. A *dark office* is a
**known client whose mail never arrives**. This GUID satisfies the second half and **not the first**:

- **PRESENT** on the live 42-office booking allowlist — re-verified own-hands at `4e0b41f9`
  (§2.1), cardinality control 42, positive control present, negative controls absent.
- **ABSENT** from Asana on **three** query forms — full GUID, 8-hex prefix, routing address — with
  **positive controls firing** (`241355e3` → 4 hits, `ba3dd6c7` → 1) and a **negative control firing**
  (`deadbeef` → 0).

**Both readings are receipted. NEITHER ZERO IS UNTAKEN.** Nothing reconciles them and **nothing
watches the disagreement.** It is not a dark client — it is an **ALLOWLIST ORPHAN**: an office
**production-enabled to take live bookings while invisible to every client record we keep.**

**Why the direction of the error is what makes it load-bearing.** If it books, the booking arrives
from an office the bar **cannot name** — clause (a) fails silently. If it fails, clause (b)'s *"names
it, with its kind, never blank"* **has nothing to name.** This is the denominator-inversion one
altitude up: *a denominator derived from Asana names only the offices Asana carries.*

### §4.1 WHAT IS **NOT** DECIDED HERE, AND WHY THE REFUSAL IS ITSELF THE DISPOSITION

**F-1 — *which source is authoritative when booking-enablement and Asana membership disagree?* — is
NOT AGENT-DECIDABLE, and I do not decide it.** Under C-16's current adapter this office is **OUT — and
not merely out, INVISIBLE**, which is different and worse than excluded. Under C-3's words it turns on
whether it is a *client*, which is **undefined** (H-3, F-3). **No artifact on this record says which
authority governs. That is a RULING, not a measurement**, and PT-02 refused the call on exactly those
grounds. So do I. An attending that manufactures a ruling to tidy a table has substituted its own
judgment for the operator's.

**Removal from the class ≠ exclusion from the denominator.** These are two different acts and only the
first is mine. **This artifact is the named-exclusion receipt for the first** — `70316996` is out of
the *dark-office class* by name, with the reason above, so a future reader can check the removal rather
than discover an absence. **Its denominator status remains OPEN under F-1 and is untouched by this
artifact.** If F-1 resolves toward "real client," it re-enters as a **sixth** and this disposition must
be amended **by name**.

### §4.2 ★ WHAT THIS OFFICE PROVES REGARDLESS OF HOW F-1 RESOLVES

C-16 requires the denominator seam to be **EXTENSIBLE**. The received reading is that extensibility
insures against a **FUTURE** source swap. **This office refutes that reading.** The enablement plane is
**already a second source, already live, and it already disagrees with the first.** **C-16 is not
aspirational — it is being EXERCISED TODAY.** That holds on either branch of F-1 and is recorded as a
finding of this disposition, not as a recommendation.

---

## §5 WHY THE DECK QUESTION DOES NOT GATE ANY OF THE ABOVE

The sprint that produced this class was gated on Q-C — *"was the deck ever SENT?"* — and Q-C returned
**INSTRUMENT UNAVAILABLE** and, independently, **WINDOW TOO SHORT**. That could have been read as
"the class cannot be dispositioned yet." **It is the wrong reading, on two independent grounds.**

**(1) MEMBERSHIP-INVARIANCE.** Every branch the deck question could return leaves membership unchanged:

| Q-C-prime returns | effect on membership |
|---|---|
| deck never sent | **none** — supplies a **KIND** for clause (b) (*"never onboarded"*), not an exclusion |
| deck sent, unopened | **none** — a kind |
| deck sent and opened | **none** — a kind |
| the instrument fails again | **none** |

> **An input whose every possible value leaves the output unchanged CANNOT BE A BLOCKER for that output.**

**(2) REQUIRING THE CAUSE WOULD INVERT THE DENOMINATOR.** If *"we must know why an office is dark
before we count it"* were the rule, then **DARKNESS ITSELF BECOMES A DE-LISTING CRITERION** — the exact
failure this initiative is named against. **The bar is built on the difference between ABSENT and
ZERO.** An office does not stop counting because we cannot yet say why it is silent.

⇒ **"REAL CLASS, CAUSE UNKNOWN" IS SUFFICIENT FOR INCLUSION**, and it is the honest form of §0.
**Q-C-prime is ON clause (b)'s critical path and OFF clause (c)'s.** It supplies a KIND, not a
membership. That is where it belongs and that is where §7 leaves it.

---

## §6 WHAT THIS DISPOSITION DOES **NOT** LICENSE — the misreading register

Every fix and every finding carries risk. These are this artifact's, stated so downstream readers
are not surprised by them.

| # | the misreading | why it is wrong | the guard |
|---|---|---|---|
| **M-1** | reading *receipted-live* as a **two-sided** liveness test | **The outbound lead-notification instrument is ONE-SIDED: it can ADD to the class, NEVER SUBTRACT.** 30-day window; it sees only offices that receive lead notifications. **State the asymmetry wherever it is cited or it will be misread.** | §3.1, §3.2 |
| **M-2** | reading the 90-day zero as **"never"** | The log group is **~149 d old** and retention is 90 d. **`2026-04-12 → 2026-06-10` is UNOBSERVABLE to CloudWatch; `2026-04-12 → 2026-08-09` is unobservable to SendGrid.** *Neither instrument can see the period in which these offices were actually onboarded.* On these instruments, `never` means `never within retention`. | §2.2 E-1/E-4 |
| **M-3** | reading a **per-office** conclusion out of `parse/stats` | **`parse/stats` has NO RECIPIENT DIMENSION.** No claim in this artifact rests on any aggregate. `parse/stats` was not called by S-01 and is not called here. | fence |
| **M-4** | upgrading *consumed-without-inspection* into **"a booking was lost"** | A peer measured that sked mail is consumed terminally without recording content — **retroactively unmeasurable**. The same discipline binds all five: **we can say their mail never arrived; we cannot say a booking was lost.** Nothing in §3 claims a lost booking. | fence |
| **M-5** | using **`Forwarding Stage`** as a denominator or exclusion predicate | Proven wrong in **both directions on the same measurement** — Lazar `Flowing`/zero-arrivals, Salkin `Verified → Stalled`. **Fourth member of the forbidden-input family.** | §3.3 |
| **M-6** | promoting **`ba3dd6c7`**'s guessed-domain zeros to a taken zero | **Guessed domains. The zeros are NOT taken.** Unmeasured ≠ zero. | §3.5 |
| **M-7** | reading **`cdd8c6cf`**'s zero as *no outbound mail* | Address confidence is **LOW**; graded **dark-on-outbound**. | §3.4 |
| **M-8** | reading §4 as *"`70316996` is excluded from the denominator"* | **It is removed from the CLASS, by name. Its DENOMINATOR status is F-1 and is OPEN.** Two different acts; only the first is done. | §4.1 |
| **M-9** | reading §0 as *"the five are IN the C-3 denominator"* | The denominator's **predicate is undefined and its grain unnamed** (F-2, F-3). They are **REAL and IN CLASS**. Their denominator membership is **not ruled here**. | §1, §3.6 |
| **M-10** | reading E-3 as settled beyond its N | S-01's strongest claim — that SendGrid is structurally blind to deck sends — rests on **ONE** in-window control office. Foundation Spine was **the only** deck staged after the retention floor. **N=1 on the control.** A second in-window deck send would strengthen it; none exists. | §2.2 |

---

## §7 THE NAMED NEXT MEASUREMENT — **Q-C-prime**. NAMED, NOT TAKEN.

> ### Q-C-prime — the **INTERCOM** conversation API. Not SendGrid.
> **Status: UNBLOCKED.** **Owner: DISPATCH, not this seat.** **Serves clause (b), not clause (c).**

**Why it is the correct successor.** Q-C's premise was refuted by its own in-window control: the deck
does **not** traverse SendGrid, so `/v3/messages` cannot answer "was the deck sent" for **any** office,
in **any** window — including offices where the answer is demonstrably *yes*. The record says the
sender is *"a constant — Nova / `support[at]contenteapp.com` **via Intercom**, not the rep."*

**Why it is unblocked — the blocker is measurably gone.** `INTERCOM_AUTH`, `INTERCOM_APP_ID` and
`INTERCOM_BASE_URL` are **present** in Secrets Manager acct `<aws-account-id>`, `us-east-1` — re-verified
own-hands at §2.1 Probe B with a firing positive control and a firing negative control. **That is the
exact credential whose absence forced the 2026-09-05 VERDICT to refuse B-arm-1.** Per-office
conversation permalinks are already on the Asana deck stories (Sand Lake `215474754286826`, Lazar
`215474129292698`, Restore Neuro `215474577154148`, Foundation Spine app-user `63ee63979f949cc64457b127`).

**Why I did not take it.** S-01 declined on the grounds that *"re-aiming the instrument is a dispatch
decision, not mine."* **The same applies to this seat, and more so:** an attending that re-aims an
instrument has stopped translating a diagnosis and started producing one. **Naming it is the whole of
my authority here.** The `received`-element UV-P at
`VERDICT-client-onboarding-delivery-2026-09-05.md` §3.1 **remains OPEN, in both directions.**

**What it will and will not deliver.** It supplies a **KIND** for clause (b) — *"never onboarded"* /
*"sent, unopened"* / *"sent, opened, still dark"*. **It cannot change any membership in §0** (§5).
Dispatching it is fork **F-5**.

### §7.1 Two further read-only probes that would NARROW F-1 — named, available, NOT taken

Recorded because the record had not noticed they are unblocked. **Both read-only. Neither is pipeline
engineering. GO/PARK untouched.** Neither *decides* F-1 — they narrow it.

1. **`get_business_by_guid_async` on the FULL GUID.** The data service holds guid→business
   **authoritatively** (`resolve_office.py:170`). **The stated obstacle — *"only the prefix→full-guid
   hop is missing"* — DOES NOT APPLY HERE, because the allowlist gives all 36 characters** (and §2.1
   re-confirmed them own-hands). Distinguishes *a real client with no CRM record* from *a stale or
   test allowlist entry*. **These are opposite operator decisions and the probe separates them.**
2. **A name-based Asana search**, as an independent second probe of the same absence. *Honest residual
   on S-01's zero:* its controls prove the method finds a GUID **when the GUID string is written into
   a task** — the other five carry it because their routing addresses embed it. **An office that never
   had a routing address provisioned would return the same zero for a different reason.** This probe
   is what would tell those two apart.

---

## §8 OPERATOR FORKS LEFT OPEN — **ALL FIVE: NO WATCHER**

Carried forward per **C-12** (*"each with owner, trigger, and watcher, **NO WATCHER** written where
true"*). **Written where true, five times.**

| # | fork | owner | status after this disposition |
|---|---|---|---|
| **F-1** | Is `70316996` a client — and generally, **which source is authoritative when booking-enablement and Asana membership disagree?** | **OPERATOR** | **OPEN. REFUSED by this seat (§4.1).** Narrowable by the two read-only probes at §7.1. **NO WATCHER.** |
| **F-2** | **Which `activating` vocabulary governs, and at which project/grain is the denominator drawn?** | ARCHITECT / OPERATOR | **OPEN.** Routed as an S-07 hook-binding question, but it now has a **second, larger consumer: C-3's denominator.** A scope **ENLARGEMENT** — **must not be settled on S-07's needs alone.** **NO WATCHER.** |
| **F-3** | **What is a "client" — contract or billing state?** | **OPERATOR** | **OPEN. ROOT fork; F-1 and F-2 are partly downstream of it.** **NO WATCHER.** |
| **F-4** | `CustomGHLId` membership | OPERATOR | OPEN, not closable by inference. **NO WATCHER.** |
| **F-5** | **Dispatch Q-C-prime (Intercom)** | **DISPATCH, not operator** | **UNBLOCKED (§2.1 Probe B, §7).** Serves clause (b) kinding, **NOT** clause (c). **NO WATCHER.** |

### §8.1 Unwatched structural rows this disposition does not close

Named, with **no owner assigned by this seat**, because assigning one is a dispatch act and this
sprint is C-INERT. **A monitoring gap is a contributing factor, not a footnote** — each row below is
part of why this class reached us through a human instead of an instrument.

| row | state |
|---|---|
| **The allowlist ↔ Asana disagreement** that produced `70316996` | **NOTHING RECONCILES THEM AND NOTHING WATCHES THE DISAGREEMENT.** Both readings receipted; neither zero untaken. |
| **H-1(ii) — the symmetric orphan** *(PT-02's inference, NOT measured)* | An office in an Asana active/activating section but **NOT** on the allowlist would be **IN** the denominator and **INCAPABLE OF PRODUCING A BOOKING BY CONSTRUCTION** — clause (c) unsatisfiable **silently, with no red signal anywhere.** **Never counted. NOT asserted non-empty.** |
| **`Forwarding Stage` false-GREEN** (Lazar `Flowing` / zero arrivals) | Live, in a surface a **human** reads. Unreconciled against arrivals. |
| **The condition itself** — `verified != enabled`, ratified in committed code since 2026-07-09 | It reached us **through client success, not through an instrument.** |

---

## §9 WHAT IS PARKED — AND STAYS PARKED

**DIAGNOSIS GO/PARK stands, unamended and unrevisited by this artifact.**

- **PARKED — "fix the booking pipeline for these accounts."** The receiving chain is provably working
  at every layer we own: MX → `mx.sendgrid.net`, Inbound Parse catch-all on
  `appointments.contenteapp.com`, alias a pure function of the GUID with no provisioning step, all six
  GUIDs on the 42-office allowlist (re-verified own-hands, §2.1), 76 other offices arriving daily
  through the same route. **Engineering aimed at the pipeline for these accounts would be building a
  cure for a cost that has not been measured to exist.**
- **PARKED — any widening of intake log lines to carry office identity.** Coupled to the PII control:
  **the office-blindness of the 1,252-event residual IS the control.** Not to be traded for
  observability without a warden + security ruling.
- **NOT DIAGNOSED HERE, deliberately:** *why* Lazar reads `Forwarding Stage = Flowing` against a
  90-day arrival zero. S-01 established the contradiction and declined to diagnose it. **So do I.**
  It could be a manual flip, a bad automation, or a real forwarding setup pointed elsewhere.

**No cure is proposed. No file outside `.ledge/decisions/` was written. No branch, no PR, no deploy.**

---

## §10 HOW THIS DISPOSITION IS FALSIFIED

A disposition that cannot be checked is an opinion. These are the checks, and each names the office
it would move.

1. **Membership-invariance (§5) is falsified** if Q-C-prime returns any value that changes a §0 row's
   *class* rather than its *kind*. **Predicted: it will not.** If it does, §5's central argument is
   wrong and §0 must be re-derived.
2. **§4's removal is falsified** if either §7.1 probe returns a **real business** for
   `70316996-f9ee-4e54-ac0a-790d2439ae71`. Then it re-enters as a **sixth**, and this artifact is
   amended **by name** — never by silently growing the table.
3. **§3.5 is falsified** the moment `ba3dd6c7`'s contact address of record is obtained and the §5.3
   probe is re-run against it — at which point its state axis becomes **measured** and its row moves
   in whichever direction the measurement says.
4. **§3.6 is violated — and the violation is checkable** — if any future denominator artifact contains
   fewer than these five offices **without naming which one it dropped and why.** *A named exclusion
   is checkable; an absence is not.* **That check is the point of writing §0 as a table of names.**
5. **§0's grades are falsified** if the outbound lead-notification plane is ever shown to be
   same-lineage with Asana — which would collapse `receipted-live` into `CRM-evidenced` for rows 1–2.
6. **The whole class is falsified** if any of the five ever produces a `guid_extracted` event.
   **That would not be a defeat — it is clause (a) advancing**, and it is the outcome this initiative
   exists to produce.

---

## §11 WHAT I DID **NOT** VERIFY

An honest halt beats a plausible answer.

1. **Anything about deck delivery, in either direction, for any of the six.** The instrument that
   could answer it exists and is credentialed (§7). **I did not run it.**
2. **Which Asana section each of the five actually occupies live.** **No seat has measured this,
   including me.** PT-02's narrow-reading hazard is therefore **asserted, and its placement is not**.
3. **H-1(ii)'s cardinality** — whether any Asana-active office is missing from the allowlist. **Never
   counted. I did not count it. It is not asserted non-empty.**
4. **`70316996`'s identity.** Not probed. §7.1 names the two probes that would narrow it; **I took
   neither**, because taking them invites the seat that takes them to answer F-1, and F-1 is the
   operator's.
5. **DIAGNOSIS §3's CloudWatch zero and S-01's live SendGrid/Asana probes.** Inherited and cited (§2.2),
   **not re-derived.** Every one is verbatim-reproducible from `PROBE …§9`.
6. **Whether another SendGrid account or subuser carries the decks.** S-01 probed one API key. **A
   different account would be invisible.** A further reason the deck question is *"this instrument
   cannot answer,"* not *"never sent."*
7. **P1 itself** — whether the clinics' Gmail forwarding is switched on. **Unreachable by any
   instrument we own.** Still only Q-D.

---

## §12 FENCES, RULINGS, AND PREDICATE — SELF-AUDIT

| fence | status | evidence |
|---|---|---|
| **1 — UNTAKEN ZERO** | **HELD, and it BIT** | Every zero this artifact asserts carries a positive control **over the dimension asserted**: allowlist membership (cardinality 42 + `b167331c` present + `deadbeef`/`e3267756` absent + prefix-collision control); Intercom secrets (3 sendgrid names POS, `zzzznotarealsecret` NEG). **Zeros I refused to convert:** `ba3dd6c7`'s guessed-domain zeros (§3.5), `70316996`'s Asana zero into a denominator ruling (§4.1), the 90-day zero into "never" (M-2). |
| **2 — DIMENSION NAMED IN WRITING BEFORE THE PROBE** | **HELD** | §2.1 states the asserted dimension for Probe A and Probe B **before** the result, in the artifact, unedited. |
| **3 — PIPES LAUNDER EXIT CODES; A LISTING IS NOT A COUNT** | **HELD** | `git show` rc and stderr captured and echoed (`rc=0 stderr:[]`). The secrets **listing** was taken **unbounded/auto-paginated (191)**, and it is recorded that a `--max-results 100` call returns 100 and would be a **truncated listing, not a count**. Allowlist cardinality taken by extract→`sort -u`→count with a **non-empty control on the extracted set**, never by eyeballing a line. |
| **4 — `.sos/` needs `--hidden` AND `--no-ignore`; brace `"${VAR}:path"`** | **HELD** | All `.sos/` reads via absolute-path `sed`/`grep`, not `rg`. Braced `"${REF}:${P}"` form throughout; repo passed as a separate arg to `git -C`. |
| **stale-tree** | **HELD** | Distinctness control (`git remote get-url origin` per repo) run **before** any read. `autom8y origin/main` re-resolved to `4e0b41f9` — a **seventh** move since the charge. All reads at explicit refs; **no working tree read**. |

| binding ruling | honored how |
|---|---|
| **Do not re-diagnose** | §1. No measurement re-derived; no evidence reinterpreted; the diagnosis was sufficient, so no back-route. |
| **"REAL CLASS, CAUSE UNKNOWN" is sufficient for inclusion** | §5, and §0 is written as *"REAL"* with cause unstated. |
| **Requiring the cause would invert the denominator** | §5(2), restated as this artifact's own reason for not gating on Q-C-prime. |
| **Q-C-prime is ON clause (b), OFF clause (c)** | §7 — named as supplying a **KIND**, never a membership. |
| **`REAL CLASS (6)` would be an overclaim** | §0 — **five, per-item, at two grades, one removed.** The token is never ASSERTED here; its three occurrences (§0, §2.2 E-7, this row) are all naming-the-refusal. Verified by grep, not by assertion — an "appears nowhere" claim in a sentence containing the token would falsify itself. |
| **OUT must be OUT BY NAME with a recorded reason** | §3.6 (binding on the five) and §4 (**executed** for `70316996`: this artifact IS its named-exclusion receipt). |
| **Do not decide F-1** | §4.1 — **refused, and the refusal is the disposition.** |
| **No pipeline engineering; GO/PARK stands** | §9. |
| **Do not read a per-office conclusion out of `parse/stats`** | M-3. Never called. |
| **Do not report the 90-day zero as "never" beyond retention** | M-2. |
| **Do not upgrade consumed-without-inspection into "a booking was lost"** | M-4. **No lost booking is claimed for any of the five.** |

**Deploy class: C-INERT — VERIFIED, not assumed.** §2.1 Probe C: `.ledge/**` is under `paths-ignore`
at `.github/workflows/test.yml:31` @ asana `389c59bc`, with the file's own deny-list-not-allowlist
warning at `:16` recorded rather than smoothed over.

**REALIZATION PREDICATE.** *"Verified-realized" = a LIVE attributed booking naming that office,
TWO-SIDED — a failure for the SAME office also names it, WITH ITS KIND, never blank — held across ALL
active clients, not one. NOT "PRs merged". DONE IS A BAR, NOT A DATE.*

> **ADVANCES: clause (c) only, and partially** — it settles which offices are REAL, at grade, and
> removes one by name. **DISCHARGES: none of the four.** No booking exists. No failure line names an
> office. No account was refused activation. **A disposition produces no booking.** Clause (c) remains
> **UNFALSIFIABLE** while the denominator's predicate is undefined (F-2, F-3) — this artifact narrows
> its *contents*, not its *definition*.

**Self-cap: MODERATE.** Single seat, self-attested, **no rite-disjoint attestation**. The rite-disjoint
attester of record for this initiative is **integrity-architect (dre, co-seated)** under
critic-never-author; nothing here is attested by it.

---

*Authored by the attending seat, rite `clinic`, sprint S-08 · WS-DARK · session `autom8y-asana-f3` ·
initiative `name-the-client` WAVE 1 · 2026-09-09.*
