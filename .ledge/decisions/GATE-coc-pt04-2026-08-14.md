---
type: decision
status: accepted
artifact_id: GATE-coc-pt04-2026-08-14
wave: chain-of-custody-closure
checkpoint: PT-04
session: coc-phase-2
rendered_by: potnia (10x-dev, Read-only; main thread owns git, persistence, and all dispatches)
date: 2026-08-14
verdict: PASS-WITH-CARRIES
origin_main: d7560153 (asana; identical to the Phase-2-entry pin — substrate of record)
fans_in: [CC-5 (RE-1), CC-7 (E1 two-action), SEC-002 (RE-2 charge)]
scope_fence: rules NO operator fork. F-2, F-4, GATE-FORK, RE-2 remediation + severity re-grade, every merge word, the AL-5 re-price (BR-4), the branch-protection registration EXECUTION, the R-CC7-1 triage, and the TB-11 finding-open all stay operator-reserved and untouched here.
self_assessment_cap: MODERATE (single orchestrator seat, self-referential; sprint receipts corroborated by the named rite-disjoint second reads, not re-run by this seat)
---

# GATE — chain-of-custody-closure PT-04 (Phase-2 fan-in)

## §1 VERDICT — PASS-WITH-CARRIES

No sprint over-claimed; no wall breached; no critic issued BLOCK. Every rung is honest against its authorised ceiling, and CC-7's job half deliberately **under**-claims. Per-sprint rung confirmation:

| sprint | rung (claimed) | ceiling (authorised) | second-read verdicts | honest? |
|---|---|---|---|---|
| **CC-5** RE-1 | `rung-BUILT` (dark @ `6b75279f`) | `rung-BUILT`/`PR-UP-MERGE-HELD` (Amdt A: buys merge-ready artifact, never a clean AL-5 regime) | structure-evaluator **CONCUR-WITH-FLAGS**; qa-adversary **PASS-WITH-NARROWINGS** | YES — §0/§6/§7 refuse any AL-5 claim; window carried UV-P |
| **CC-7** job half | `rung-BUILT-DARK` (@ `a922d8f9`) | `PR-UP-MERGE-HELD` | arch-adversary **PASS/CONCUR** ("under-claims, one rung below ceiling"); qa-adversary **PASS-WITH-FLAGS** | YES — under-claims (PR is a push, F-4 forbids) |
| **CC-7** registration | `rung-STAGED` (runbook, never executed) | `rung-STAGED` | both concur PATCH form correct, ordering law sound | YES — not executable this session |
| **SEC-002** RE-2 | `STRUCTURALLY-VERIFIED` (committed @ `24a52c52`) | severity-only, gates no rung; RE-2 F-001 stays **HIGH** un-re-graded (R-2) | qa-adversary **HIGH survives** | YES — no re-grade; UV-Ps preserved |
| **CC-1** (carried) | `PR-UP-MERGE-HELD` (PR #365) | untouched this session | — | **CONFIRMED untouched** (autoMerge=false; CC-5 §6 + CC-7 §5 attest #365 unarmed) |

## §2 NCSR CONFIRMATION — 5/5, MODERATE cap held

Five rite-disjoint second reads, one pair per sprint, plus SEC-002's disjoint reader: (1) ASSESSMENT-cc5 structure-evaluator[arch]; (2) CRITIQUE-cc5 qa-adversary[10x-dev]; (3) ADVERSARY-cc7 arch-adversary[arch]; (4) CRITIQUE-cc7 qa-adversary[10x-dev]; (5) CRITIQUE-sec002 qa-adversary[10x-dev] over the dependency-analyst[arch] dossier. **No critic graded its author STRONG** — every frontmatter self-caps MODERATE; every STRONG in the bodies is leg-scoped and rite-disjoint-corroborated. 1:1 disjointness holds in all five pairs.

**BR-5 composition flag (standing, NOT a fault of this wave):** arch supplied 3 author/critic seats again (structure-evaluator, arch-adversary, dependency-analyst); qa-adversary[10x-dev] carried the other 3 reads — so the pool **did** spread beyond arch (qa-adversary, idle in Phase 1, now the disjoint reader on all three sprints). The arch-heavy supply is a composition observation for the roster, not a gate defect.

## §3 MATERIAL CARRIES into H-4 + CC-8 — MOVES vs BOUNDED

**MOVES a fork or a future rung (load-bearing — carry with provenance):**
- **R-CC7-1 / CH-03 false-green residual** — 31/49 live-at-HEAD secret-shaped strings baseline-masked, but **0 are `asana-native-pat`** and cred-t21 fossils **CONFIRMED absent at HEAD**. Benign now. **BLOCKS any future "history is clean" claim that drops R-CC7-1** (arch-adversary would revise that artifact to BLOCK). The triage pass is operator-scoped, out of CC-7. **Attach R-CC7-1 to every downstream citation of the green gate.**
- **RE-2 stays HIGH (R-2, no re-grade)** but its *rationale* moved: NF-1 population-level property (the exemption path has one unfiltered `if !business_scoped`, not a two-SA exception) + AGG-1 (second boot-time emitter `sa_reconciler.py` restores the bypass tuple — weakens the "revocable via tuple removal" mitigation) + AGG-2 (no revocation backstop; 300s TTL is the *sole* bound). Remediation target shifts "review two SAs" → "the exemption path has no filter." **Feeds the operator's RE-2 remediation, which stays reserved.**
- **CF-1 determinant** — YES at the registry layer, now **chain-traced** registry→OpenFGA(two emitters)→issuance→claim (`token_service.py:780` bypass=True, business_id omitted). The "second uncounted population" **inverts**: it is a population-of-one (`iris`≡`autom8y-hermes`), and its `asana:read` OAuth grant is **inert** (iris mints on the SA path). This is a **governance-integrity defect** (divergent scope truth, shipped docstring wrong), not an access-control differential — an **adjacent finding to open** (operator's word, NOT SEC-002's self-assignment; NOT CF-7).

**BOUNDED (do not move a fork/rung):**
- **FLAG-1 (CC-5)** — latent multi-entity Tier-2 capability now resident in code, fenced by default `("offer",)` + one pinning test + operator-reserved env lever = a **config** boundary, not structural. Watch-item; operator awareness + keep the lever reserved.
- **N-1 (CC-5)** — "budget-neutral" is **Asana-scoped only**; 4 net-new CloudWatch `put_metric_data` calls/run. Name it in the merge-word packet. (N-2 one absent-offer cache read; N-3 fetch-without-persist second residual — both bounded, pre-existing untouched files.)
- **gitleaks finding-count instability** — 49 (builder) / 46 (qa) / CH-01 1924-vs-1838 scan-count delta: baseline is a **superset** of live findings → **SAFE-direction**; cannot blind the gate (over-suppression refuted two ways).
- **NF-1 fact→UV-P** — reclassify "18 of 18" to "≥18 exempt / 0 business-scoped as of `2159f967`, drifting upward"; property stands/strengthens, **does not move severity**.
- **SH-1 DISCHARGED-CLEAN** — six charge-named F-001 anchors byte-identical on origin/main; branch closure targets a *different* (wildcard `has_scope`) bypass and is ahead of main. Thin fetch-fenced residual on ace/iris registry byte-confirmation only (additive drift, not a code-chain gap).

## §4 HARD-WALL CONFIRMATION — all intact

1. **F-4 / no-merge** — nothing merged, pushed, PR'd, deployed, or registered. CC-5 §6, CC-7 §5, RUNBOOK §7 attest; arch-adversary corroborated own-hands (empty caller diff, clean tree, branch 1-ahead/0-behind). PR #365 unarmed.
2. **Registration** — `rung-STAGED`, operator/repo-admin-reserved, ordering law (register-AFTER-land) authored; NOT executed and NOT executable this session.
3. **AL-5 window untouched** — carried UV-P everywhere; re-price is **BR-4, operator's** (§6). No clean-regime claim (R-9 trap carried verbatim in CC-5 §0).
4. **MODERATE self-cap** held on every seat; no author graded STRONG.
5. **Single-writer per path** — CC-5→`src/` (+test), CC-7→`.github/workflows/`+`.gitleaksignore`, SEC-002 cross-repo read-only. Path-disjoint; **no §6 duplication recurrence**; clocks kept on the main thread.
6. **CR-5** — zero credential reads across all three seats (post-run PAT-shape greps clean).
7. **limb-(a)** — eunomia's alone, BLOCKED until both WS-A halves **LAND**; unreachable under F-4. **CF-7 UNKNOWN** — not widened (SEC-002 explicitly did not enter it). **CF-2** UV-P untouched.

## §5 CC-8 REACHABILITY — DEFER to the handoff

Per entry-gate **Amendment B**: CC-8's limb-(iii) rides CC-7 (BUILT-DARK/STAGED — not landed) and limb-(a) is eunomia's, **unreachable under F-4**. Dispatching CC-8 this session buys only a partial attest at HELD/DARK/STAGED rungs with the load-bearing limb structurally closed — negligible surface. **Recommendation: defer CC-8 to the handoff**; the honest CC-8 is post-land, co-scheduled with H-4 once an operator merge word lifts F-4.

## §6 BR-4 ESCALATION — restated as decision-quality input (this seat rules nothing)

The AL-5 "one clean regime" prize needs an operator merge+deploy word **inside a runway that does not exist**: window opens ~2026-08-15T12:45Z (**UV-P**), today is 2026-08-14, and the path is merge + deploy + warm-cycle while F-4 is unlifted and no merge word exists. CC-5 is merge-**ready**, not merged. Surfaced to the operator: either the merge+deploy word comes early enough, or **O-7a segmentation is the expected outcome and CC-5 stands on its engineering merits, not its clock**. The runway decision, like F-4, is the operator's.

## §7 DISPOSITION

**PASS-WITH-CARRIES.** No BACK-ROUTE (no BLOCK finding, no over-claim to repair). No HALT (no wall breach, no tripwire fire; the load-bearing-premise PV counter is not incremented). BR-1 anchor-repair migration DISCHARGED in CC-5 §1; BR-2 locational repair is downstream-editorial. The three sprints hand forward to H-4 with the §3 carries name-checked and the §5/§6 escalations surfaced to the operator. Every operator fork remains reserved and untouched.
