---
type: handoff
status: accepted
artifact_id: IGNITION-exec-insight-delivery-wave-2026-08-13
initiative: exec-insight-delivery
date: 2026-08-13
revision: 2
purpose: >-
  One-shot ignition kit for a FRESH Claude Code session to dispatch the
  exec-insight-delivery wave — potnia orchestrating, pythia adjudicating forks —
  under the ratified shape and the operator's standing grant.
audited_by: >-
  potnia (orchestration readiness): READY-WITH-CORRECTIONS, D-1..D-15 — ALL FOLDED
  into this revision. pythia (fork-surface + grant): PRESERVED WITH CORRECTIONS,
  P-1..P-11 — ALL FOLDED. Five main-thread probes grounded the remaining claims
  (branch protection receipted both repos; security rite name = `security`;
  both elided paths resolve; handoffs dir trackable).
---

# IGNITION — exec-insight-delivery wave (rev 2, audited)

## §0 What this kit is

The initiative is **framed** (`.sos/wip/frames/exec-insight-delivery.md`),
**shaped** (`….shape.md` — EX-1..EX-6, three hard edges, PT-03..PT-08), and
**ratified** (`RULING-operator-exec-defaults-ratification-2026-08-13.md` — shape
adopted; Rung E ratified as drafted; G4′ operator-set; Q-2 cadence OPEN).
Live-verified this session: `ari rite current` → 10x-dev; `ari rite pantheon` →
25 rows; all **eight** seats this wave names exist. **Cross-rite sync is a
confirmed NO-OP** — arch, eunomia, hygiene, sre already co-seated.

### The spine card — routing truth; the full shape is read per-section, never whole

| sprint | author (rite) | rite-disjoint critic | NCSR | edges |
|---|---|---|---|---|
| EX-1 exec register | **MAIN THREAD** (10x-dev) — draft EXISTS, see §1 EX-1 block | `verification-auditor` (eunomia) | NR-1 | none |
| EX-2 say-able re-derivation | `architect` (10x-dev) | `audit-lead` (hygiene) | NR-2 | none |
| EX-3 TemporalFilter (ii) + discriminator (iii) | `principal-engineer` (10x-dev) | `structure-evaluator` (arch) | NR-3 | **E1 HARD → limb (ii) ONLY**; limb (iii) unedged |
| EX-4 Rung-E instrumentation | `principal-engineer` (10x-dev) | `structure-evaluator` (arch) | NR-4 | none (E5 discharged — Q-1 ratified) |
| EX-5 recurring readout | `requirements-analyst` + `principal-engineer` (10x-dev) | `verification-auditor` (eunomia) | NR-5 | **E3 HARD → EX-5's EXIT only** — the ratification narrowed it: *"EX-5's design limb proceeds; its exit (and Rung E's deadline) still needs this ruling"* (`RULING-operator-exec-defaults…` Q-2). Design limb starts Phase 1 regardless |
| EX-6 rail delivery | `principal-engineer` + `observability-engineer` (sre) | `audit-lead` (hygiene) — **sre disqualified, it co-authors** | NR-6 | **E2 HARD partial → receipt limb only**; design limb unedged |

Gates: **PT-03** potnia (width) · **PT-04** pythia (fires on EX-2's EXIT, not a
phase boundary) · **PT-05** potnia (fan-in; **the NCSR roll-call is this gate's
primary output**) · **PT-06** pythia (two OPEN slates; **fork 1 BLOCKS every
movement-class number** — see §3 item 7) · **PT-07** potnia · **PT-08** potnia
(exit — consumption receipts, never merges).

### Citation legend — the shape's elided refs, resolved (all repo-relative)

| elided | path |
|---|---|
| `RULING…` (morning) | `.ledge/decisions/RULING-operator-morning-set-2026-08-13.md` |
| ratification | `.ledge/decisions/RULING-operator-exec-defaults-ratification-2026-08-13.md` |
| `BRIEF…` | `.ledge/handoffs/BRIEF-morning-2026-08-13.md` |
| `PREDICATE…` | `.ledge/decisions/PREDICATE-sayable-set-under-refusing-verdict-axis-2026-08-12.md` |
| `RAILS…` | `.ledge/decisions/RAILS-insight-delivery-verified-2026-08-12.md` |
| `CONTRACT…` | `.ledge/decisions/CONTRACT-offers-freshness-axis-frozen-2026-08-11.md` — **canonical; never the `.sos/wip/` copy** |
| `PROBE…` | `.ledge/reviews/PROBE-story-cache-warmth-2026-08-13.md` *(path verified)* |
| `DEFECT…` / `FINDING-option-g…` | `.ledge/decisions/DEFECT-temporal-filter-imputed-false-move-2026-08-12.md` / `…FINDING-option-g-imputation-indistinguishable-2026-08-12.md` |
| `GATE-pt01…` / `GATE-pt02…` | `.ledge/reviews/GATE-pt01-spine-fan-in-2026-08-12.md` / `…-pt02-fork-surface-…` |
| `DESIGN-option4-…` | `.sos/wip/DESIGN-option4-verification-axis-annex-2026-08-12.md` *(path verified; gitignored home)* |

### Premise corrections carried, surfaced not absorbed

- Shape §C.4 records `pythia` resolving in *neither* the pantheon nor
  `.claude/agents/`. **Corrected**: it resolves at USER level
  (`~/.claude/agents/pythia.md`) and `/shape` itself dispatches
  `Task(subagent_type="pythia")`. `Task(pythia)` is live. Same class as DV-1
  (myron), opposite polarity.
- The premise-error class has fired repeatedly on this arc **today** (DV-1,
  morning §4a, two in this kit's rev 1, the pythia-resolution above). Treat any
  inherited "X does not exist / resolves nowhere" as **UNKNOWN until re-probed**.

---

## §1 THE PASTE-BLOCK — copy everything between the fences into a fresh CC session opened at `/Users/tomtenuta/Code/a8/a8/repos/autom8y-asana`

````
@.ledge/handoffs/IGNITION-exec-insight-delivery-wave-2026-08-13.md
@.ledge/decisions/RULING-operator-exec-defaults-ratification-2026-08-13.md

Dispatch the exec-insight-delivery wave. You are the MAIN THREAD: sole
dispatcher, sole owner of all git and every clock. The kit above is your
inheritance — its §3 fences bind every seat and appear verbatim in every
charge. Operator grant: agents may READ all repos on the filesystem — above,
below, or across the tree; the §3 fences survive the grant.

FIRST MOVE — run `ari rite current && ari rite pantheon`. Expect 10x-dev
active with borrows arch/eunomia/hygiene/sre and every seat in the kit §0
spine card present. If any is absent, HALT and surface — never substitute.

THE SHAPE (.sos/wip/frames/exec-insight-delivery.shape.md, ~39k tokens) is
READ-LATER, never whole — its own §8 law: "Nothing reads the whole corpus."
At PT-03 read its §3+§5+§A; when charging sprint X read §2's EX-X block and
§8's EX-X row ONLY; at each PT gate read that gate's block ONLY. The morning
ruling (kit §0 legend) is read per-R-item on citation.

SEQUENCE
0a. Offer the operator the Q-2 sitting (/interview — cadence). Q-2 is OPEN
    with NO default. It sets EX-5's EXIT and Rung E's deadline — but per the
    ratification's narrowing of E3, EX-5's DESIGN limb proceeds in Phase 1
    regardless. Do not block the wave on this offer.
0b. RE-2 (S2S security routing) FIRST, per ratified Q-9: E1 STANDS STRICT —
    security routing precedes the TemporalFilter fix, gating EX-3 limb (ii)
    and ONLY limb (ii). "Dispatched" means: YOU author
    .ledge/handoffs/HANDOFF-10x-dev-to-security-s2s-authz-2026-08-13.md via
    Skill("cross-rite-handoff") (handoff_type: assessment, carrying UV-P-C-1
    and UV-P-C-2 as the open half). That artifact's existence is PT-03 Q3.3's
    YES. Whether the security rite then executes IN-SESSION or OUT-OF-BAND is
    UNRULED and operator-only (kit §2) — record which, decide neither.
1.  Task(potnia) at PT-03 — width. Potnia is Read-only: run its 3.4 probe
    yourself and hand it the output with the kit + ratification. It rules
    width against the NARROWED E3 (kit §0 spine card), answers Q3.1-3.6.
2.  Phase 1 — dispatch per the spine card, charges built per kit §4. Agents
    author FILES ONLY; if a Write is harness-refused they return content
    verbatim and you inscribe it.
3.  Task(pythia) at PT-04 when EX-2 EXITS. Every pythia gate states its
    option slate is OPEN and grades completeness; it rules nothing
    operator-reserved.
4.  Critics per the spine card — rite-disjoint, never the author's rite.
    arch-adversary is seated nowhere. Any sprint asserting a NEGATIVE gets
    its NCSR reader per §A (kit §4 carries the reporting duty).
5.  Task(potnia) at PT-05 — Phase-1 fan-in; the NCSR roll-call is the
    primary output.
6.  Phase 2 — EX-5 (design limb if not already running) ∥ EX-6 DESIGN limb
    in parallel (no edge between them). Then Task(pythia) at PT-06 — two
    OPEN slates; fork 1 BLOCKS every movement-class number (kit §3 item 7).
    Then Task(potnia) at PT-07.
7.  Phase 3 — EX-6 RECEIPT limb (E2: needs EX-5's postable payload).
8.  Task(potnia) at PT-08 — exit. Exits are CONSUMPTION receipts, never
    merges. Then author the wave-close HANDOFF per kit §5.

EX-1 SPECIAL STATE: the one-off draft EXISTS at
.ledge/reviews/REPORT-exec-state-of-work-2026-08-13.md (status: draft). Do
NOT re-author it, and do NOT create the REGISTER-… path the shape's EX-1
block names — that exit-artifact name is superseded by this file. It has NOT
been checked against EX-1's seven exit criteria. Q-7 is ratified YES and
unconditional: Task(verification-auditor) charged with (a) EX-1 exit
criteria 1-7 verbatim from shape §2, (b) NR-1's refuters + §A.3's reporting
duty, (c) PT-05 Q5.2 — "which decision does this text choose?". Verdict
grammar STANDS / FALLS / NARROWS, and FALLS is a LIVE outcome — a register
that pre-selects one of R-16's three decisions is REVERTED, not annotated.
"Do not re-author" bars a rewrite of authorship, NOT a challenge to
substance. Output: .ledge/reviews/CRITIQUE-exec-register-2026-08-13.md.
Fold verdicts without softening a DISSENT; then hand to the operator.
Delivery is OPERATOR-PERFORMED — no agent sends anything anywhere.

LANDING: never commit from the operator's working tree (93 dirty files,
live count). Cut every worktree from origin/main, never local main (local
lags at 4129ae7e; origin/main = afdad5ed, which carries #359):
    git fetch origin
    git worktree add -b ex-{n}-{slug} .knossos/worktrees/ex-{n}-{slug} origin/main
One sprint, one PR, per-repo atomic; PRs land SERIAL — receipted branch
protection: {strict:true, enforce_admins:true} on this repo, so auto-merge
--squash fires. No monorepo PR is in scope this wave (all six sprints land
here); if one ever appears, the monorepo is {strict:true,
enforce_admins:false} — an armed MERGE auto-merge silently never fires
there. Receipt: gh api .../branches/main/protection, 2026-08-13.

CLOCKS: every clock lives on you. Subagents that self-park on background
waiters never resume — re-drive them. Seat facts: entropy-assessor has NO
Bash — route shell-heavy critique only to audit-lead (hygiene) /
structure-evaluator (arch) / verification-auditor (eunomia). qa-adversary
is NOT a critic anywhere in this wave — it is a 10x-dev native and every
author here is 10x-dev; seating it breaks rite-disjointness. potnia is
Read-only: hand it probe results, never probes.

REPORT to the operator at each PT gate and wave close: outcome first,
honest rungs (DONE / LANDING / PENDING / ASPIRATIONAL), self-attestation
capped MODERATE without rite-disjoint corroboration. Where any artifact
diverges from the two rulings in the kit legend, the RULING governs and the
divergence is SURFACED, never absorbed.
````

---

## §2 INAUGURAL `ari` COMMANDS

**None are required.** Verified NO-OP, not an omission — and the paste-block's
FIRST MOVE re-fires the verification in the fresh session rather than trusting
this one:

```bash
ari rite current      # → Active Rite: 10x-dev
ari rite pantheon     # → 25 rows; every spine-card seat present
```

Borrowed rites already co-seated: **arch · eunomia · hygiene · sre**. **Never
run `ari sync --rite=`** — a different, destructive verb (regenerates the
channel dir, auto-removes orphans; adversary precedent D2).

**One CONDITIONAL, operator-run — only if the operator elects to run RE-2's
security routing IN-SESSION.** Whether RE-2 executes in-session or out-of-band
is **UNRULED** (C-9): Q-9 ratified only the *sequencing* (E1 STANDS STRICT),
and R-11 routed the gap to the security rite without ruling where it runs.
RE-2 is a routing edge with **no owned sprint**; EX-3 has no security limb.
(Q-4 governs **RE-1**, the story-warm starvation — a different edge.) The
registered rite name is **`security`** (verified: `ari rite list`). Budget is
**97.0% (48,500/50,000)** and this borrow costs ~8,000 tokens — a **6,500-token
breach** — so it first needs `--context-budget` raised **or** a current borrow
dismissed, both operator acts:

```bash
# BOTH operator-run. Second positional is the literal component keyword
# `agents` (valid: skills | agents | omitted) — NOT a list of agent names.
ari rite invoke security agents --dry-run   # names + token cost print here
ari rite invoke security agents
# then ONE Claude Code restart
```

---

## §3 FENCES THAT SURVIVE THE GRANT (bind every seat; verbatim into every charge)

1. **CR-1** — all three Asana write classes (comments, tasks, custom fields)
   are OPERATOR-RESERVED. The S2S authorization gap means this process fence is
   the ONLY control. No agent writes to the live board, ever.
2. **CR-2** — `s3://autom8y-asr-verdicts` is operator-reserved. Not read, not
   listed.
3. **CR-5 — credentials.** No agent mints, extracts, copies or logs credential
   material. **And, binding under the read grant: on ENCOUNTERING credential
   material in any file, history object, config or output — stop reading, do
   not quote it, do not transcribe it into any artifact, SVR
   `command_output_verbatim` block, or returned message; report only the path
   and the fact.** Charter §5(b) makes anything touching security/credentials a
   hard autonomy stop regardless of reversibility. **Specific live hazard**: a
   Critical, unrotated `ASANA_PAT` is reachable in **this repo's git history**
   (`.know/defer-watch.yaml:382-403`; commits `a578ca85`, `525431de`,
   `15cffee1`, path `.claude/settings.local.json` — gitignored at HEAD, live in
   history). `git log -p`, `git show <sha>:` and cross-repo greps reach it. A
   task requiring credential material STOPS and reports what it would need.
4. **MONOREPO TRAP (this line appears VERBATIM in every sprint charge)**:
   *"`/Users/tomtenuta/Code/a8/a8/repos/autom8y` is on a divergent branch (281
   files differ from origin/main) with a sibling session actively committing.
   Always `git show origin/main:<path>` for any monorepo read."*
   **4b. THE CONVERSE, not optional**: in **this** repo (`autom8y-asana`) the
   **working tree is authoritative** — the shape, frames and several artifacts
   are gitignored or untracked, and local `main` lags `origin/main`. A seat
   that generalizes rule 4 here will report present artifacts **absent** — a
   false negative of exactly the class §A exists to catch.
5. **No client/external communication.** Delivery to the CEO/cofounder is
   OPERATOR-PERFORMED. Slack rail autonomy per the morning ruling R-7 — the
   gate-(b) modal scoped to Asana writes — covers the `#account-health` machine
   rail only, and that reading's failure mode is SILENT (UV-P-S3-1 closed by
   operator attestation; evidence class = attested, not probed).
   ⚠ **Homonym**: "D-4" names TWO things in this corpus — the morning gate-(b)
   ruling (this item) and the RAILS D-1..D-4 fallback-`text` duty (EX-6 exit 2).
   Label which at every use.
6. **G4′ (operator-set, Q-3)**: every published number enumerates its sign
   branches; PASS iff all non-neutral branches share one sign; an unenumerated
   branch is an undeclared direction. **Its ratified limit rides with it: G4′
   compels enumeration, not completeness of enumeration — the second reader
   remains the actual detector.** k-of-n rides as denominator only (D-6 — no
   further exceptions without a new ruling). P-1/P-12 naming fence + CONTRACT
   [A-2026-08-12] NON-ALIASING bind all wire fields.
7. **Defer registry**: GATE-FORK deferred (trigger: team phase begins) ·
   **D-5 (retrospective route) MOOTED by R-9, NOT ruled — never record it as
   ruled** · S5 window-fenced to 2026-08-18 (Q-6 operating basis: binds
   producer deploys; still factually unverified) · K-0b operator-only — **and
   per DF-7 it does NOT gate this wave; do not halt on it** · telos remains
   PROPOSED (Q-5) · cadence OPEN (Q-2) · UV-P-2 (team demand) open and
   unanswered — R-1 redirected the audience, it did not answer this · **Q-8 =
   NOT FIRED: no agent fires the authenticated `section-timelines` call, and
   the imputation rate is reported INFERRED (MODERATE ceiling), never measured,
   everywhere any seat speaks** · UV-P-S3-2 is UNKNOWN, not FALSE — an artifact
   saying "does not exist" fails NR-6(c) · **R-O3 delegation FLAGGED-UNRULED**
   (the R-20 pin is not the delegation) · **RE-1 (fleet story-warm starvation)
   gates no sprint here ONLY BECAUSE PT-06 fork 1 BLOCKS every movement-class
   number (DF-4). That is a constraint this wave imposes, not a fact it
   verified — the shape's one unreceipted negative. No movement-class number
   ships in this wave.**
8. **Rung E fence (ratified)**: the exec rung NEVER substitutes for rung 4;
   neither is graded in place of the other; any attestation names which ladder
   it grades or it is a Gate-B refusal. Limbs (b)/(c) are OPERATOR-ONLY.
9. **ORIENTATION, NOT STEERING (R-16, ratified; F-E3 is rung-level).** No
   artifact this wave produces — the one-off register **or** the recurring
   readout — may recommend, rank, or lead with a call to action. R-16 ruled the
   register an *orientation document, not a decision document*, deliberately
   keeping the exec's three decisions open: keep investing / trust the numbers /
   what is broken and what it costs. Q-1 ratified F-E1..F-E5 as drafted, so
   **F-E3 fails RUNG E even when the exec acts** — *"the readout's text
   pre-selected the action."* Binds EX-1 (SC-1), EX-5 and EX-6 (PT-06 Q6.3).
   **Failing it is silent in every success signal** — delivery receipt,
   generation receipt and attributed decision all read GREEN while the rung
   fails.
10. **C-8 — zero K-lane MUTATION**, as narrowed by R-4. Read-only consumption
    of a shipped response field is permitted; **mutation is not**. R-4 thawed
    reads, not writes. Nothing rides a K-lane PR.
11. **C-4 — the say-able set is ONE** (item 1a) until EX-2 re-derives it under
    G4′. No second number class ships on any other ground.
12. **C-9 — nothing the operator did not rule is recorded as decided.** Where
    this kit is silent it is silent; silence is never absorption. This fence
    binds this kit's own sentences.
13. **No infrastructure mutation.** No `terraform apply` — bare or targeted —
    no `terraform init` (itself state-touching), no mutating AWS call. AWS
    reads only. Any infra change is **plan → show the operator → operator
    applies**, `-target`ed, never whole-directory (a whole-directory apply in
    `terraform/services/asana` binds **7 alarms, not 1**).
    `observability_alarms.tf` currently carries **+198/−14 staged-not-applied**
    in the operator's tree — do not touch it. **L4 keep-warm remains REFUSED**
    unless explicitly un-refused (`PACKET-option4-interview-stack-2026-08-12.md:70`).
    ⚠ **DV-3 rider**: the falsified **68/68-as-completeness** premise is still
    live in five artifacts — no sprint inherits it by citation; its
    gate/attestation uses (non-truncation receipt) are CORRECT and must NOT be
    "fixed."

## §4 CHARGE TEMPLATE PER SPRINT (context-engineering)

Each dispatch carries ONLY: its sprint block from the shape (§2's EX-X + §8's
EX-X row) · its station's skills · **kit §3 verbatim** · **ALL of its §A.2
pre-registered refuters (2–4 per sprint — a FLOOR, not a ceiling)** · §A.3's
reporting duty: name **which** refuters were swept and **what each returned,
including the nulls** (a null is evidence; an unswept refuter is not), the
**hop taken past where the argument stopped** named concretely (file, call,
module, constant), any refuter the reader **added**, and a verdict of
**STANDS / FALLS / NARROWS** with corrected scope if it narrows · the no-git +
harness-refusal fallback · its critic's name and rite. **Nothing else** — the
5-for-5 error class was fed by seats reasoning past their station.

## §5 WHAT THE NEXT OPERATOR /handoff SEAM RECEIVES

At wave close the main thread authors `HANDOFF-exec-wave-close-{date}.md`:
per-sprint **consumption** receipts · the PT-05 NCSR roll-call (verdicts per
refuter, nulls included) · Rung-E limb (a) attestation state (limbs (b)/(c)
remain operator observations) · the surviving Q register (Q-2, Q-5, Q-8,
K-0b, GATE-FORK, R-O3, UV-P-2) · anything newly surfaced, surfaced-never-absorbed.
