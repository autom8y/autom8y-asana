---
type: handoff
status: accepted
artifact_id: NORTH-2026-08-13
date: 2026-08-13
purpose: >-
  The rebased north-facing orientation across all active arcs, synthesized from
  a four-seat verification swarm plus a Q-7 delta gate — every load-bearing
  claim re-probed own-hands, no inherited dates, no inherited discharges.
inputs: >-
  wave-verification seat (origin/main @ d45aa305 re-probed) · cross-arc clock
  sweep · operator-surface consolidation (20 open / 6 struck) · coherence check
  (6/6 COHERENT + the Q-7 gap) · Q-7 delta pass (DISCHARGED, byte-pinned) ·
  main-thread ECS measurement (the AL-5 anchor moved)
---

# NORTH — 2026-08-13

## §0 The state, verified not inherited

**The exec-insight-delivery wave is real.** All four PRs' code confirmed at
`origin/main` @ `d45aa305` by direct `git show`: the TemporalFilter
imputed-interval guard, the `imputed`/`story_count` discriminator reaching the
OpenAPI contract, `report_generated` with `content_hash`, the rail
distinguishability set. The content_hash canonicalization mismatch is
**confirmed real** (EX-5 hashes a JSON array of blocks at
`readout/generation.py:103`; EX-6 hashes a `{blocks,text}` dict at
`rail_delivery/delivery_receipt.py:62-67`) — **latent, not live** (nothing at
origin/main compares them yet), correctly carried as the sre handoff's
load-bearing entry condition REC-001.

**All six coherence checks: COHERENT.** Say-able set held at ONE
(critic-concurred rev-6); no steering in landed code; the two ladders
type-separated with RUNG-4 schema-pinned `unattested`; zero K-lane touches;
R-O3 untouched. One aliasing wrinkle inside the instrument built to catch
aliasing: the block-count comparator's reason enum is labeled
`CONTENT_HASH_MISMATCH` (already CONCERN-1 in the wave's own critique).

**Q-7 is now genuinely discharged.** The gap the coherence seat found — the
second-reader stamp resting on pre-correction text, corrections folded by the
author's own rite — was closed by a delta pass from the ratified eunomia seat:
**Q-7 DISCHARGED-ON-CORRECTED-TEXT**, bound to
`REPORT-exec-state-of-work-2026-08-13.md` @ sha256 `2d703a41…b6e9c3f` (176
lines). All four corrections FAITHFULLY APPLIED, EC-4's dissent discharged on
substance with its reviving falsifier recorded (delete `REPORT:119-121` or the
"rather than assume it is zero" clause and EC-4 reverts), orientation fence
re-swept clean including a fold-specific refuter (hedge-accumulation as covert
distrust-steer: NULL). **The register is delivery-ready pending your edit**;
§D8 of the critique gives the two-line self-check so a typo fix needs no new
pass.

## §1 The clock table, measured — 2026-08-13 → 2026-08-20

| when (UTC) | what | action |
|---|---|---|
| **Aug 13 ~11:15Z** | ✅ *happened*: the wave's merges rolled ECS `:767`/`:768` — **moving the AL-5 re-baseline anchor** (measured live: `:768` IN_PROGRESS at 11:15Z) | none — but the old date is dead |
| **Aug 14 ~10:58Z** | substrate-v2 parity **floor elapses**; ≥2-cycle conjunct already satisfied → evidence-close is a mechanical wrap | convene the wrap promptly or it becomes an EXTEND decision again |
| **Aug 14** | ⚠ `asana-mcp-postfelt-hardening` telos **verification deadline lands UNATTESTED** — arc untouched since Jul 20; drives TELOS_OVERDUE. (Its parent `asana-mcp-v1` shares the date but is REALIZED — do not conflate) | yours: attest, amend, or let it go overdue knowingly |
| **Aug 14** | defer-watch `mcp-preload-duration` trigger fires — a reduce-vs-persist ruling is meant to be **staged for you** | expect the staging; rule at leisure |
| **~Aug 15 11:15Z+** | **AL-5 re-baseline sample window opens** — ≥48h deploy-free from `:768`'s completion, **not** the old Aug-14 anchor. Any further merge to this repo pushes it again. Prediction on file: duty RISES; collapse falsifies the tuning | keep the repo deploy-quiet through the sample, or record each deploy as a regime boundary |
| **Aug 18 10:36Z** | **TRIPLE COLLISION**: substrate-v2 hard ceiling · GATE-FORK "free until" boundary · the S5/SA/SB window fence lifts. ⚠ Whether Aug 18 binds the *deferral* or only *producer deploys* is still the unverified Q-6 assumption | rule GATE-FORK (or extend) at or before; re-check Q-6 first |
| **Aug 19** | `fleet-delegation-portfolio` telos deadline lands UNATTESTED (untouched since Jul 24) | yours: same shape as Aug 14's |
| *not in window* | the 14-day Stage-2 soak and V=14,400 auto-confirm — **K-4 has not landed; that clock has not started** | none |

## §2 Your decision surface — 20 open, top of the ladder first

The full leverage-ranked register is preserved below in §5; the spine:

1. **UV-P-2** — one message: has the offers team asked for anything? Still the
   highest-leverage free act in the estate; hard-blocks GATE-FORK's honesty.
2. **K-0b** — one yes/no read: does `RULING-operator-adr007-ratification`
   already discharge "this ADR ratified"? Its literal text matches. Your word
   opens **the entire K-lane** (K-0a PASSED, K-0c LANDED — this is the last
   gate).
3. **Q-2** — pick a cadence from the 7-option proposal (B, weekly fixed-AM,
   recommended). Releases EX-5's exit and converts Rung E's placeholder
   deadline into a real one.
4. **Deliver EX-1** — the register is Q-7-discharged and byte-pinned; edit and
   hand it over personally. Rung E's limbs (b)/(c) cannot start before this.
5. **RE-2 locus** (security in-session vs out-of-band — the borrow is already
   seated) · **accept the sre handoff** (REC-001 first).
6. **cred-t21** — rotate the leaked PAT (Critical, rotation currently
   disabled, runbook ready) · then **gate-gap-1** (gitleaks rule so the class
   can't recur; CI gitleaks currently `|| true`).
7. **Two routings never adjudicated** (found by the sweep — absent from every
   ruling): a human owner for the offers schema/routes; a **content** owner for
   the enrollment explainer. And the **log-retention routing was accepted but
   its handoff never authored** — the only one of the three in that state.
8. The long tail: GATE-FORK/D-5 (trigger: team phase, colliding with Aug 18) ·
   Q-5 telos · Q-8 live call · R-O3 tension · `.sos/wip` corpus promotion ·
   housekeeping (52 worktrees, 111 dirty paths, 3 stashes, PR #199, station-B
   items, the unexplained `10000.raw` at repo root — unopened, path+fact only).

## §3 Strike list — carried as open somewhere, actually DISCHARGED

| stale record | truth | fix needed |
|---|---|---|
| `DEFECT-temporal-filter…` says `routes_to: OPERATOR`, `status: draft` | **FIXED** by PR #360 (`49cf12ca`), two-sided teeth | discharge banner on the file |
| morning ruling §4b: "the canonical contract is still untracked" | **LANDED** by PR #359; working-tree copy diffs zero against origin/main | none — §4b is historical |
| `CARDS…` §PROPOSED still reads "neither is decided" | **FU-5 OPENED and FU-6 PROMOTED** by your morning ruling R-11..13 | update the CARDS file so nobody re-litigates |
| morning §NOT-COVERED 5 (rung wording) and 8 (G4′ standing) | discharged by the exec ratification Q-1/Q-3 | none — chain is clean |
| gate-(b) §OPEN "undecided in both directions" | closed by R-7 + R-14 | none |
| `GATE-pt05…:75-84` + wave-close `:69` attribute "NARROWS folded → STANDS" to the eunomia critic | that was author-rite self-attestation **until today's delta pass** | those rows should cite `CRITIQUE-exec-register…` **§ DELTA PASS 2** |

## §4 The landing question — ~90 wave artifacts exist only in a dirty tree

The wave's `.ledge/` corpus (register + critiques incl. today's delta pass,
gate records PT-04..08, cadence proposal, template spec, capture protocol,
three handoffs, this NORTH, the strategic rebase brief) is **uncommitted**, in
a tree now at **111 dirty paths**, on a local `main` 11 commits behind. The
sibling session's offer stands and either session can execute it: **selective
landing via a clean worktree cut from `origin/main`** — one docs PR, plus the
three §3 stale-record corrections in the same pass. Recommended before
anything else rots; it is the only item on this page with a decay rate.

## §5 Full consolidated register (swarm output, preserved verbatim in substance)

*Ranked by leverage; every item remains yours to take or leave. Items 1-6 are
§2's spine. The remainder:* GATE-FORK (D-2, free-until-Aug-18 caveat Q-6) ·
D-5 (rides GATE-FORK; the COLD probe now informs it) · Q-5 telos ratification
(status quo is PROPOSED by ruling) · Q-8 operator-personal live call (upgrades
imputation from inferred to measured; supplies Rung E's first live occurrence)
· OS-6/OS-7/CR-2 (latent authorizations, nothing pending) · R-O3 (delegation
vs R-20 pin, flagged-unruled, fires at the K-1 producer PR) · `.sos/wip`
corpus promotion (R-3 covered one file only) · station-B divergent copies
(blocked on PR #199, confirmed still open) · station-B stash adjudication
(3 stashes now; re-confirm identity before acting) · housekeeping bundle (52
worktrees · 111 dirty · monorepo checkout divergent-by-281-files with a
sibling session committing — reads stay `git show origin/main:` only).

## §6 The through-line, one paragraph

Every arc is now running the same law from different directions: **a claim is
worth what its verification chain is worth.** The offers gate refuses rather
than guesses (P-3, honestly aborting 6×/day). The K-lane will let it vouch
again, and one word from you (K-0b) opens that build. The exec register
carries per-number epistemics to leadership, and its own governance was held
to the same bar today — its second-reader stamp was found resting on stale
text and re-earned on the corrected bytes before it reaches a CEO. The wave's
instruments (imputed-vs-observed on the wire, generation⋈delivery receipts,
two type-separated ladders) exist so the *next* claim is cheaper to verify
than this one was. The swarm that produced this page found real drift in
every direction it looked — a moved clock, two dormant deadlines, six stale
"open" items, one self-certification loop — **and every finding was a second
reader going one hop past where a record stopped.** That is the method; it is
no longer a note in a retro, it is how this estate runs.
