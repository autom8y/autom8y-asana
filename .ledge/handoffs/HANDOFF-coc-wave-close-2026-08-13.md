---
type: handoff
status: accepted
artifact_id: HANDOFF-coc-wave-close-2026-08-13
wave: chain-of-custody-closure
seam: PT-07 wave-exit → operator
date: 2026-08-13
origin/main: d7560153
landing: EVERYTHING AUTHORED-UNMERGED (Q-4 HALT). Nothing merged, nothing deployed, AL-5 window untouched.
governs: NOTHING — records the wave's honest exit state and the operator's decision surface. No fork ruled.
---

# HANDOFF — chain-of-custody-closure, wave close to operator

The wave ran Phase 1 five-wide, verified every sprint with a rite-disjoint
second reader, and halted at its operator-decision boundary. Everything below
rests authored-unmerged in the working tree (Q-4 HALT); the one code sprint
(CC-1) sits built in an isolated worktree branch, PR **not** opened. Nothing is
counted done that is not done.

## §1 Rung roll-call — honest exit rung per sprint

| sprint | what | exit rung (honest) | on GitHub? |
|---|---|---|---|
| **CC-1** | swap-detector closure (predicate i) | **rung-BUILT-DARK** — mechanism closure, two-sided teeth re-derived own-hands (critic STRONG on re-run legs); NOT a live-emitter closure; 2 named narrowings | **no** — worktree branch `coc-cc1-reconverge`, PR ready to open UN-ARMED on your word (→ rung-PR-UP-MERGE-HELD once CI observed) |
| **CC-2** | RE-2 two-layer authz design | **rung-DESIGN**, owner-named, ratification-pending; `rung-ENFORCED` refused (structural, not scheduling) | no — paper, authored-unmerged |
| **CC-3** | RE-2 blast radius | **UV-P-dispositioned** (both UV-Ps CLOSED-WITH-RECEIPT, adverse); infra legs STRONG | no — paper |
| **CC-4** | RE-1 warm-path slate | **rung-DESIGN slate**, binds NOTHING to F-1 | no — paper |
| **CC-6** | gitleaks enforcement recon | **UV-P-dispositioned** + fix-locus finding; (c) NARROWED | no — paper |
| **CC-5** | RE-1 repair | **SHUT** — F-1 halt; not dispatched | — |
| **CC-7** | gitleaks biting gate | **NOT STARTED** — gated on CC-6/E1 + your F-7/F-8 + CC-7-boundary decision | — |
| **CC-8** | attest limbs (i)+(iii) | **NOT REACHED** — needs CC-7 (limb iii); limb-(a) attestation stays eunomia's and BLOCKED until both WS-A halves LAND | — |

PT-03 fan-in verdict: **PASS-WITH-CARRIES** (`GATE-coc-pt03-2026-08-13.md`).

## §2 NCSR ledger — five negatives, five STANDS, zero FALLS

NR-1 STANDS (2 narrowings) · NR-2 STANDS (N-1 narrows) · NR-3 STANDS (severity framing Critical→High) · NR-4 STANDS (author null discharged beyond claim) · NR-5 STANDS (fix-locus (c) narrows). Rite-disjointness 5/5; no critic graded its author STRONG; the wave's single STRONG (NR-1) is leg-scoped to own-hands re-derivation. Full roll-call: `GATE-coc-pt03-2026-08-13.md` §1.

**Two MATERIAL downstream corrections (Phase 2 must consume the corrected artifacts):**
- **MC-1** — gitleaks locus (c) is a **two-action** fix (local job **+** branch-protection contexts registration); shipped as one action it is a silent non-biting gate — the exact class this wave closes. CC-7 must ingest `CRITIQUE-cc6-gitleaks-recon-2026-08-13.md:99-149`.
- **MC-2** — RE-2 F-001 is **High, business-scoped** absent an exempt-SA grant (the CWE-862 defect STANDS unqualified; only the Critical framing narrows).

## §3 The three operator decisions (priced, corrected, UNRULED)

Full adjudication: `.ledge/reviews/GATE-coc-pt02-pt04-forks-2026-08-13.md`.

### F-1 — RE-1 ownership + scope · HALT, no default
8-option slate, two tiers priced separately: **Tier 1 offers-only** (~4,192 tasks, ~6-8 min, one Lambda) vs **Tier 2 full starved set** (~48,662 tasks, twelve entity types — the fleet redesign the fences forbid building here). O-E (routed-out, named owner) is genuinely first-class, zero cost, zero AL-5 disturbance. **CC-5 opens only when you rule ownership AND scope.** RE-1 also feeds GATE-FORK's option-(g) critical path (warm→measure→decide). Decaying input: **AL-5 opens ~2026-08-15T12:45Z (~2 days)** — a warming fix landed before it buys one clean regime; after, O-7a segmentation is mandatory. Zero-cost options (O-E/O-H) have no expiry.

### F-7 / F-8 — gitleaks · defaults hold, one narrowing
**F-7 = enforce-with-baseline, now evidence-backed** (an enforcing run trips on the historical leak; rotation alone won't green it — "rotate + **baseline**-then-enforce", the baseline is what unblocks CI, and it does NOT wait on F-2). Baseline must be fingerprint-keyed and cover every historical finding that trips, not only cred-t21's three commits. **F-8 = in-repo viable, no halt, but (c) NARROWED** to two actions (job + registration) and it collides with CC-7's single-PR boundary. Your call on CC-7: (1) proceed on defaults with an amended two-action boundary, (2) rule the locus first, or (3) go upstream (a) (org-wide blast radius unenumerated).

### RE-2 severity determinant — one cheap cross-repo yes/no
**Are `ace`/`iris` provisioned as exempt-SAs (`bypass_scope_enforcement=True`)?** NO → stays High (real defect, business-scoped). YES → re-escalates toward Critical. Resolvable by the co-seated security rite (SEC-002) or by you directly if known. The SA registry was already read own-hands, so if the grant is registry-declared it's cheap; if issuance-time it needs SEC-002's trace. Adjacent widening: `iris` carries no asana scope in the SA registry but the same logical identity gets `asana:read` via OAuth-client terraform — a second uncounted population.

## §4 Defer registry delta

**Fleet pegs authored** at `/Users/tomtenuta/Code/a8/a8/repos/.know/defer-watch-manifest.md`: DW-COC-01 (RE-1 fleet starvation), DW-COC-02 (RE-2 authz gap, VERIFIED High + escalation determinant), DW-COC-03 (gitleaks `|| true` fleet gate-gap), DW-COC-04 (bare ASANA_PAT live-env exposure, extends cred-t21).

**Load-bearing carries (move a fork or a rung):** CF-1 exempt-SA determinant · CF-2 CC-1 hashless-emitter census (UV-P not fact) · CF-3 offer-GID-sharing null · CF-4 CR-5 regex residual · CF-5 baseline-scope extension · CF-6 UV-P-3 token-species reclassification (structural half CONFIRMED) · CF-7 `/api/v1/tasks|projects/*` JWT-excluded, publicly enumerated incl. DELETE, posture UNKNOWN (mutation surface wider than the five write classes) · CF-8 AL-5 clock. Bounded carries CF-9..CF-22 + CF-17 (NEW live finding: prod Lambda emits CloudWatch metrics dimensioned `environment=staging` while `AUTOM8Y_ENV=production` — hygiene/sre referral, out of scope).

## §5 The operator's next-word menu

| word | effect |
|---|---|
| **rule F-1** (owner + tier) | opens CC-5 (the RE-1 build) at the ruled scope |
| **rule CC-7 opening** (defaults+amended boundary / locus-first / upstream) | unblocks CC-7 → then CC-8 attest |
| **resolve the RE-2 determinant** (or route to co-seated security) | fixes severity at High or Critical |
| **lift F-4** (fence lift) | permits merges — but re-price against the AL-5 window first (any merge+deploy moves it) |
| **"open the held PRs"** | I push CC-1's branch + open the UN-ARMED PR (→ PR-UP-MERGE-HELD); never `--auto` (enforce_admins fires) |
| **word to eunomia** | limb-(a) Phase-4 attestation — BLOCKED until both WS-A halves LAND (not merely build) |

**Nothing operator-reserved was ruled by any seat.** F-1, F-2, F-4, F-7, F-8, the RE-2 remediation/severity, GATE-FORK, and CC-7 opening are all yours. No merge, no deploy, no credential read, no cross-repo probe, no git verb beyond the isolated worktree.

## §6 Orchestration note — the duplication defect, owned

Under a burst of transient API errors, the harness retried background subagents, spawning duplicate writers on shared paths (CC-1 twice into one worktree → poisoned; CC-2 twice; CC-4 failed pre-write). Contained: poisoned worktree quarantined (RED asset preserved), CC-1 re-run clean single-writer with an anti-collision guard, CC-2 authoritative version confirmed on disk, CC-4/CC-3-critic re-run. Lesson recorded: code/shared-path sprints under API instability need single-writer guarantees; paper sprints are last-writer-wins-tolerant. No artifact shipped from a poisoned tree — both poisoned CC-1 agents halted honestly rather than claim a rung.
