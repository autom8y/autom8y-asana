---
type: review
status: proposed
---

# QA-ADVERSARY — S6 Truthful Observability (PR #282) — 2026-07-29

**Scope**: branch `feat/substrate-v2-s6-observability` (worktree `autom8y-asana-wt-w2-s6`, HEAD `d574f0c6`).
Diff vs origin/main = 3 files: `src/autom8_asana/substrate/observe.py` (+481), `terraform/services/asana/substrate_v2_provability_alarms.tf` (+250, authored-not-applied), `tests/unit/substrate/test_observe.py` (+586). `prod_touch: NONE` maintained — all probes offline (fake stores/clients; `terraform validate -backend=false`; zero AWS API calls).

**Design bar**: TDD-substrate-v2 §4 Seam-5 ([H19]-[H23], C7) + §3 RC-F; RC-acceptance-predicates RC-F-1..4; ADVERSARY-substrate-v2-design-s1 AV-6/[H20] (§3 :307-327).

**Charter for this review**: RC-F is "observability that CANNOT read green while broken." The adversary's job was to construct a broken state that reads green. **One was constructed — at the seam BETWEEN the two authored artifacts.**

---

## VERDICT: NO-GO (one CRITICAL, bounded fix; re-review is DELTA-scope on F-1 + binding test)

The in-process evaluator is genuinely well-built — two-sided teeth throughout, [H19]/[H20]/[H21]/C7 all mechanically real and probe-verified. The CRITICAL is not in the Python: it is that the terraform alarms and the emitter disagree on CloudWatch metric identity, so the four alarms — as authored — bind to metric series that will never receive a datapoint. The suite authored to cure the DMS-24h dead-metric class reproduces the DMS-24h dead-metric class.

---

## Findings

### F-1 — CRITICAL — All four PROV alarms query a `{environment}` dimension the emitter never emits (alarm-on-dead-metric; DMS-24h class reborn) [STRUCTURAL | STRONG — executed probe]

- **tf side**: every alarm declares `dimensions = { environment = var.environment }` — `substrate_v2_provability_alarms.tf:116` (PROV-1), `:145` (PROV-2), `:173` (PROV-3), `:202` (PROV-4).
- **emitter side**: all run-level metrics are emitted with NO `Dimensions` key at all — `observe.py:457-463` (`_run_metric` builds `MetricName/Value/Unit/Timestamp` only).
- CloudWatch metric identity = (namespace, name, EXACT dimension set). An alarm on `UnprovableCount{environment=…}` never sees dimensionless `UnprovableCount`. **PROBE-A** (executed, receipt below) mechanically diffed the four tf identities against `CloudWatchProvabilityEmitter.build_metric_data()` output: all four → `NO EMITTED SERIES MATCHES`.
- **Consequence as authored, once applied**:
  - PROV-1/3/4 (`treat_missing_data = "notBreaching"`) read **OK forever** — a STALE/CORRUPT/MISSING artifact, an incomplete sweep, and a C7 set-drift are ALL silent. **RC-F-1 FALSIFIED** at the authored-artifact level.
  - PROV-2 (`treat_missing_data = "breaching"`) goes **ALARM permanently** even while the evaluator heartbeats healthily — **RC-F-2 FALSIFIED** (fires on healthy = operator-training noise), and a genuine evaluator-death is masked (already in ALARM). **RC-F-4 FALSIFIED**: the dead-man does not watch a proven-live metric — it watches a series that never exists.
- **Aggravator (coverage theater signature)**: the two artifacts are each internally consistent and test-locked AGAINST each other. The test helper asserts run-level metrics are dimensionless (`test_observe.py:191-196` — matches datum where `"Dimensions" not in datum`), and no test string-diffs emitted identity vs the tf (grep `environment` in `test_observe.py` → 0 hits). 22 green tests + `terraform validate` PASS (executed, Terraform v1.11.4: "Success! The configuration is valid.") while the composition is dead — validate checks syntax/references, not metric-identity semantics.
- **Precedent in the SAME module**: `observability_alarms.tf:224-227` documents this exact class as a proven gap ("N1 §B-2: the {environment=production} dimension does NOT exist today… this alarm evaluates INSUFFICIENT_DATA until AI-5 (add `environment` to the BridgeFleetHealth emit)"). The house-convention match is emitted-dims == alarm-dims (AL-5: `project_gid` on both sides, `observability_alarms.tf:344-346`/`:357-359`; `cloudwatch_emit.py:174-177` emits `metric_name`/`project_gid` — never `environment`).
- **Fix (bounded, co-authored on BOTH sides + a binding test)**: either (a) drop `dimensions` from the four alarms (namespace is deployment-scoped), or (b) add the `environment` dimension to the emitter's run-level metrics — then add a test that reads the `.tf` and asserts (namespace, metric_name, dimension-key-set) per alarm ∈ emitted identities, so a future rename/re-dimension on either side goes RED. The tf header already knows the stakes ("a rename on either side silently un-wires these alarms", `substrate_v2_provability_alarms.tf:56-59`) — the discipline exists for the NAME but was not applied to the DIMENSIONS, and it lives in a comment, not a test.

### F-2 — MEDIUM — Expected-set fetch failure kills the whole run pre-emission: no heartbeat, no completeness, no metrics at all [TACTICAL | STRONG — executed probe]

- `observe.py:248-250`: `registry_targets()` / `store_enumeration()` are awaited OUTSIDE any try — an exception propagates out of `evaluate_all` before `emit_run`. **PROBE-B** (executed): both `registry backend 500` and `S3 ListObjectsV2 AccessDenied` → `evaluate_all` RAISED, emitter received 0 runs.
- The module docstring over-claims: "never crashes the sweep (the heartbeat must still emit)" (`observe.py:244-246`) is true only for PER-ARTIFACT failures.
- Post-F-1-fix the backstop is PROV-2 — but at 3600s dead-man granularity vs 900s cadence: a persistent enumeration failure is caught at ~1h (spec-acceptable per Seam-5 "the evaluator's own silence fires"), while an INTERMITTENT one (every other run failing) never breaches PROV-2 (Sum≥1 per window) and reads fully green on the successful runs.
- **Fix**: catch expected-set fetch failure, still emit heartbeat + `Completeness=0` (or a dedicated `EvaluationFailed=1`) so PROV-3 fires on the next datapoint at 15-min granularity, and the docstring becomes true.

### F-3 — MEDIUM — Empty expected-set reads vacuously green on all four alarms; nothing binds `ExpectedCount` [TACTICAL | STRONG — executed probe]

- `observe.py:139-140`: `completeness` explicitly returns `100.0` when `expected_count == 0`. **PROBE-C** (executed): registry=∅ ∧ store-enumeration=∅ → `UnprovableCount=0`, `Heartbeat=1`, `Completeness=100.0`, `ExpectedSetMismatchCount=0` — an evaluator watching NOTHING reads GREEN on all four alarms. `ExpectedCount=0` IS emitted (`observe.py:481`) but no alarm consumes it.
- Construction: a common-mode `ExpectedSetSource` misconfiguration (wrong prefix/env; an implementation that swallows its own errors into empty sets) — C7's two-sidedness only discriminates when at least one side is non-empty. Production always expects ≥1 artifact, so a floor is cheap and two-sided-safe.
- **Fix**: PROV-5 `ExpectedCount < 1` floor alarm (same cadence/period), or treat an empty union as a failed run per F-2's path. Note: the store-truncation attack degrades LOUD, not green (a paginated/partial store listing drops members from the store side → registered ones become `registry_only` → PROV-4 + PROV-1 fire), so C7's failure direction is correct there; the empty-set corner is the one that rots.

### F-4 — LOW — `CloudWatchProvabilityEmitter` does not satisfy its own `ProvabilityEmitter` port under mypy-strict [TACTICAL | STRONG — executed probe]

- Port: `emit_run(...) -> None` (`observe.py:199`); concrete: `emit_run(...) -> dict[str, Any]` (`observe.py:406`). **PROBE-F** (executed): `emitter: ProvabilityEmitter = CloudWatchProvabilityEmitter()` → mypy-strict `error: Incompatible types in assignment … Expected: def emit_run(self, run: EvaluationRun) -> None / Got: … -> dict[str, Any]`.
- The 22 tests dodge it: `_build_evaluator`'s `emitter` param is `Any` (`test_observe.py:180`) and the direct construction sites carry `# type: ignore[arg-type]`. "mypy-strict green" is true of the module file yet the production wiring line is a type error waiting for S8. `ProvabilityEmitter` is S6-authored (not SEAM-0-frozen — origin/main `observe.py` had only `ProvabilityEvaluator` + `EvaluationRun`), so the port is free to fix: return the diagnostic from a separate method, or declare the port `-> object`.

### F-5 — LOW (CARRY → S2/Seam-1) — Future-dated `built_from_live_at` reads PROVABLE forever under the frozen predicate [STRUCTURAL | MODERATE — executed probe against the frozen contract transcription]

- **PROBE-D** (executed, faithful transcription of the frozen contract at `freshness.py:46-54` — the real `is_provable` is still the SEAM-0 `NotImplementedError` stub, S2-owned): proof stamped 24h in the FUTURE → `PROVABLE`, `age_seconds=-86400.0`, `MaxStalenessAgeSeconds` emitted **negative**, nothing fires. A writer clock-skew or bad-stamp bug freezes green (the re-stamp class the S1 review worried about; `content_digest` guards the frame bytes, not the timestamp).
- S6 is faithful to the frozen contract, so this is not an S6 defect — it is a Seam-1 guard note for S2 (refuse/clamp negative age, or CORRUPT on future stamp) + optionally an S6 diagnostic (emit min-age). Route to potnia for the S2 charter.

### F-6 — INFO — [H23] "reuses cloudwatch_emit" is shape-reuse, not code-reuse

- `observe.py:370-380` duplicates `_get_cloudwatch_client` (byte-equivalent region logic to `cloudwatch_emit.py:109-117`). Docstring honestly says "Mirrors". Accepted; noted as DRY drift risk (region-resolution divergence would be silent).

---

## Executed probe receipts (all offline; env = primary-checkout `.venv`, Python 3.12.13)

| Probe | Construction | Result |
|---|---|---|
| Suite re-run | `pytest tests/unit/substrate/test_observe.py` in worktree | **22 passed** (claim verified) |
| mypy module | `mypy --strict src/autom8_asana/substrate/observe.py` | clean (claim verified) |
| PROBE-A | mechanical string-diff: tf (metric, dim-keys) × 4 alarms vs `build_metric_data()` identity | **all 4 alarms: NO EMITTED SERIES MATCHES** → F-1 |
| PROBE-B | `registry_targets`/`store_enumeration` raise | `evaluate_all` RAISED; 0 runs emitted; no heartbeat → F-2 |
| PROBE-C | registry=∅ ∧ store=∅ | all four alarm metrics green; `ExpectedCount=0` unbound → F-3 |
| PROBE-D | `built_from_live_at = NOW+24h` | PROVABLE; `MaxStalenessAgeSeconds=-86400` → F-5 |
| PROBE-E | gid `"123"` (registry) vs `"123 "` (store) | mismatch=2, unprovable=1 → **LOUD** (PROV-4+PROV-1) — key-variant drift fails toward firing ✓ |
| PROBE-F | `emitter: ProvabilityEmitter = CloudWatchProvabilityEmitter()` under mypy-strict | **assignment error** → F-4 |
| PROBE-G | `terraform init -backend=false && terraform validate` (v1.11.4) | **"Success! The configuration is valid."** — and therefore validate provably CANNOT catch F-1 (semantic identity, not syntax) |

## Verified-good (adversarially probed, held)

- **Two-sided teeth in-process**: fires on STALE/CORRUPT/MISSING, silent on provable (test suite + probes) — RC-F-1/RC-F-2 hold AT THE METRIC-VALUE level; they break only at the tf binding (F-1).
- **[H19] injection real**: `always_corrupt` substitution test proves verdicts come from the injected predicate; CORRUPT requires digest re-derivation from bytes (echo would be blind) — both test-locked.
- **[H21]**: `ArtifactMissing` → determinate `provable=0` verdict, counted complete, never a skip (`observe.py:261-274`).
- **[H20]**: per-artifact indeterminate read/check → `unevaluated`, completeness < 100 while heartbeat still lands; completeness ≠ heartbeat independence test-locked.
- **C7 both directions loud**, including fresh-but-unregistered (AV-6 cure) firing PROV-4 alone — the exact "counts match, all provable, still not green" construction.
- **Heartbeat at END of sweep** (`observe.py:323`): a hung evaluator emits nothing → PROV-2 catches (post-F-1 fix); heartbeat-at-start would read alive while hung — the right choice.
- **Emitter best-effort**: `put_metric_data` failure swallowed + logged; total emission failure → heartbeat metric absent → PROV-2 path (post-F-1 fix); run-level metrics ride the first chunk (8 items < 1000 cap).
- **Kill-switch**: `arm_paging=false` + `""` SNS defaults → all PROV actions resolve `[]` (`observability_alarms.tf:110-113`, prov locals `substrate_v2_provability_alarms.tf:94-99`) — **disarmed at apply**, documented as DP-4a confirm-first in the file header (:36-41). C10 end-to-end fired-alarm evidence explicitly deferred to S8 per potnia ruling (:43-47) — consistent with the TDD.
- **Conformance**: frozen `ProvabilityEvaluator` Protocol signature verbatim vs origin/main (docstring-only addendum naming the S6 realization); `EvaluationRun` name preserved, fields S6-filled per sole-consumer authority; package `__init__.py` untouched, **exports = 24**; diff touches no sibling/v1 file; mypy-checked structural conformance test present (`test_observe.py:204-213`).

## Release recommendation

**NO-GO** on F-1 alone (CRITICAL: as authored, applying the suite can never satisfy RC-F-1/2/4 — green-while-broken realized at the artifact seam). Fix is small and mechanical: reconcile dimensions on one side + add the tf↔emitter identity-binding test; then this lands as GO with conditions:
- **Before S8 arm/apply**: F-2 (fail-partial emission on expected-set failure) and F-3 (ExpectedCount floor / PROV-5) — or explicit potnia acceptance ticketed.
- **Before production wiring**: F-4 (port return type).
- **Route to S2 charter**: F-5 (future-stamp guard in `is_provable`).

**Cross-rite handoff assessment**: docs impact NONE (no user-facing behavior; DARK build). Security handoff NOT required (no auth/payments/PII/session surface). SRE handoff REQUIRED at S8 (alarm apply + arm + C10 end-to-end fired-alarm evidence + DMS-24h retirement precondition per `substrate_v2_provability_alarms.tf:215-231`) — not at S6.

**Acid test**: before this review, an unprovable artifact under the applied suite would have failed in production in a way nobody tested — the alarms would have said OK. That is no longer a surprise.

— qa-adversary (10x-dev, borrowed seat), evidence self-attestation capped MODERATE where not backed by executed receipts; all F-1..F-5 findings carry executed-probe receipts (STRONG within this review's scope).

---

# ITERATION-2 (DELTA re-review, round 2 of 2) — commit `9e784982` — 2026-07-29

**Delta scope**: `d574f0c6..9e784982`, same 3 files (+456/-64). Touch-set vs merge-base `4400ec7d` still exactly 3 files (origin/main advanced with S1 #278 + S2 #281 — no overlap with S6's files). All probes offline; `prod_touch: NONE` maintained.

## VERDICT: GO

All four findings CLOSED with executed receipts; the S2-GO addendum is real and two-sided; no blocking new finding. One new LOW (latent, non-blocking) filed, one residual documented, one S8 wiring obligation named.

## Per-fix verification

### (a) F-1 CLOSED — alarm↔emitter identity reconciled + binding test with two-sided teeth

- Emitter now stamps `{environment}` on every run-level datum (`observe.py` `_run_metric` adds `Dimensions: [{Name: DIMENSION_ENVIRONMENT, …}]`) and `{environment, project_gid, entity_type}` on the gauge. **RE-PROBE-A (executed, independent round-1 parser)**: all SIX alarms (PROV-1..6) → `MATCH` against emitted identities; emitted environment VALUE = `production` = tf `var.environment` default (`observability_alarms.tf:38-42`).
- Binding test `test_terraform_alarms_bind_to_emitted_metric_identities` (`test_observe.py:656-706`) parses the .tf textually and asserts namespace default == constant, env-value default == `DEFAULT_ENVIRONMENT`, and per-alarm exact dimension-key-set equality. **Negative controls (executed, all RED)**:
  - NEG-1 emitter-side: run-level metrics stripped of Dimensions in-memory → RED (`alarm queries dims {'environment'} but emitter emits set()`).
  - NEG-2 tf-side: dim key renamed `environment`→`env` in a doctored copy → RED.
  - NEG-3 tf-side: metric renamed `UnprovableCount`→`UnProvableCount` → RED (`NOT emitted (dead series)`).
- **fmt-fragility probed**: `terraform fmt -check -diff` exit 0 — the file is fmt-canonical, so a fmt run today is a no-op; fmt-triggered parser breakage empirically ruled out. See F-7 below for the latent multi-line case.

### (b) F-2 CLOSED — no silent pre-emission crash path remains (one designed residual)

- Expected-set fetch failure now caught (`observe.py` `evaluate_all` try/except around both enumerations): **RE-PROBE-B (executed)** — `evaluate_all` RETURNS a failed run (`evaluation_failed=True`), emitter received 1 run, `Completeness=0.0` (PROV-3 fires next datapoint, 15-min granularity), `ExpectedCount=0` (PROV-5 fires), `EvaluationFailed=1` emitted (diagnostic), heartbeat still lands. Test-locked at `test_observe.py:724-744`.
- Per-artifact read/check failures: unchanged skip-loud path ([H20]) — re-verified in suite.
- Emitter mid-batch partial failure: per-batch try/except continues to subsequent chunks; run-level metrics (incl. heartbeat) ride batch 1 (11 items < 1000 cap).
- **RESIDUAL R-1 (LOW, by-design, documented)**: a `ProvabilityEmitter` implementation that RAISES propagates out of `evaluate_all` (**executed probe**: `RaisingEmitter` → propagates). For the CW emitter the only raise-capable seam is lazy boto3 client construction (import/region) — `put_metric_data` errors are swallowed. Backstop: PROV-2 dead-man (≤1h), whose series-match is now proven. Non-blocking; consistent with Seam-5 "the evaluator's own silence fires."

### (c) F-3 CLOSED — PROV-5 floor; absence semantics coherent across the suite

- PROV-5 (`ExpectedCount`, Minimum `< 1`, `substrate_v2_provability_alarms.tf`) — **executed**: empty registry∧store → `ExpectedCount=0` → fires; failed fetch → `ExpectedCount=0` → also fires. Test-locked (`test_observe.py:752-762`).
- `treat_missing_data=notBreaching` on PROV-5/PROV-6 cannot read OK-while-broken during TOTAL absence because absence is PROV-2's designated job (`breaching`) and PROV-2's metric identity now provably matches the emitted heartbeat series (RE-PROBE-A row 2). No alarm-state assignment leaves a green gap: data-present-and-broken → PROV-1/3/4/5/6; data-absent → PROV-2 ALARM.

### (d) F-4 CLOSED — honest port conformance, no dodges

- `emit_run(...) -> None` (diagnostic payload moved wholly to pure `build_metric_data`). mypy-strict green on BOTH `observe.py` and `test_observe.py` (executed). `test_cloudwatch_emitter_conforms_to_the_emitter_port` (`test_observe.py:769-781`) makes the port assignment type-checked. Zero `type: ignore` remain in the test file (grep receipt: only a comment stating their removal); the fakes now structurally satisfy the Protocols.

### (e) Addendum VERIFIED — future-dated disclosure, two-sided

- **RED side (executed + test-locked `test_observe.py:799-816`)**: future-dated proof → `future_dated_count=1` → `FutureDatedProofCount=1.0` → PROV-6 fires; `MaxStalenessAgeSeconds` clamped to `0.0` (never `-86400`).
- **GREEN side (executed + test-locked `:819-828`)**: normal fresh proof → count `0`, PROV-6 silent.
- **Clamp-masking probe (executed, my own construction)**: mixed sweep {future-dated, 7200s-stale, fresh} → `max_staleness=7200.0` (clamp does NOT mask genuine staleness), `future_dated_count=1`, `unprovable=1` — PROV-1 and PROV-6 co-fire correctly.
- PROV-6's tf description asserts the real predicate is fooled by negative age — **verified TRUE against origin/main's landed S2 body** (`freshness.py:102-110` on main: `age_seconds > sla` → STALE else PROVABLE; the code even carries the `F8 -> S5 carry` comment naming Seam-5 observability as the disclosure locus). Cross-sprint coherent; F-5 (iteration 1) is discharged at S6-disclosure altitude with the S5 serving-side obligation riding the handoff per the tf note. Nuance verified harmless: main's real predicate checks age-before-digest (a stale-AND-corrupt artifact reads STALE where the test's transcription reads CORRUPT) — both non-PROVABLE, no alarm-semantics difference.

### (f) Full gate re-run + surviving probes

- **28/28 tests green** (22 prior + 6 new: binding, fetch-failure, empty-floor, port-conformance, future RED, future GREEN); mypy-strict clean (module + tests); `terraform validate` "Success"; `terraform fmt -check` exit 0; exports **24**; frozen `ProvabilityEvaluator` Protocol **verbatim vs merge-base** (executed structural diff → `PROTOCOL-SIGNATURE-VERBATIM`); kill-switch unchanged (PROV-5/6 wired into the same `paging_armed_alarms` locals pattern; defaults disarmed; DP-4a header now reads PROV-1..6; outputs updated to 6 names).
- Surviving round-1 probes re-executed: whitespace-variant key drift still LOUD (mismatch=2); C7 both directions, [H20]/[H21], heartbeat-at-end — all hold.

## New finding (round 2)

### F-7 — LOW (latent, non-blocking) — binding-test tf parser under-reads MULTI-LINE `dimensions` blocks

- `_parse_tf_alarm_identities` (`test_observe.py:628-644`) splits the dimensions body on `","` — HCL multi-line maps are newline-separated, so only the first key is extracted. **Executed proof (FMT-FRAGILITY-1)**: doctored PROV-1 with multi-line `dimensions { environment, workflow_id }` truly queries 2 keys; parser extracted `['environment']`; the binding test **PASSES** on that doctored tf → latent false-green for a future multi-key alarm.
- Bounded today: all six current alarms are single-line single-key, and `terraform fmt` will not reflow them (fmt-check exit 0 — empirically a no-op). But the trap is live for the next author: the SIBLING file's house style IS multi-line dims (`observability_alarms.tf:147-149`, `:244-246`).
- **Fix (one line, next touch — not blocking)**: extract keys with `re.findall(r"(\w+)\s*=", dims_body)` (newline-tolerant) instead of comma-split.

## Carry to S8 (named, not an S6 defect)

- **Environment VALUE wiring**: the binding test pins default-to-default (`"production"` == `"production"`). At apply/wiring time, an operator setting `var.environment="staging"` while the Lambda constructs `CloudWatchProvabilityEmitter()` with the default diverges the VALUES (same dead-series class at value altitude). S8 must wire `environment=` explicitly from the same config the tf apply consumes, and C10's end-to-end fired-alarm evidence will prove the composed identity live.

## Release recommendation (final)

**GO** — arm auto-merge for #282. Conditions carried forward: F-7 one-line parser hardening at next touch; R-1 residual documented; S8 carries {environment-value wiring, C10 end-to-end fired alarm, PROV apply + arm via DP-4a, DMS-24h retirement precondition}; F-5's serving-side (S5) obligation rides the S2-GO ledger per the PROV-6 note.

**Acid test (round 2)**: every state I constructed to read green now trips at least one alarm whose metric identity is probe-proven live — and the one latent way I found to fool the tripwire itself (F-7) is filed with an executed reproduction.

**Round-2 probe counts**: 28/28 suite; 3/3 binding-test negative controls RED; 1 fmt-conformance probe; 1 multi-line-parser false-green reproduction; 6/6 alarm identity matches; 5 surviving-probe re-runs; 2 conformance gates (exports, Protocol verbatim); 2 terraform gates (validate, fmt-check); 1 cross-sprint claim check vs origin/main S2.
