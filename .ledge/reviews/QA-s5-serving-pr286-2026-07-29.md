---
type: review
status: accepted
---

# QA — S5 Serving Gate (PR #286, feat/substrate-v2-s5-serving @ 3e7ccfb7)

```yaml
verdict: GO
reviewer: qa-adversary (P7, rite 10x-dev)
date: 2026-07-29
scope: serve.py (GatedSubstrateReader + emission) · serve_adapters.py (CP-1..6) · mcp/asana_mcp/errors.py (additive 424) · 4 new test files (57 tests) + mcp 424 tests (7)
bar: DP-3 §Ratification · TDD §4 Seam-4 ([H14]-[H18], C2) + §11 C13 · RC-acceptance RC-C-1..3 + CP-1..6 + C1 · QA-s2 §5 F8 · ADR-serve-stale-within-bound (SUPERSEDED — must be unconstructable)
worktree: /Users/tomtenuta/Code/a8/a8/repos/autom8y-asana-wt-w2-s5 · env: primary .venv + PYTHONPATH shadow
prod_touch: NONE
probes_executed: 22 adversarial probes (15 scripted battery + 7 teeth/bypass/misclass probes), all receipts executed by my own hands
suites: 178 unit/substrate green · 51 harness green (178+51 = the "229 corpus") · 11 mcp errors green · seam contracts 3/3 · mypy strict green · ruff green
```

## 1. C1 REGRESSION (the wound) — LOCKED

Every stale-but-present path refuses loud; a 200-with-stale is unconstructable.

- **P-200-1** [executed]: 4 reasons x 3 payload shapes (missing / single-copy / sunset-marked)
  through ALL THREE translators (`serve_result_to_http`, `serve_result_to_cli`,
  `serve_result_to_recheck`): zero combos yield 200 / frame / exit-0 / PROVABLE;
  `Retry-After` present on every 424.
- **P-C2-1** [executed]: SAME reader instance, injected clock at age==SLA → serves
  (`<=` boundary per frozen predicate); age==SLA+1µs → `Refused(STALE)` on the very
  next read. No cached verdict. **P-C2-2**: refusal is not sticky (fresh re-swap serves).
- **P-CONC-1** [executed]: concurrent double-read on a stale artifact — both refuse,
  2 SLI emissions (`_refuse` fires the choke-point emitter on EVERY `Refused`).
- Refused-swallow sweep: CLI → exit 3 + `frame=None` + no number in message; recheck →
  `REFUSED` (never a fresh-mtime lie); HTTP → 424 + `frame=None`; persistence wrapper
  returns the sum (caller exhaustiveness applies). No honest-empty-200 arm exists.
- **[H15] teeth two-sided** [executed]: a consumer matching only `Provable` with
  `assert_never` on the rest → mypy `arg-type` error ("has incompatible type Refused");
  the both-arms consumer → clean. A bare value is unobtainable without handling `Refused`.
- The SUPERSEDED stale-200: no confidence label, no bound parameter, no code path that
  accepts staleness anywhere in serve.py/serve_adapters.py. Unconstructable, verified by read.
- Infra faults are NOT masked as refusals: non-404/non-corrupt store error propagates
  (`test_infra_error_propagates_not_masked_as_refusal` green) — receiver-5xx vs 424 division holds.

## 2. DIGEST@INGRESS — no tear constructible; the check bites

- **Tear analysis** (store.py `read_current`, read receipt): the proof RIDES the pointer
  (`PointerState.proof`), and bytes are fetched at the IMMUTABLE version key the same
  pointer named (`If-None-Match: *` write-once staging). A pointer swap between
  pointer-read and frame-GET yields the OLD coherent (bytes, proof) pair — gated
  normally; a GC-reaped version mid-read → 404 → `ArtifactMissing` → loud MISSING.
  Cross-version tear (new proof + old bytes or inverse) is not constructible through
  this read path.
- **Two-sided digest teeth** [executed]: tampered bytes under a fresh proof → CORRUPT
  (P-DIG-1); untampered → serves (P-DIG-2). **P-DIG-3**: a MISWIRED `digest_of_frame`
  (echoes the proof's own digest) WOULD serve tampered bytes — proving the corrupt
  fixtures are the discriminating tooth, and pinning the S8 obligation: the production
  `parse+canonical_digest` wiring must re-run this fixture class at S8 (the injected
  stand-in here proves gate LOGIC only — inherent to the dark build, correctly scoped).
- Un-digestable/foreign bytes → CORRUPT with age payload, never a 500 (test + probe green).

## 3. PER-READ AGE (C2) + [H16] tooth completeness

- Behavioral: `is_provable` re-fires per read (counting test green); boundary flip
  P-C2-1; adapters hold no state (verified by read + attr tooth).
- **[H16] AST tooth teeth-check** [executed]: decorator scan covers serve.py AND
  serve_adapters.py; functools-import scan covers serve.py only; reader attr allowlist
  pinned. **Gap found**: an ALIASED memoizer (`from functools import lru_cache as _fast`)
  evades the decorator-id set (executed: `ids=['_fast']`, caught=False), and no in-suite
  behavioral test exercises adapter-level caching. **P-ADPT-1** [executed]: double-query
  through `QueryServeAdapter` → 2 reader reads — adapters as-built do NOT cache. Finding
  F-3 (LOW): add adapter double-call behavioral test or alias-resolving scan.

## 4. ADAPTER FIDELITY (CP-1..6) + [H17]

- All six CPs hold a `SubstrateReader` / consume a `ServedNumber`; none imports the store
  (tooth green); plane-blindness unconstructable (typed `ArtifactId`; CP-5 key =
  `artifact_key(aid)`; CP-6 has no `entity_type: str | None` surface).
- **[H17] teeth bite** [executed, two-sided]: planted module importing
  `substrate.store` → tooth RED (`test_substrate_store_is_imported_only_by_...` FAILED);
  removed → GREEN.
- **[H17] bypasses found** [executed, both stay GREEN under the tooth]:
  (a) root-export bypass — a module importing only `autom8_asana.substrate`
  (`ArtifactStore` Protocol is among the 24 exports) and calling `.read_current` on an
  INJECTED concrete store trips nothing (the tree-wide scan is import-name-based; the
  `.read_current` attribute scan is adapters-only); (b) `importlib.import_module`
  string-built access trips nothing. Finding F-2 (MEDIUM, tooth-completeness — NOT a
  landed defect; real tree is clean): extend the `.read_current` attribute scan
  tree-wide with the same allowlist. The serve.py docstring claim "a consumer cannot
  obtain bytes except through this gate" slightly overclaims the tooth as landed.
- Boundary errors: unknown/non-servable entity AND malformed project_gid → 422, reader
  never reached, shape-hostile body (probe + test). Finding F-1 (LOW): the malformed
  project_gid arm reuses code `SUBSTRATE_UNSERVABLE_ENTITY_TYPE` and echoes the
  entity_type — misattributed diagnostic (suggest a distinct `SUBSTRATE_MALFORMED_PROJECT_GID`).

## 5. HTTP / MCP

- 424 + `Retry-After` for all four reasons [test + probe]; single parameterized value
  bound to the rebuild schedule per DP-3 (ratified shape).
- Shape-hostile envelope [executed]: strict rows-client `body["rows"]` → KeyError (loud);
  extended data-shaped key sweep ({rows, value, data, aggregate, result, records, items,
  df, frame, dataframe}) empty on all bodies incl. sunset + 422; envelope JSON
  round-trips (sunset datetimes isoformat). A `.get("rows", [])` client silently gets
  its OWN injected default — not envelope-defensible, noted for the S8 client-SDK (P11 law).
- mcp additive branch: 7 two-sided tests re-run green + pre-existing c3 taxonomy green
  (11 total); flat-envelope `code` pickup verified via `_body_code`'s top-level branch.
- **Non-substrate 424 probe** [executed]: `{"error":{"code":"WEBDAV_LOCK_FAILED"}}` w/
  424 → `kind=data-integrity-refusal` + a message asserting "asana substrate refused".
  `map_http_error` consumes satellite responses only (callers: `tools/_common.py:49,:64`,
  `tag_resolve.py:315`) and no satellite surface emits 424 today, so blast radius is
  zero as landed (matches the branch's ADDITIVE+INERT declaration). Finding F-4 (LOW):
  condition the substrate-asserting message text on the `SUBSTRATE_REFUSED_` code prefix;
  also the `McpToolError` kind docstring set omits `data-integrity-refusal` (doc drift).

## 6. C13 + F8

- C13: marker crosses the wire (`sunset_breach.{surface, sunset_after, observed_at}`,
  reason stays `stale`); frozen four fields untouched; 4-field ctor still valid;
  harness corpus green (the "229" = 178 unit/substrate + 51 harness, both suites green;
  the anticipatory `HarnessRefusePayload` slot-shadow resolves cleanly).
- The reader itself can never MINT a sunset breach (no sunset input) — CONSISTENT with
  the TDD §11 C13 ruling (payload marker = the mandatory S5 obligation, delivered;
  runtime detection = defense-in-depth backstop for a "near-unconstructable" case) and
  with the S5 dispatch spec (no runtime-detection exit criterion). Finding F-6 (INFO):
  named carry — the backstop wiring rides with S8/RC-D.
- F8 [executed]: age == −5.0 exactly → silent + serves; age < −5.0 → ONE
  `FutureDatedProofCount` disclosure + still serves per `is_provable` (P-F8-1); the
  disclosure also fires on the future-dated-plus-undigestable CORRUPT path (P-F8-2).
  Benign 1s skew silent (test green). Emission is best-effort (CW failure never raises).
- **Deliberate divergence, note for S8 receipts** (F-5, INFO): serve discloses beyond
  5s tolerance; S6 observe counts ANY negative age. In the (−5s, 0) fresh-swap window
  the scheduled sweep will show `FutureDatedProofCount` datapoints with ZERO serve-side
  twins. Same metric NAME, different namespaces (`Autom8y/SubstrateServe` vs S6's) and
  different thresholds — S8 receipts must attribute by namespace or the counts will
  look contradictory.

## 7. CONFORMANCE

- `git diff origin/main --name-status`: 2 M (mcp/asana_mcp/errors.py — the sanctioned
  additive branch; substrate/serve.py — S5-owned seam fill) + 6 A (serve_adapters.py +
  4 unit test files + mcp test). NOTHING else.
- rebuild.py / observe.py / freshness.py / identity.py / store.py / substrate/__init__.py
  untouched [executed `git diff --quiet` per file]; v1 surfaces (dataframes/, api/,
  metrics/) untouched.
- Exports 24/24 (guard asserts count + no-dupes; runtime `len(__all__)==24` re-confirmed);
  frozen `SubstrateReader` Protocol block BYTE-IDENTICAL to origin/main; only the
  sanctioned C13 additive (SunsetBreach + optional `sunset_breach=None`) lands inside
  the frozen region; RefuseReason closed at 4; Provable/Refused/ServedNumber untouched.
- mypy strict: green on both modules. ruff: green on all changed files.
- fastmcp failures PRE-EXISTING [executed]: same 5 `test_assembly_floor` failures with
  `ModuleNotFoundError: No module named 'fastmcp'` fire identically on the primary
  checkout whose mcp/ is diff-clean vs origin/main. Not introduced.

## 8. Findings register

| ID | Sev | Finding | Disposition |
|----|-----|---------|-------------|
| F-1 | LOW | Malformed project_gid 422 reuses `SUBSTRATE_UNSERVABLE_ENTITY_TYPE` code — misattributed diagnostic | fix-at-leisure (client-error taxonomy fidelity; not C1-class) |
| F-2 | MEDIUM | [H17] tooth: root-export-Protocol + injected-store `.read_current` and importlib access evade (executed green bypasses) | tooth-completeness carry — extend `.read_current` attr scan tree-wide; real tree clean today |
| F-3 | LOW | [H16] tooth misses aliased memoizers; no in-suite adapter no-cache behavioral test (P-ADPT-1 green as-built) | add adapter double-call test with the S8 wiring PR |
| F-4 | LOW | Any satellite 424 → substrate-asserting message (WEBDAV probe receipt); `McpToolError` kind docstring omits new kind | condition message on `SUBSTRATE_REFUSED_` prefix; doc fix |
| F-5 | INFO | serve(5s) vs observe(<0) future-dated threshold divergence — deliberate per F8 ruling | S8 receipts: attribute FutureDatedProofCount by NAMESPACE |
| F-6 | INFO | Sunset-breach runtime detection unwired in reader (marker capability only) | consistent w/ TDD C13 + dispatch spec; named S8/RC-D carry |
| F-7 | LOW | `Provable(frame=None)` runtime-constructible (frozen dataclass, no runtime validation) → a bug caller could mint a frame-less 200 | mypy strict is the landed guard; optional `__post_init__` isinstance tooth |
| F-8 | LOW | `retry_after_seconds` unvalidated (negative int would emit a nonsense header) | clamp at S8 wiring where the rebuild schedule binds |

No CRITICAL/HIGH. No C1-class finding: no input, race, boundary, or bug path produced a
200/exit-0/PROVABLE for stale-but-present content in 22 executed probes.

## 9. Cross-rite handoff assessment

- **Docs impact**: none user-facing yet (DARK build; no consumer repointed — cutover is S8).
- **Security handoff**: security-reviewer is co-seated per the S5 dispatch spec; refusal
  bodies verified topology-free (no S3 key/bucket/version/path; MISSING is
  enumeration-oracle-safe: never-built vs reaped are byte-identical shapes; 422 echoes
  only the caller's own input token).
- **SRE handoff**: `Autom8y/SubstrateServe` namespace + `SubstrateRefusalCount`
  {environment} + {environment, reason} series and `FutureDatedProofCount` need RC-F
  alarm terraform at S8 (the S6 {environment} identity-contract lesson is carried verbatim).

**VERDICT: GO** — arm merge for #286. Conditions: none blocking. Carries F-2/F-5/F-6
to the wave ledger for S8.
