---
type: review
artifact_type: qa-adversarial-review
artifact_id: QA-s7-harness-pr283-2026-07-29
title: "QA S7 — cutover-gate harness (PR #283): scaffold review + authored 22-predicate corpus"
date: 2026-07-29
author: qa-adversary
status: proposed
verdict: GO
consumes:
  - .ledge/specs/RC-acceptance-predicates-substrate-v2.md
  - .ledge/reviews/DEFECT-seam1-entity-blind-prober-plane-split-2026-07-27.md
subject:
  pr: 283
  branch: feat/substrate-v2-s7-gate-harness
  scaffold_sha: 2e481af47dc0bac550e442192502a57da4aa01b5
  corpus_sha: c6b7d85b204ed51477a9e818af3f497915156b70
prod_touch: NONE
---

# QA S7 — Harness Scaffold Review + Authored Adversarial Corpus (PR #283)

**VERDICT: GO** on the combined (scaffold + corpus) state, with the findings and
follow-ups below. Dual role per the S7 shape: (1) authored the 22-predicate
adversarial fixture corpus into the scaffold; (2) adversarially reviewed the PE's
scaffold while working inside it. `prod_touch: NONE` — fixture-driven only; the
live leg stays DARK behind `LiveParityNotArmedError` (verified: the dark guard
bites even when `armed=True` without an injected outbound impl).

## 1. Verification summary (receipts)

All runs from the worktree `/Users/tomtenuta/Code/a8/a8/repos/autom8y-asana-wt-w2-s7`
via the primary checkout's `.venv` + `PYTHONPATH` shadow (worktree `.venv` is empty
— see F-7):

| Gate | Command | Result |
|---|---|---|
| pytest | `python -m pytest tests/harness/substrate_gate/ -q` | **51 passed** |
| mypy (CI does NOT gate `tests/harness/**` — PE flagged; confirmed: `test.yml:53` `mypy_targets: 'src/autom8_asana'`) | `mypy --strict tests/harness/substrate_gate/` | **clean, 14 files** |
| ruff | `ruff check` + `ruff format --check` | **clean** |
| Discrimination matrix | reference vs SilentServe vs OverRefuse over the full corpus | reference **39/39 PASS**; SilentServe tripped **21/21** broken (100%); OverRefuse tripped **18/18** good (100%) |

Corpus totals: **39 replay cases** (21 broken / 18 good, all `qa-` prefixed cases
authored this pass; 9 PE worked cases retained) **+ 5 construction-altitude
realizations** (RC-C-1, RC-C-3, RC-E-2, RC-E-3, RC-E-4) **+ 10 structural/probe
tests**. Refuse-reason span: DIVERGENT ×5, CORRUPT ×6, STALE ×8, MISSING ×2 —
every member of the CLOSED enum exercised, plus SERVE.

## 2. Corpus coverage table — 22 predicates × two-sided teeth

Saboteur column: broken replay cases verified to trip `SilentServeSubstrate`
(silent wrong-serve caught); good replay cases verified to trip
`OverRefuseSubstrate` (over-refusal caught). Construction cases are discriminated
by the disciplined twin succeeding while the blind/impure construction is refused
(saboteur N/A — no `SeededState` exists to feed a saboteur).

| Predicate | GOOD (serve side) | BROKEN (reject side) | Reason | Trips saboteur |
|---|---|---|---|---|
| RC-A-1 | PE single-source; QA `empty-but-valid` boundary ($0 ≠ MISSING) | QA `two-readable-copies` (DEFECT :74 per-section + consolidated both live) | DIVERGENT | ✅ both sides |
| RC-A-2 | QA `coherent-post-warm` ($84,385, composition asserted) | PE Parity Exemplar; QA `zero-net-composition-shift` boundary | DIVERGENT | ✅ both sides |
| RC-A-3 | QA `rebuild-refreshes-read` (warm output IS read target) | PE `missing` | MISSING | ✅ both sides |
| RC-A-4 | QA `single-writer-discipline` | QA `entity-blind-shadow-writer` (the wound's root cause as input) | DIVERGENT | ✅ both sides |
| RC-B-1 | QA `value-edit-reproved` | PE age-stale; QA `gid-preserving-value-edit` (**D8**) | STALE / CORRUPT | ✅ both sides |
| RC-B-2 | QA `age-exactly-sla` (inclusive boundary) | PE content-stale; QA `false-fresh-reconsolidation` (**wound-replay**); QA `sla+1s` boundary twin | STALE / CORRUPT | ✅ both sides |
| RC-B-3 | QA `min-over-section-instants` (C1 semantics) | QA `bulk-stamp-34-sections` (DEFECT :22a) | CORRUPT | ✅ both sides |
| RC-B-4 | QA `establishable-currency` | PE digest-mismatch corrupt | CORRUPT | ✅ both sides |
| RC-C-1 | typed construction serves | **construction-refusal**: `ArtifactId` w/o `entity_type` → TypeError; signature asserted no-default/no-None | n/a (type) | ✅ two-sided at type altitude |
| RC-C-2 | QA `entity-typed-plane-serves` + `SeededState`-requires-typed-aid floor | QA `legacy-plane-shadow-readable` (default-None trap as input) | DIVERGENT | ✅ both sides |
| RC-C-3 | brand-new consumer WITH discriminator serves | **construction-refusal**: brand-new consumer (no guard-list reference) cannot compile/construct plane-blind | n/a (type) | ✅ two-sided at type altitude |
| RC-D-1 | QA `bridge-inside-sunset` | PE sunset-breach, **upgraded: + C13 marker assertion** | STALE + marker | ✅ both sides + marker-blind caught |
| RC-D-2 | QA `sunset-boundary-at-now` (strictly-after semantics) | QA `one-second-past-sunset` + C13 marker | STALE + marker | ✅ both sides + marker-blind caught |
| RC-D-3 | QA `legacy-plane-deleted` (deletion, not disabling) | QA `legacy-plane-survives-cutover` (agreeing mirror past window) + C13 marker | STALE + marker | ✅ both sides + marker-blind caught |
| RC-E-1 | QA `last-good-survives-kill` | QA `torn-mid-rebuild-frame` (**mid-rebuild partial state must never serve**) | CORRUPT | ✅ both sides |
| RC-E-2 | serve is pure + idempotent (zero writes into seeded prod-state) | **construction-refusal**: write attempt into the read path's input → `FrozenInstanceError` (DEFECT :76 class) | n/a (frozen) | ✅ two-sided |
| RC-E-3 | read surface exposes zero write-named capability | **structural**: `WriteCapableFake` IS flagged by the probe (probe discriminates) | n/a (structural) | ✅ two-sided |
| RC-E-4 | `PacedLiveParitySource.routes_through_paced_primitives()` True; dark-guard + no-AsanaClient (PE tests) | **structural**: `UnpacedFake` flagged False (429-storm shape distinguishable) | n/a (structural) | ✅ two-sided |
| RC-F-1 | QA `silent-on-provable` | PE stale-unprovable; QA `fires-on-corrupt` (all unprovability classes fire) | STALE / CORRUPT | ✅ both sides |
| RC-F-2 | PE provable-serve | **saboteur-side-only** (documented exemption): over-refusal is a substrate defect, not a corruptible input — its REJECT side IS the OverRefuse trip on the good case, asserted explicitly | n/a | ✅ OverRefuse FAILs it |
| RC-F-3 | QA `rarely-queried-but-fresh` + structural no-query-history-input floor (`FreshnessProof` fields asserted) | QA `stale-under-query-silence` (AL-5 query-gating wound) | STALE | ✅ both sides |
| RC-F-4 | QA `watched-signal-live` | QA `watched-signal-absent` (orphaned dead-man: absence must FIRE; SilentServe's silent-zero is the exact wound) | MISSING | ✅ both sides |

Serve-altitude proxies are disclosed in-docstring where the full-fidelity surface
is out of harness reach at wave-2: RC-C-2 per-path matrix (S8, real CP-1..6),
RC-D-2 CI teeth (CI altitude), RC-F-1..4 alarm emission (S6 observability seam).
This mirrors the RC spec's own OQ-2 degradation-disclosure discipline.

## 3. C13 addendum — how it was folded in

Per the architect's TDD §11 C13 ruling (received mid-task): the CLOSED
`RefuseReason` enum STANDS (sunset-breach → `STALE`), and the payload MUST carry
a named field-level marker. Implementation (harness-internal, seam un-narrowed):

- `SunsetBreach {surface, sunset_after, observed_at}` + `HarnessRefusePayload`
  (a frozen subclass of the frozen seam `RefusePayload` adding the 5th ADDITIVE
  optional field; the frozen four land verbatim) — `cases.py`.
- The reference oracle's sunset branch emits the marked payload; the classifier
  asserts marker equality when the expectation carries one — `reference.py`,
  `replay.py`.
- All three sunset cases (RC-D-1 worked upgraded, RC-D-2/D-3 authored) expect
  `STALE` **+ populated marker**. Teeth proof: `OverRefuseSubstrate` refuses
  STALE with a marker-less payload — the REASON coincides on these cases, so
  ONLY the marker discriminates, and the suite asserts those refusals FAIL
  (`test_sunset_marker_cases_catch_a_marker_blind_refusal`). Machine-checked,
  not comment-only. No EXPIRED member invented.
- **Follow-up for S4 owner**: the frozen seam `substrate/serve.py::RefusePayload`
  does not yet carry the C13 field; the harness realizes the ruling internally.
  S4 should land the seam-side additive field per C13, at which point the
  harness subclass collapses into the seam type.

## 4. Scaffold findings (adversarial review)

**F-1 — DETECTION HOLE, FOUND + CLOSED: `diverges()` was scalar-only.**
`divergence.py::diverges` compared `served_value` totals only. Two copies with
equal totals but a shifted composition (offers moved ACTIVE→STAGED, ±$4,000,
net $0 — the DEFECT's own move pattern with zero net) were silently served,
canonical-first: a section-level consumer reads 14-day-stale content under a
green serve. That is the wound class at section altitude, inside the harness's
own oracle. Fixed (per-section comparison added, `divergence.py`), and locked by
`qa-rc-a-2-broken-zero-net-composition-shift`. Fix is harness-internal (test
oracle, not production code); PE should review the change on the PR.
[TACTICAL — verified by the new case failing against the pre-fix behavior class
and passing post-fix; SilentServe still trips it.]

**F-2 — SCHEMA EXPRESSIVENESS: five predicates are not `SeededState`-expressible,
and the scaffold mis-declared them as pending REJECT replay slots.**
RC-C-1, RC-C-3, RC-E-2, RC-E-3, RC-E-4 have falsifying inputs that are
unconstructable by design (that IS their BY-CONSTRUCTION acceptance mode) — no
`ReplayCase` can carry them, so the scaffold's `_PENDING_TITLES` promising
"→ REJECT" replay cases for them could never have been filled as declared, and
`test_pending_slots_are_declared_broken_reject_placeholders` codified the wrong
expectation. Resolved: slot taxonomy now partitions replay slots vs
`construction_slots()` (`CONSTRUCTION_ALTITUDE_IDS`), and the five are realized
two-sidedly at construction altitude (TypeError / FrozenInstanceError /
structural-flag with the disciplined twin succeeding). [STRUCTURAL finding about
the scaffold's declared surface; resolution authored in this pass.]

**F-3 — CLASSIFIER HOLE, FOUND + CLOSED: `ExpectServe` asserted total+plane only.**
A substrate serving the RIGHT total over the WRONG per-section content passed.
Added optional `composition` assertion to `ExpectServe` + classifier
(back-compatible; used where composition is load-bearing, e.g. the coherent
post-warm case). [TACTICAL — closed.]

**F-4 — Σ-INVARIANT WAS AUTHOR-BYPASSABLE: schema now resists sloppy authors.**
`divergence_payload`'s Σ per_section_delta == magnitude invariant holds only if
each materialization's composition sums to its `served_value` — previously
unenforced, so an inconsistent fixture would make the ledger silently
mis-explain. Added a `Materialization.__post_init__` guard (ValueError at
construction, ±$0.005) + corpus-wide uniqueness/coherence tests
(`case_id` uniqueness, variant⟺expectation shape). A vacuous case is now
additionally unconstructable via `test_no_case_is_vacuous` (reference PASS +
variant-appropriate saboteur FAIL required for EVERY case). [TACTICAL — closed.]

**F-5 — Verdict comparison audited, no further holes:** reason matching is
identity-sensitive (`reason is expected.reason` — no reason-insensitive
matching); value tolerance is absolute $0.005 (sub-cent, appropriate for dollar
aggregates; not exploitable at fixture magnitudes); plane compared exactly;
expected-side blindness holds (`serve` receives only `SeededState`, never the
`ReplayCase`). Minor notes, acceptable as-is: (a) a substrate serving
undecodable frame bytes crashes the runner (loud, but a `FAIL`-classification
would be cleaner at S8 when real frames appear); (b) `run_case` does not catch
substrate exceptions — same posture, acceptable for wave-2, revisit at the S8
adapter. [TACTICAL — noted, not blocking.]

**F-6 — Sunset reason collision (pre-C13) — RESOLVED by C13.** The original
brief's concern (sunset-breach indistinguishable from age-STALE in the closed
grammar) was ruled by the architect mid-task; discriminated by altitude + the
named marker. Encoded as above; distinguishability is now machine-tested.

**F-7 — ENV: worktree `.venv` is empty (uv 401 class).** All gates run via the
primary checkout's `.venv` + `PYTHONPATH=<wt>/src:<wt>` shadow. Reproducible
invocation is in §1. Not blocking, but S8 execution docs should carry it.

**F-8 — CI GAP (confirmed, PE had flagged): `tests/harness/**` is not
mypy-gated.** `.github/workflows/test.yml:53` pins `mypy_targets:
'src/autom8_asana'`. The harness is now 14 files of `--strict`-clean typed code
whose RC-C claims lean on the type checker; recommend adding a harness mypy
island to CI so the construction-refusal floor is continuously enforced, not
locally-heroically. [TACTICAL — recommend routing to PE/platform.]

**F-9 — Scaffold strengths (positive findings):** expected-blindness by
construction; the CLOSED reason grammar consumed from the frozen seam (no
parallel refusal vocabulary invented); the dark guard genuinely two-keyed
(`armed=True` alone still refuses); `routes_through_paced_primitives` is a
property of the object, not a comment; the no-AsanaClient import tripwire covers
every harness lib module. The scaffold was corpus-ready as claimed — all 13
pending predicates were authorable without schema surgery except the five whose
inexpressibility is itself the predicate (F-2).

## 5. Follow-ups routed

| # | Item | Route |
|---|---|---|
| 1 | Land the C13 `sunset_breach` additive field on the frozen seam `RefusePayload`; collapse `HarnessRefusePayload` into it | S4 owner (architect-ruled, C13) |
| 2 | Add harness mypy island to CI (`tests/harness/**`) | principal-engineer / platform |
| 3 | S8: real CP-1..6 per-path serve matrix (RC-C-2 full fidelity), real-frame decode into `FrameContent`, exception→FAIL classification decision | S8 leg |
| 4 | S6: true alarm-emission fixtures for RC-F-1..4 (harness carries the serve-altitude proxy only) | S6 leg |
| 5 | PE review of the two harness-lib fixes made during corpus authoring (`diverges` composition-awareness; classifier extensions) | principal-engineer (on PR #283) |

## 6. Attestation

| Artifact | Path | State |
|---|---|---|
| Corpus + scaffold fixes (commit `c6b7d85b`) | `autom8y-asana-wt-w2-s7/tests/harness/substrate_gate/{cases,corpus,divergence,reference,replay,__init__,test_corpus_scaffold}.py` | pushed to `feat/substrate-v2-s7-gate-harness` (updates PR #283) |
| This review | `.ledge/reviews/QA-s7-harness-pr283-2026-07-29.md` | filed (primary checkout) |

Self-attestation ceiling: the corpus author attesting the corpus caps at
MODERATE per self-ref-evidence-grade-rule; the discrimination matrix
(reference 39/39 / SilentServe 21/21 / OverRefuse 18/18) is mechanically
re-runnable by any rite-disjoint attester via the §1 commands. The acid test —
*"if this goes to production and fails in a way I didn't test, would I be
surprised?"* — the residual surprise surface is exactly the disclosed proxies
(§2 note) and the S6/S8 legs, all routed.
