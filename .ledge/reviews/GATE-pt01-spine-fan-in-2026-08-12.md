---
type: review
status: accepted
artifact_id: GATE-pt01-spine-fan-in-2026-08-12
initiative: asana-native-insight-delivery
gate: PT-01 (hard — spine fan-in)
seat: potnia (10x-dev), Read-only, zero mutations
date: 2026-08-12
verdict: SPINE COHERENT WITH CONDITIONS
inscribed_by: main thread (the seat has no Write tool; this is its report of record)
---

# PT-01 — SPINE FAN-IN GATE

**Verdict: `SPINE COHERENT WITH CONDITIONS`.**

The four artifacts' *verdicts* are mutually consistent and jointly answer what
this initiative can honestly deliver, from where, and what to do with the
leftovers — but their **prescriptions contradict each other at one wire**, two of
three `SAY-ABLE` readouts rest on a surface that has never been invoked, and no
sprint owns the composition questions that appear only when three readouts share
one rail.

PT-01 does **not** fail. The shape's on_fail condition is *"ruling the fork on an
incomplete briefing"*; the briefing is not incomplete — it is **unconsolidated
and in two places over-flattened**. That is an assembly charge, not a re-run.

## Conditions

| # | condition | owner |
|---|---|---|
| **C-1** | Resolve the S1/S4 `meta`-read contradiction. Either O-7 is ruled, or S1 §3.0.1's named path is corrected to match S4 §9 | operator (O-7) or S1 |
| **C-2** | The fork briefing must carry items 2 and 5a at their **actual** evidential standing — uncorroborated, seat-flagged — never flattened into "three say-able readouts" | PT-02 |
| **C-3** | S4 exit criterion 6 (arch-adversary) is **OPEN by S4's own self-audit**. Discharge or waive on the record | main thread / operator |
| **C-4** | Assign the two unowned composition questions — readout-vs-readout legibility, and cadence — before any build | PT-02 |
| **C-5** | Item 2's error-direction claim is re-derived or probed | S1 / one probe |

## The sharpest coherence finding — a live contradiction

S1 rev-3's named build path for item 1a instructs: *"Read `honest_contract_complete`
and the truncation pair off `meta`."*

S4's K-lane attestation holds **because the readout does the opposite**: *"The
readout consumes the row payload … never the `meta` object. It **counts rows
itself** rather than reading `meta.total_count` / `meta.returned_count`."*

`honest_contract_complete` is a `RowsMeta` field, and `RowsMeta` is named
**verbatim** on the shape `:1502-1503` fence. S4 treats reading
`meta.honest_empty` — the same class — as acquiring a real dependency and
correctly presents it as a **fork, not a free action**. S1 prescribes the
analogous read as the recommended path, in the same paragraph where it cites
S4's contrary trade. **Both cannot stand.** It is not a withheld-items question,
as S1's O-7 framing implies — it is live on a `SAY-ABLE` item's build path and on
S4's central attestation.

**What hinges on O-7:**

| item | needs PERMISSIVE | needs STRICT |
|---|---|---|
| S1 item 1a's named build path | ✅ | breaches |
| S4 §9's K-lane attestation as written | — | ✅ |
| S4 §8.4 precondition 6, Branch B | ✅ free | ✅ real dependency, correctly forked |
| S1 item 1b and §2.7 row 3 | both become reachable | stay withheld |

The fence's own operative sentence — *"if a readout wants a number that **only
exists** on the K-lane, it WAITS"* — is the test S4 used to **permit**
`max(last_modified)` and to **fork** `honest_empty`. **The two artifacts have
already applied it in opposite directions on the same field class.**

## Convergence, verified independent

S1's `/rows` and S4's option (b) are **the same surface**, reached from opposite
directions — S1 by asking which envelope carries the receipts a render needs, S4
by asking which source is contractable. Neither cites the other; each names a
property the other does not. **The spine's strongest structural result.**
*(Distinguished from the one proven S1→S4 elision, which was on a different
finding and which S4 rev-2 conceded and credited.)*

## Coverage holes — two, each visible only side-by-side

1. **Readout-vs-readout legibility.** S3's D-1..D-4 solve readout-vs-abort. S1
   then promoted three readouts. D-2 requires "a glyph unused in this channel";
   three readouts need three, mutually. Nobody owns it.
2. **Cadence.** No artifact names how often any readout fires. Telos rung 2
   requires *"two consecutive deliveries on the readout's own cadence"* — **the
   realization predicate is gated on a parameter nobody owns.**

## The error-class judgment — the gate's most valuable output

**It is a property of how the spine was charged, not a coincidence — and it is
three of four seats, not two.**

S1 five times · S3 once (a directory-scoped grep proving `create_comment` absent
under `api/`, generalized to "no HTTP route" — false; S3's own verdict: *"a true
receipt is not a true claim"*, committed one row away from having just caught the
same error in an inherited receipt) · S4 once (option (g), with the file open).
**S2 is the only seat that did not — and S2's charge was inverted**: it was told
to *falsify* premises against source rather than inherit them.

Three structural mechanisms:

1. **The charge makes absence the honourable answer.** Under a floor reading
   *never confidently wrong*, every seat correctly inferred that over-claiming is
   the sin — then discharged that discipline by **over-refusing**, which the same
   floor does not punish.
2. **The receipt grammar cannot express a negative.** A presence anchors to
   `file:line`; an absence cannot. So a *true scoped probe* and a *false general
   claim* are **indistinguishable at review time**.
3. **No gate anywhere fires on over-refusal.** Every error was caught by external
   critique or by a coordinator handing a seat a surface — **never by the seat's
   own machinery.**

### The prediction

> The next error is another **false negative**, and the spine has one large
> unrefuted absence claim left: **S1's cost tier (iii) — "edit history is
> genuinely absent and not constructible"** — its sole surviving "expensive"
> tier, asserted by the seat whose three prior absence claims were each refuted
> by reading further.
>
> Two un-swept refuters: the retained frame snapshots (`dataframe_cache_put`),
> and **story subtypes beyond `section_changed`** in `DEFAULT_STORY_TYPES`.
>
> *Sweeping both is cheaper than the fork decision that currently rests on the
> claim.*

**The second refuter fired at the first place it was looked.** See
`PROBE-pt01-c5-and-edit-history-2026-08-12.md`.

## The rung

**MODERATE is right for the spine, and it is not the right *floor* everywhere it
is claimed.** Three strands carry genuine rite-disjoint STRONG. But **the current
text of three of four spine artifacts has had zero rite-disjoint reading** — S1's
items 2 and 5a postdate the critic's delta pass, S3 rev-2 is post-condition, S4
rev-2's *named* critic never ran at all.

## Self-attestation

**MODERATE.** This gate had no rite-disjoint reader — and per its own §6 that is
exactly the condition under which this spine has produced its errors.

**Nothing operator-reserved was ruled**: GATE-FORK, the gate-(b) §OPEN scope
question, OS-1/OS-2, the telos, CR-2/OS-7, OS-6, O-7 and K-0b are named and
routed, decided nowhere.
