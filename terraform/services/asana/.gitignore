# Terraform init/state artifacts -- never commit (authored-only module)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan

# Operator-local variable input. terraform.tfvars.example IS committed (it is the
# documented knob surface); the real tfvars is operator input, not repo state.
terraform.tfvars
*.auto.tfvars
