# WS-P2-04: Test Fixes + automation/ Reorg (OPTIONAL)

**Objective**: Fix 20 phantom HTTP mock targets in unit tests, and optionally begin
automation/ pipeline rules extraction to lifecycle/.

**Rite**: hygiene
**Complexity**: PATCH (G-07) + MODULE (G-06, if attempted)
**Gaps**: G-07 (phantom mocks), G-06 (automation/ reorg -- Scenario C only)
**Preconditions**: None (independent of P2-01 through P2-03)
**Estimated Effort**: 5 hours (G-07: 2-3 hrs, G-06: 2-3 hrs if started)

**THIS SESSION IS OPTIONAL. Execute only if time permits after P2-01 through P2-03.**

---

## Problem

**G-07**: 20 unit tests mock `httpx.AsyncClient` but production uses `Autom8yHttpClient`.
These tests pass vacuously (mocking something unused) and do not validate actual behavior.
Fix patterns are documented in `.wip/REMEDY-tests-unit-p1.md`.

**G-06**: `automation/` contains 33 files (10.8 kLOC) but only 12 external imports.
The pipeline rules (`pipeline.py` hierarchy placement, validation) logically belong in
`lifecycle/`. Scenario C: extract only the pipeline rules subset, not full decomposition.

---

## Implementation Sketch

### G-07: Fix Phantom HTTP Mock Targets (2-3 hrs)

**Reference**: `.wip/REMEDY-tests-unit-p1.md`, Batch 1 (RS-001, RS-002)

**Step 1**: Fix 11 tests in `tests/unit/clients/data/test_client.py`
1. Read the remedy report RS-001 section for exact fix patterns
2. Replace `httpx.AsyncClient` patch target with `autom8_asana.clients.data.client.Autom8yHttpClient`
3. Update assertions from `httpx.AsyncClient` constructor kwargs to `HttpClientConfig` fields
4. Fix 3 close tests: `aclose()` -> `close()`
5. Run: `pytest tests/unit/clients/data/test_client.py -x`

**Step 2**: Fix 8 tests in `tests/unit/services/test_gid_push.py`
1. Read the remedy report RS-002 section for exact fix patterns
2. Apply same mock target correction
3. Run: `pytest tests/unit/services/test_gid_push.py -x`

**Step 3**: Verify fix count
```bash
# Should be 0 remaining phantom httpx mocks
grep -r "httpx.AsyncClient" tests/unit/clients/data/test_client.py | wc -l  # -> 0
grep -r "httpx.AsyncClient" tests/unit/services/test_gid_push.py | wc -l   # -> 0
```

### G-06: automation/ Pipeline Rules -> lifecycle/ (2-3 hrs, IF TIME PERMITS)

**Scenario C only**: Extract pipeline rules, not full decomposition.

1. Identify which files in `automation/` contain pipeline placement/validation rules
   (likely `pipeline.py`, possibly `validation.py`)
2. Move to `lifecycle/pipeline_rules.py` or similar
3. Update import sites (~5-10 files)
4. Run: `pytest tests/unit/automation/ tests/unit/lifecycle/ -x`

**If this takes more than 2 hours**: STOP and document remaining work in MEMORY.md.
This is low-leverage (+2 points) and should not delay Phase 2 completion.

### Final QA Gate

- Full suite: `AUTOM8Y_ENV=production .venv/bin/python -m pytest tests/ -x`

---

## File-Scope Contract

```
TOUCH (G-07):     tests/unit/clients/data/test_client.py,
                  tests/unit/services/test_gid_push.py
TOUCH (G-06):     automation/pipeline.py (if attempted),
                  lifecycle/pipeline_rules.py (NEW, if attempted)
DO NOT TOUCH:     src/autom8_asana/core/, src/autom8_asana/protocols/,
                  src/autom8_asana/query/, src/autom8_asana/clients/data/_policy.py
```

---

## Do NOT

- Decompose automation/ fully (Scenario B) -- only Scenario C (pipeline rules)
- Change production code in clients/data/ while fixing test mocks
- Spend more than 2 hours on G-06 -- stop and defer if complex
- Touch files in P2-01, P2-02, or P2-03 scope

---

## Green-to-Green Gates

- G-07: zero `httpx.AsyncClient` mock targets in test_client.py and test_gid_push.py
- G-07: previously-failing tests now pass
- G-06 (if attempted): pipeline rules moved, imports updated, tests green
- Final: full test suite green

---

## Definition of Done

- [ ] 11 tests in test_client.py fixed (mock target + assertions)
- [ ] 8 tests in test_gid_push.py fixed (mock target)
- [ ] 3 close tests fixed (aclose -> close)
- [ ] (OPTIONAL) Pipeline rules extracted to lifecycle/
- [ ] Full test suite green
- [ ] MEMORY.md checkpoint: "P2-04: phantom mock fixes DONE [+ pipeline rules if completed]"
