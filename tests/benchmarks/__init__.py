"""Benchmark scripts for cache performance measurement.

These are not pytest tests - they are standalone scripts that output
timing data for performance analysis.

Usage:
    python -m tests.benchmarks.bench_cache_operations
    python -m tests.benchmarks.bench_batch_operations
"""
