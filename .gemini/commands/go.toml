name = 'go'
description = 'Session status dashboard — shows where you are and suggests next action'
prompt = "---\nname: go\ndescription: \"Session status dashboard — shows where you are and suggests next action\"\nargument-hint: \"[task description] [--full]\"\nallowed-tools: Bash, Read\nmodel: opus\n---\n\n## Purpose\n\n`/go` is a lightweight status dashboard. It reads system state, presents it, and suggests what to do next. It does NOT dispatch to other commands — the user decides what to invoke.\n\n## Context\n\nAuto-injected by SessionStart hook (project, rite, session, git).\n\n## Your Task\n\nShow session status and suggest next action. $ARGUMENTS\n\n## Phase 1: Collect State (parallel)\n\nRun these in parallel to gather all state in under 3 seconds:\n\n```bash\n# 1. Session status\nari session status --output json 2>/dev/null\n\n# 2. Session list (recent, any status)\nari session list --output json 2>/dev/null\n\n# 3. Active rite\ncat .knossos/ACTIVE_RITE 2>/dev/null || echo \"none\"\n\n# 4. WIP artifacts\nls .sos/wip/ 2>/dev/null\n\n# 5. Procession status (if session has procession block)\nari procession status --output json 2>/dev/null\n\n# 6. Realization-lens state: LIVE eunomia refusals + INCEPTION gaps.\n# (a) LIVE eunomia refusals/attestations — enumerate VERDICT artifacts.\nls -t .sos/wip/eunomia/VERDICT-*.md 2>/dev/null | head -5\n# (b) INCEPTION gaps — active initiatives lacking .know/telos/{slug}.md.\n# Cross-reference ari session list .initiative values against .know/telos/ presence.\nls .know/telos/*.md 2>/dev/null\n# Telos-overdue flags surface via naxos triage (optional, separate command).\nari naxos triage --output json 2>/dev/null | head -40\n```\n\nAlso read the session context table injected by the SessionStart hook (already in your context above).\n\n## Phase 2: Classify and Present\n\nEvaluate the collected state and classify into one of four states: ALREADY_ACTIVE, RESUME_PARKED, NEW_WORK, or ORIENTATION.\n\n### Mode Selection\n\nIf `$ARGUMENTS` contains `--full`:\n- Strip `--full` from arguments (it is a flag, not a task description)\n- Use **Full Mode** below\n\nOtherwise: use **Brief Mode** (default).\n\n---\n\n### Brief Mode (default)\n\nFor each classified state, output a compact card. No preamble, no extra commentary.\n\n**ALREADY_ACTIVE** (session with status=ACTIVE):\n```\nRite:    {active_rite}\nPhase:   {current_phase}\nSession: {session_id}\n\nnext: {hint}\n```\n\nIf `ari procession status` returned data (procession is active), append a procession line:\n```\nRite:        {active_rite}\nPhase:       {current_phase}\nSession:     {session_id}\nProcession:  {type} — station {current_station} ({completed_count+1}/{total_stations})\n\nnext: {hint based on station goal or phase}\n```\n\nResolve the `next:` hint: read `.knossos/ACTIVE_WORKFLOW.yaml`, find the phase matching `current_phase`, check its `next` field. If `next` names a phase → `/handoff {that_phase_agent}`. If `next: null` (terminal) → `/sos wrap` or `/commit && /pr`. If the user provided `$ARGUMENTS`, use their intent as the hint instead. If a procession is active and the station work appears complete, hint `ari procession proceed`.\n\n**RESUME_PARKED** (single parked session):\n```\nParked:  {session_id} ({relative_time} ago)\nRite:    {active_rite}\n\nnext: /sos resume\n```\n\n**RESUME_PARKED** (multiple parked sessions):\n```\nParked sessions:\n1. {id_1} — {initiative_1} ({time})\n2. {id_2} — {initiative_2} ({time})\n\nnext: /sos resume\n```\n\n**NEW_WORK** ($ARGUMENTS provided, no active/parked sessions):\n```\nNo active session.\nIntent: {$ARGUMENTS}\n\nnext: /consult \"{$ARGUMENTS}\"\n```\n\n**ORIENTATION** (nothing active, no arguments):\n```\nRite:    {active_rite or \"none\"}\nSessions: {active_count} active, {parked_count} parked\n\nnext: /consult\n```\n\nIf `.sos/wip/` artifacts exist without a session, append: `Note: {n} artifacts in .wip/ with no active session.`\n\n---\n\n### Realization-Lens Panel (ALL states, Brief Mode)\n\nAfter the primary state card, append a compact realization-lens section. Render EXACTLY two signal sets:\n\n```\nRealization-Lens:\n  Live refusals:  {n} ({most_recent_verdict_id} @ {relative_time})\n  Inception gaps: {n} ({initiative_1}, {initiative_2}, ...)\n                  (no .know/telos/{slug}.md declared)\n```\n\n- **Live refusals** = count of `.sos/wip/eunomia/VERDICT-*.md` files; show most recent verdict filename and its mtime.\n- **Inception gaps** = count of open/framed initiatives (active sessions + `.sos/wip/frames/*.md`) whose slug has no matching `.know/telos/{slug}.md`.\n- If both counts are zero, omit the panel entirely (ambient signal, not ceremony).\n\nSignal-set discipline (LOAD-BEARING): render ONLY live refusals and inception gaps. Do NOT render closed-unattested counts, N_applied aggregates, aggregate health grades, or percentage-realized rollups.\n\n---\n\n### Full Mode (`--full`)\n\nDisplay expanded dashboard with sprint progress and multiple options.\n\n**ALREADY_ACTIVE** (session with status=ACTIVE):\n\nDisplay a compact status block. No preamble.\n\n```\nSession: {session_id}\nRite: {active_rite} | Phase: {current_phase}\nInitiative: {initiative}\n\nSprint Progress:\n- [x] Completed task\n- [ ] Current task  <-- you are here\n- [ ] Next task\n\nSuggested next action: {inferred from phase and sprint state}\n```\n\nIf a procession is active (`ari procession status` returned data), add a procession block:\n\n```\nProcession: {type}\n  Completed: {completed_station_1} ({rite}) → {completed_station_2} ({rite})\n  Current:   {current_station} ({current_rite})\n  Next:      {next_station} ({next_rite})\n  Progress:  {completed_count}/{total_stations} stations\n```\n\nRead the session's `SESSION_CONTEXT.md` and any `SPRINT_CONTEXT.md` to populate sprint progress. If sprint data is not available, show the phase and initiative without a task list.\n\nIf the user provided `$ARGUMENTS`, acknowledge the active session AND relay their intent as the suggested next action.\n\n**RESUME_PARKED** (one or more sessions with status=PARKED, none ACTIVE):\n\n*Single parked session*:\n```\nParked session found: {session_id}\nInitiative: {initiative} (parked {relative_time})\n\nResume with: /sos resume\n```\n\n*Multiple parked sessions*:\n```\nFound {n} parked sessions:\n\n1. {session_id_1} — {initiative_1} (parked {relative_time})\n2. {session_id_2} — {initiative_2} (parked {relative_time})\n\nResume one with: /sos resume\nStart fresh with: /sos start \"<initiative>\"\n```\n\n**NEW_WORK** ($ARGUMENTS provided, no active/parked sessions):\n\n```\nNo active sessions.\n\nYour intent: {$ARGUMENTS}\n\nGet started:\n  /consult \"{$ARGUMENTS}\"  — Get rite recommendation and workflow routing\n  /sos start \"{$ARGUMENTS}\"    — Start directly with current rite ({active_rite})\n```\n\n**ORIENTATION** (nothing active, no arguments):\n\n```\nRite: {active_rite or \"none\"}\nSessions: {count active} active, {count parked} parked\n\nOptions:\n  /go <task>         — Show dashboard with intent\n  /consult           — Get guidance on what to do\n  /sos start <task>  — Start a new session\n  /sessions      — Browse all sessions\n  /rite --list   — See available rites\n```\n\nIf `.sos/wip/` artifacts exist without a session, mention them:\n\n```\nNote: Found {n} artifacts in .wip/ with no active session.\nReview with: ls .sos/wip/\n```\n\n### Realization-Lens Panel (Full Mode)\n\nAfter the state block, render a realization-lens section showing per-row detail. Render EXACTLY two signal sets:\n\n```\nRealization-Lens:\n  Live eunomia refusals:\n    - VERDICT-{slug1}-close.md     refused {time}  {1-line excerpt}\n    - VERDICT-{slug2}-handoff.md   advisory {time} {1-line excerpt}\n  Inception gaps:\n    - {initiative-slug-1}   active {age}   NO telos declared\n    - {initiative-slug-2}   framed {age}   NO telos declared\n```\n\n- Live refusals: list up to 5 most recent `.sos/wip/eunomia/VERDICT-*.md`, ordered by mtime descending. Excerpt = first non-empty body line.\n- Inception gaps: list every open/framed initiative missing `.know/telos/{slug}.md`. For each, show age since session creation or frame mtime.\n- Omit either subsection when empty. Omit the whole panel when both are empty.\n\nSignal-set discipline (LOAD-BEARING): render ONLY live refusals and inception gaps. Do NOT render closed-unattested counts, N_applied aggregates, aggregate health grades, or percentage-realized rollups. The panel is ambient signal only — it surfaces what the user should notice, not a composite score.\n\n## Behavioral Rules\n\n1. **Present, don't dispatch.** `/go` shows status and suggests commands. It does NOT invoke `/sos start`, `/sos resume`, `/consult`, or any other command. The user decides what to do next.\n\n2. **Read everything, ask almost nothing.** Collect all state in parallel, then present. Never ask \"do you have an active session?\" — check it yourself.\n\n3. **One question maximum.** If state is unambiguous, present it. Only ask when multiple parked sessions exist and the user needs to choose.\n\n4. **No mythology.** Use plain operational vocabulary. Session, rite, phase, sprint.\n\n5. **No preamble.** Do not say \"Let me check your session state...\" Just collect, decide, and present.\n\n6. **Terse output.** Tables and short lines, not paragraphs. Every extra line costs time-to-productive.\n\n## Examples\n\n```bash\n# Brief status card (default)\n/go\n\n# Full dashboard\n/go --full\n\n# Brief with intent\n/go \"add user authentication to the API\"\n\n# Full with intent\n/go --full \"add user authentication to the API\"\n```\nUser arguments: {{args}}"
