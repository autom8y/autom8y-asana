"""gfr-dynvocab operator tooling (retained scripts, not shipped engine code).

Contains the GAP-1 probe harness. NOT under pytest ``testpaths`` (``["tests"]``),
so the certified suite never auto-collects it. Importing this package has no
side effects.
"""
